---
phase: 02-d-bus-client-device-list
plan: 05
type: execute
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - src/logiops-gui/dbus/logid_device.xml
  - src/logiops-gui/DaemonConnection.cpp
  - src/logiops-gui/qml/DeviceDelegate.qml
  - src/logiops-gui/qml/DetailPane.qml
  - src/logiops-gui/icons/battery.svg
  - src/logiops-gui/icons/charging.svg
  - src/logiops-gui/CMakeLists.txt
  - test/smoke/device-list.sh
autonomous: false
requirements: [DEV-02]
must_haves:
  truths:
    - "The device proxy XML carries the daemon's Battery/Charging/BatteryKnown properties + BatteryChanged signal, and DaemonConnection wires BatteryChanged -> DeviceModel.onBatteryChanged live (no polling)"
    - "The list item and detail pane show numeric battery % + battery glyph + charging bolt, with threshold colors, and '—' (muted) when BatteryKnown is false"
    - "Battery updates arrive via the signal and repaint only the battery roles (no flicker, no reorder, no full reset)"
    - "The live smoke script asserts the daemon's Battery property is present and readable"
  artifacts:
    - path: "src/logiops-gui/dbus/logid_device.xml"
      provides: "Battery/Charging/BatteryKnown property + BatteryChanged signal added to the .Device interface"
      contains: "BatteryChanged"
    - path: "src/logiops-gui/qml/DeviceDelegate.qml"
      provides: "Battery block: %+glyph+charging bolt, thresholds, '—' when unknown"
      contains: "batteryKnown"
  key_links:
    - from: "src/logiops-gui/DaemonConnection.cpp"
      to: "DeviceModel.onBatteryChanged"
      via: "connect per-device proxy BatteryChanged + initial Battery read"
      pattern: "onBatteryChanged|BatteryChanged"
    - from: "src/logiops-gui/qml/DeviceDelegate.qml"
      to: "batteryPercent/charging/batteryKnown roles"
      via: "per-role bindings with threshold color"
      pattern: "batteryPercent"
---

<objective>
Wire the real battery data end to end now that the daemon exposes it (Plan 02) and the model/shell exist (Plans 03/04). Extend the device proxy XML with the daemon's `Battery`/`Charging`/`BatteryKnown` properties + `BatteryChanged` signal, connect that signal (plus an initial read) into `DeviceModel.onBatteryChanged`, and render the battery block (numeric % + glyph + charging bolt + threshold colors, "—" when unknown) in the list item and detail pane. Finalize the live smoke assertion for the Battery property.

Purpose: DEV-02 becomes real, live, signal-driven battery — the last piece of the phase goal.
Output: updated `logid_device.xml` + regenerated proxy, `DaemonConnection.cpp` battery wiring, `DeviceDelegate.qml`/`DetailPane.qml` battery UI, battery/charging SVGs, smoke assertion.

Wave 3: depends on Plan 02 (daemon Battery property/signal must exist) AND Plans 03/04 (model + shell). Has a human-verify checkpoint for live battery on real hardware.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-d-bus-client-device-list/02-RESEARCH.md
@.planning/phases/02-d-bus-client-device-list/02-UI-SPEC.md
@.planning/phases/02-d-bus-client-device-list/02-02-PLAN.md

<interfaces>
<!-- Daemon battery surface added by Plan 02 (pizza.pixl.LogiOps.Device): -->
<!--   property Battery       y (uint8 percentage, RO) -->
<!--   property Charging      b (RO) -->
<!--   property BatteryKnown  b (RO) -->
<!--   signal   BatteryChanged(y percentage, b charging, b known) -->
<!-- DeviceModel slot (Plan 03): -->
<!--   void onBatteryChanged(const QString& path, int percent, bool charging, bool known); -->
<!-- DeviceModel roles already present: BatteryPercentRole, BatteryKnownRole, ChargingRole -->
<!-- UI-SPEC battery thresholds: >20% green / <=20% amber / charging -> bolt (green) regardless / -->
<!--   unknown -> no fill, "—" muted, no warning color. Label 14px tabular figures (stable width). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend device proxy XML + wire BatteryChanged into the model</name>
  <read_first>
    - .planning/phases/02-d-bus-client-device-list/02-02-PLAN.md (the EXACT daemon battery surface Plan 02 landed: property names Battery/Charging/BatteryKnown, signal BatteryChanged(percentage,charging,known))
    - src/logiops-gui/dbus/logid_device.xml (the trimmed .Device XML from Plan 03 — add the battery members)
    - src/logiops-gui/DaemonConnection.cpp (Plan 04 — addDevice(): where per-device proxy is built, properties read, StatusChanged connected; add the battery equivalents here)
    - src/logiops-gui/DeviceModel.h (onBatteryChanged slot signature)
  </read_first>
  <action>
VERIFY-FIRST: introspect the live daemon (`busctl --system introspect pizza.pixl.LogiOps /pizza/pixl/logiops/devices/0`) to confirm the exact battery property/signal names + D-Bus types Plan 02 produced, in case they differ from the planned `Battery`/`Charging`/`BatteryKnown`/`BatteryChanged`. Use the verified names.

1. Add to `src/logiops-gui/dbus/logid_device.xml` (inside `<interface name="pizza.pixl.LogiOps.Device">`):
   - `<property name="Battery" type="y" access="read"/>`
   - `<property name="Charging" type="b" access="read"/>`
   - `<property name="BatteryKnown" type="b" access="read"/>`
   - `<signal name="BatteryChanged"><arg name="percentage" type="y"/><arg name="charging" type="b"/><arg name="known" type="b"/></signal>`
   The regenerated proxy gains `battery()`/`charging()`/`batteryKnown()` accessors + a `BatteryChanged(uchar,bool,bool)` signal.

2. In `DaemonConnection.cpp` `addDevice(...)`:
   - After the initial async read of name/pid/active, also async-read `battery()`/`charging()`/`batteryKnown()` and call `_model->onBatteryChanged(path, percent, charging, known)` (initial value).
   - Connect the per-device proxy's `BatteryChanged(uchar percentage, bool charging, bool known)` to a lambda calling `_model->onBatteryChanged(path, percentage, charging, known)` — the LIVE no-poll path (CONF-03). Guard against UI-thread blocking (QDBusPendingCallWatcher for the initial reads, Pitfall 4).
   - If the proxy reports the Battery property is absent (older daemon / unsupported device), call `onBatteryChanged(path, 0, false, /*known=*/false)` so the row shows "—" gracefully.

No CMake change needed beyond re-running generation (the XML is already in the `qt_add_dbus_interface` list from Plan 03); confirm it rebuilds.
  </action>
  <verify>
    <automated>grep -q "BatteryChanged" src/logiops-gui/dbus/logid_device.xml && grep -q "name=\"Battery\"" src/logiops-gui/dbus/logid_device.xml && grep -q "onBatteryChanged" src/logiops-gui/DaemonConnection.cpp && cmake --build build --target logiops-gui 2>&1 | tail -6 && echo PASS</automated>
  </verify>
  <done>The device proxy XML carries Battery/Charging/BatteryKnown + BatteryChanged; DaemonConnection reads the initial battery and subscribes BatteryChanged into DeviceModel.onBatteryChanged live; missing property degrades to "—". Executable rebuilds under -Werror.</done>
</task>

<task type="auto">
  <name>Task 2: Battery UI in list item + detail pane + smoke assertion</name>
  <read_first>
    - .planning/phases/02-d-bus-client-device-list/02-UI-SPEC.md (battery fill thresholds: >20% green / <=20% amber / charging bolt green regardless / unknown -> "—" muted no warning; Label 14px tabular figures; item anatomy point 4 "Battery block"; no-flicker Behavior on color/width 200ms)
    - src/logiops-gui/qml/DeviceDelegate.qml (Plan 04 — the battery block placeholder "—" to replace)
    - src/logiops-gui/qml/DetailPane.qml (Plan 04 — the battery section stub to fill)
    - test/smoke/device-list.sh (Plan 01 — the best-effort Battery read to turn into a real assertion)
  </read_first>
  <action>
1. `src/logiops-gui/qml/DeviceDelegate.qml` — replace the battery placeholder with the real block, right-aligned, per UI-SPEC anatomy 4:
   - When `model.batteryKnown`: a battery glyph (`icons/battery.svg`) + numeric `model.batteryPercent + "%"` in Label 14px with tabular figures (stable width — no horizontal jitter on update); fill/text color = `model.batteryPercent > 20 ? Theme.charging : Theme.warning`. When `model.charging`, overlay `icons/charging.svg` bolt in `Theme.charging` (green) regardless of level.
   - When `!model.batteryKnown`: show `"—"` in `Theme.mutedForeground`, no glyph fill color, no charging bolt, no warning color.
   - Per-role bindings only (bind `model.batteryPercent`/`model.charging`/`model.batteryKnown` directly) so a battery tick repaints ONLY this block — no row move, no list reorder (no-flicker contract). `Behavior on color` ~200ms ease-out for fill changes; no bounce.
   - `Accessible.name` includes the battery state (e.g. "80 percent, charging") per UI-SPEC accessibility, updating on change.

2. `src/logiops-gui/qml/DetailPane.qml` — fill the "Battery" section: Heading "Battery", then the numeric %/charging state or "Battery status unavailable" (muted) when unknown. Read-only (no controls — Fase 3).

3. `icons/battery.svg`, `icons/charging.svg` — simple tintable glyphs; add to the QML module resources in CMake.

4. `test/smoke/device-list.sh` — promote the best-effort Battery read (from Plan 01) into a real assertion: if the daemon owns the name and >=1 device, `busctl --system get-property pizza.pixl.LogiOps <path> pizza.pixl.LogiOps.Device Battery` must return a `y` value in 0..100 (PASS) OR, if the device legitimately doesn't report battery, BatteryKnown==false (still PASS, not FAIL). Keep the graceful-skip when daemon/hardware absent.
  </action>
  <verify>
    <automated>grep -q "batteryKnown" src/logiops-gui/qml/DeviceDelegate.qml && grep -q "batteryPercent" src/logiops-gui/qml/DeviceDelegate.qml && test -f src/logiops-gui/icons/battery.svg && grep -q "Battery" test/smoke/device-list.sh && bash -n test/smoke/device-list.sh && cmake --build build --target logiops-gui 2>&1 | tail -5 && echo PASS</automated>
  </verify>
  <done>List item + detail pane render numeric %+glyph+charging bolt with threshold colors and "—" when unknown; battery updates repaint only the battery roles (no flicker/reorder); the smoke script asserts the live Battery property; executable builds clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify live battery display (numeric %, charging bolt, threshold color, "—" when unknown, no flicker)</name>
  <action>Manual verification by the operator — no automated action; build and run logiops-gui against a live daemon with a battery-reporting device and confirm the behaviors below.</action>
  <what-built>Live battery display end to end: the daemon's BatteryChanged signal driving the GUI battery block (numeric % + charging bolt + threshold color) with no flicker.</what-built>
  <how-to-verify>
1. Ensure the daemon (with Plan 02's battery feature) is running and you're in `logiops`. Build + run `logiops-gui`.
2. Confirm each battery-reporting device shows a numeric % + battery glyph in the sidebar; the detail pane "Battery" section matches.
3. Put the mouse on/off the charger (or let it drain) — the % and charging bolt update LIVE without restarting the app, and the row does NOT jump/re-sort or blink (no-flicker).
4. Confirm a device that doesn't report battery shows "—" (muted), not a fake 0% or a warning color.
5. Run `bash test/smoke/device-list.sh` — it PASSES or SKIPs (never FAILs) on your setup.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. battery not updating live, flicker on tick, wrong threshold color, "—" not shown for unsupported device).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daemon BatteryChanged signal -> GUI | The GUI trusts battery values from the well-known bus name; rendered as text/fill only |
| HID device -> daemon battery decoder | (upstream, Plan 02) untrusted 0x1000 report — bounds-checked there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Tampering | out-of-range battery percentage (>100) from a spoofed/buggy signal rendering oddly | mitigate | Clamp displayed percentage to 0..100 in the delegate binding; treat known=false as the safe default |
| T-02-16 | Spoofing | rogue owner emitting fake BatteryChanged | accept | Phase 1 system-bus policy restricts owning the name to root; GUI subscribes only to that name |
| T-02-17 | DoS | rapid BatteryChanged storms causing repaint churn | accept | Per-role dataChanged repaints only the battery block; Qt coalesces; acceptable for a settings panel |
</threat_model>

<verification>
- `cmake --build build --target logiops-gui` builds under -Werror.
- `bash test/smoke/device-list.sh` PASSES/SKIPs and asserts the Battery property when a live daemon+device is present.
- Human-verify checkpoint: live battery updates with no flicker, "—" for unsupported devices, correct threshold colors.
</verification>

<success_criteria>
- DEV-02 real and live: daemon Battery/Charging/BatteryKnown + BatteryChanged wired through DaemonConnection into DeviceModel, rendered as %+glyph+bolt with thresholds and "—" when unknown.
- Battery updates are signal-driven (no polling) and repaint only battery roles (no flicker/reorder).
- Smoke asserts the live Battery property.
</success_criteria>

<output>
After completion, create `.planning/phases/02-d-bus-client-device-list/02-05-SUMMARY.md`.
</output>
