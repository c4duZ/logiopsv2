---
phase: 03-core-config-ui-persistence
plan: 03
type: execute
wave: 3
depends_on: [00, 01, 02]
files_modified:
  - src/logid/config/schema.h
  - src/logiops-gui/DeviceController.h
  - src/logiops-gui/DeviceController.cpp
  - src/logiops-gui/qml/config/PointerTab.qml
  - src/logiops-gui/qml/config/DpiCycleEditor.qml
  - src/logiops-gui/qml/config/ScrollTab.qml
  - src/logiops-gui/CMakeLists.txt
  - tests/phase3/DpiCycleTest.cpp
  - tests/phase3/CMakeLists.txt
autonomous: false
requirements: [DPI-01, DPI-02, DPI-03, SCR-01, SCR-02, SCR-03]
user_setup: []

must_haves:
  truths:
    - "User can drag a DPI slider bounded by the device's reported min/max and see the live snapped value (DPI-01)"
    - "User can add/remove a device-scoped list of DPI cycle presets, each with an editable label, and both values and labels persist via Save() (DPI-02, DPI-03)"
    - "User can toggle/tune SmartShift (on/off + threshold + torque, torque gated by support) (SCR-01)"
    - "User can toggle hi-res scroll and invert direction (SCR-02)"
    - "User can set thumbwheel divert/invert plus a tap simple action on supported devices (SCR-03)"
    - "Every scroll sub-section is hidden when its capability is absent"
  artifacts:
    - path: "src/logiops-gui/qml/config/PointerTab.qml"
      provides: "DPI slider + cycle editor (DPI-01..03)"
      min_lines: 50
    - path: "src/logiops-gui/qml/config/ScrollTab.qml"
      provides: "capability-gated SmartShift/hires/thumbwheel controls (SCR-01..03)"
      min_lines: 60
    - path: "src/logiops-gui/qml/config/DpiCycleEditor.qml"
      provides: "labeled DPI preset list editor (DPI-02/03)"
      min_lines: 40
  key_links:
    - from: "src/logiops-gui/qml/config/PointerTab.qml"
      to: "DeviceController.setDpi plus .DPI.SetDPI"
      via: "slider onMoved -> async setter (daemon snaps)"
      pattern: "setDpi|dpiMin|dpiMax"
    - from: "src/logiops-gui/qml/config/DpiCycleEditor.qml"
      to: "DeviceController preset accessors (addPreset/removePreset/setPresetLabel) writing the chosen persistence target from Task 0"
      via: "add/remove/relabel preset rows -> async writeback (option-a device-scoped .DPI preset IPC; option-b first CycleDPI button action)"
      pattern: "addPreset|dpiPresets|setPresetLabel"
    - from: "src/logiops-gui/qml/config/ScrollTab.qml"
      to: "DeviceController.hasSmartShift / hasHires / hasThumbwheel"
      via: "section visibility gating"
      pattern: "hasSmartShift|hasHires|hasThumbwheel"
    - from: "src/logiops-gui/qml/config/ScrollTab.qml"
      to: "pizza.pixl.LogiOps.ThumbWheel.SetTap"
      via: "tap action assignment (reuses action picker)"
      pattern: "SetTap|setTap"
---

<objective>
Build the Pointer and Scroll tabs. Pointer: a min/max-bounded DPI sensitivity slider (DPI-01) plus a labeled DPI-cycle preset editor with add/remove (DPI-02, DPI-03). Scroll: capability-gated SmartShift (on/off + threshold + torque), hi-res (toggle + invert), and thumbwheel (divert/invert + tap action) sections (SCR-01..03). Includes one daemon decision with TWO coupled parts: where the DPI-cycle VALUE list lives (since the only first-class cycle list today is on a per-button CycleDPI action, not device-scoped) AND where the per-preset labels live (the schema has no label field today).

Purpose: A vertical slice over the .DPI/.SmartShift/.HiresScroll/.ThumbWheel surface, isolated by file ownership from Buttons (Plan 02) and Profiles (Plan 04). Both tabs bind to the DeviceController capability/value properties from Plan 01.
Output: PointerTab + DpiCycleEditor + ScrollTab QML, DeviceController value/setter extensions, and the chosen DPI cycle-list + labels persistence.

Ordering note: depends_on includes 02 because this plan mutates src/logiops-gui/CMakeLists.txt and tests/phase3/CMakeLists.txt (also owned by Plan 02) AND its Scroll-tab thumbwheel-tap mapping reuses Plan 02's categorized action picker (ReassignPanel). The shared-file overlap and the ReassignPanel reuse force this plan into a later wave than 02; an orchestrator reading depends_on alone must not schedule it in parallel with 02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-core-config-ui-persistence/03-RESEARCH.md
@.planning/phases/03-core-config-ui-persistence/03-UI-SPEC.md

<interfaces>
.DPI @ device node: GetDPIs(y sensor) returns (ai dpis, q step, b isRange); GetDPI(y) returns q; SetDPI(q dpi, y sensor); GetSensors returns y.
  Daemon snaps to dpiStep/range via getClosestDPI (DPI.cpp). Do NOT round GUI-side.
.SmartShift @ device node: GetConfig returns (y active, y threshold, y torque); SetActive(b, b clear); SetThreshold(y, b clear); SetTorque(y, b clear); property TorqueSupport(b).
.HiresScroll @ device node: GetConfig returns (b hires, b invert, b target); SetHires(b); SetInvert(b); SetTarget(b).
.ThumbWheel @ device node: GetConfig returns (b divert, b invert); SetDivert(b); SetInvert(b); SetLeft(s type); SetRight(s type); SetTap(s type).
  VERIFIED (src/logid/features/ThumbWheel.cpp:334 setTap): SetTap builds a simple action via actions::Action::makeAction at .../thumb_wheel/tap, so assigning a SIMPLE action to tap works today, NO daemon change for tap (resolves RESEARCH A2). left/right are gesture-typed (Phase 4 territory); this phase exposes the tap simple-action path plus divert/invert.
DPI cycle list (DPI-02) — CRITICAL SCOPING NOTE: The only first-class cycle-VALUE list in the daemon today is config::CycleDPI, which exists ONLY as a per-BUTTON action interface (.Action.CycleDPI with SetDPIs(ai)/GetDPIs()) created AFTER Button.SetAction("CycleDPI"). VERIFIED (src/logid/actions/CycleDPI.cpp): the SetDPIs/GetDPIs methods are registered on the per-button Action node (Action(device, "CycleDPI", {...})), NOT on the device node — there is NO device-scoped CycleDPI interface and NO ".DPI.SetDPIs". The UI-SPEC's standalone Pointer-tab cycle editor has NO button context, so the cycle VALUES need an explicit home. Both the cycle VALUES (DPI-02) and the per-preset LABELS (DPI-03) are decided together in Task 0 (the schema has no label field anywhere either). Do NOT write the plan as "values round-trip through CycleDPI.SetDPIs" without first resolving WHICH node the Pointer-tab list writes to.
DeviceController (Plan 01) already exposes hasSmartShift/hasHires/hasThumbwheel/hasTorque, dpiMin/dpiMax/dpiStep/sensorCount and seeded values dpi/smartShift*/hires*/thumb*; extend with cycle preset + label accessors here.
</interfaces>

@src/logid/features/DPI.cpp
@src/logid/features/SmartShift.cpp
@src/logid/features/HiresScroll.cpp
@src/logid/features/ThumbWheel.cpp
@src/logid/config/schema.h
@src/logiops-gui/DeviceController.h
@src/logiops-gui/qml/config/ConfigTabs.qml
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Decide where the DPI-cycle VALUES and per-preset LABELS live (DPI-02, DPI-03)</name>
  <decision>The standalone Pointer-tab DPI-cycle editor needs (1) a home for the cycle VALUE list and (2) per-preset LABELS, but the daemon has NEITHER a device-scoped cycle list NOR any label field. The only cycle list today (config::CycleDPI / .Action.CycleDPI.SetDPIs) lives on a per-BUTTON action node, so a Pointer-tab editor with no button context has no node to write to. Where do the cycle VALUES persist, and where do the LABELS persist?</decision>
  <context>
    VERIFIED (src/logid/actions/CycleDPI.cpp): SetDPIs/GetDPIs are registered on the per-button Action node, not on the device node. There is no ".DPI.SetDPIs" and no device-scoped CycleDPI. So "values round-trip through CycleDPI.SetDPIs" is underspecified for a standalone editor — it never says WHICH button's action node receives them. CONTEXT locked "a separate DPI-cycle list editor with add/remove and per-preset labels" and presents it device-scoped on the Pointer tab, but left the persistence mechanism to Claude's discretion (RESEARCH A1, Open Question 1). This decision determines whether a daemon schema + IPC addition is needed and whether the cycle values + labels survive in /etc/logid.cfg via Save().

    DEFAULT RECOMMENDATION: option (a) — a device/profile-scoped preset list — because the UI presents the editor device-scoped (no button context), so the persistence should be device-scoped too rather than silently bound to one button.
  </context>
  <options>
    <option id="option-a">
      <name>(RECOMMENDED) Device/profile-scoped DPI-preset list (values + labels) in the schema + a writable .DPI IPC method</name>
      <pros>
        - Cycle VALUES persist device-scoped, independent of any button binding — matches how the UI presents the editor (standalone Pointer tab, no button context).
        - LABELS persist in the cfg via Save() like every other setting; single source of truth; faithful to Options+.
        - No silent coupling to "the first CycleDPI button" that the user never sees.
      </pros>
      <cons>
        - Touches src/logid/config/schema.h: add a device/profile-scoped preset list holding {value, optional label} pairs (e.g. an optional list on config::DPI or on config::Profile), and register the field in the group field list (mirror the existing dpis/sensor registration pattern).
        - Add a small .DPI IPC method pair to read/write the device-scoped preset list (GetPresets/SetPresets following the DPI.cpp IPC pattern), so cycle values persist independently of any button binding.
        - Must re-verify the save() round-trip with the new field — the Plan 00 round-trip test (DpiCycleTest + the Plan 00 save_roundtrip test) is EXTENDED to assert BOTH the preset values AND labels survive a load -> mutate -> Save -> re-parse cycle.
      </cons>
    </option>
    <option id="option-b">
      <name>(FALLBACK) Bind the Pointer-tab editor to a specific CycleDPI button action; labels GUI-local</name>
      <pros>
        - Zero daemon schema change for VALUES; cycle values ride the existing .Action.CycleDPI.SetDPIs on a chosen button (e.g. the first button currently set to CycleDPI, or one the GUI sets to CycleDPI on demand).
        - Ships against the daemon's actual model.
      </pros>
      <cons>
        - The Pointer-tab list is no longer truly device-scoped: it is the preset list of ONE button action. The UI MUST surface this linkage explicitly (e.g. "These presets are used by buttons set to Cycle DPI") so the user understands which binding they edit.
        - LABELS still have no schema home -> they live GUI-local (QSettings keyed by devicePath); labels do NOT move with the cfg if shared (Phase 5 sharing); source-of-truth split.
        - If no button is set to CycleDPI, the editor must create/own one or be empty — extra edge-case wiring + UI explanation.
      </cons>
    </option>
  </options>
  <resume-signal>Select: option-a (RECOMMENDED — add a device/profile-scoped preset list {value,label} to the schema + GetPresets/SetPresets on .DPI, implement in Task 1, and EXTEND the Plan 00 round-trip test to assert values AND labels survive Save) OR option-b (bind the editor to a specific CycleDPI button action, surface that linkage in the UI, store labels GUI-local via QSettings, skip the schema edit)</resume-signal>
</task>

<task type="auto">
  <name>Task 1: DeviceController DPI-cycle + labels accessors (plus chosen persistence target) + unit test</name>
  <files>src/logiops-gui/DeviceController.h, src/logiops-gui/DeviceController.cpp, src/logid/config/schema.h, tests/phase3/DpiCycleTest.cpp, tests/phase3/CMakeLists.txt, src/logiops-gui/CMakeLists.txt</files>
  <read_first>
    - src/logiops-gui/DeviceController.h (existing Q_PROPERTY/setter layout from Plan 01, extend, do not rewrite)
    - src/logid/config/schema.h (struct CycleDPI at :93, the signed_group / group field registration pattern; struct DPI and struct Profile as candidate homes for a device/profile-scoped preset list, if option-a)
    - src/logid/actions/CycleDPI.cpp (GetDPIs/SetDPIs at :52-63 — CONFIRM these live on the per-BUTTON Action node, NOT the device node; this is exactly why option-a adds a device-scoped surface)
    - src/logid/features/DPI.cpp (SetDPI/GetDPIs plus getClosestDPI snapping; confirm GUI must NOT pre-round; this is where option-a's GetPresets/SetPresets IPC pattern is mirrored)
    - the generated logid_dpi_proxy.h
  </read_first>
  <action>
    Extend DeviceController with cycle-list state: Q_PROPERTY dpiPresets (a QVariantList or sub-model of {value,label}) plus Q_INVOKABLE addPreset(int dpi, QString label), removePreset(int index), setPresetLabel(int index, QString label), setPresetValue(int index, int dpi).

    Persistence target is decided in Task 0 and MUST be implemented as the chosen path (no hand-wave):
    - IF option-a (RECOMMENDED): add a device/profile-scoped preset list holding {value, optional label} to the schema (extend struct config::DPI or config::Profile in schema.h and register the new field in its group field list, mirroring the existing dpis/sensor registration at :100-102); add GetPresets/SetPresets to the .DPI feature IPC (follow the DPI.cpp IPC ctor pattern) that read/write that schema list so Save() persists BOTH values and labels. DeviceController.addPreset/removePreset/setPresetLabel/setPresetValue call .DPI.SetPresets async with the full {value,label} list. EXTEND tests/phase3/save_roundtrip_test.cpp (Plan 00) to assert preset values AND labels survive a load -> mutate -> Save -> re-parse round-trip.
    - IF option-b (FALLBACK): cycle VALUES ride .Action.CycleDPI.SetDPIs on the explicitly-chosen button action (resolve/create that button in DeviceController and expose which button it is so the UI can surface the linkage); LABELS use QSettings keyed by devicePath. Document the chosen button linkage in DeviceController so PointerTab can show "used by buttons set to Cycle DPI".

    Ensure setDpi(int) calls .DPI.SetDPI async and does NOT round GUI-side (daemon snaps), then re-reads GetDPI to reflect the snapped value. Write DpiCycleTest: assert add/remove/label edits update the model, dpi values clamp to [dpiMin,dpiMax] before emitting, and the preset list is written to the CHOSEN persistence target (option-a: a .DPI.SetPresets payload carrying {value,label} pairs; option-b: a CycleDPI.SetDPIs payload on the linked button plus a QSettings label write) — assert the ACTUAL path exercised, not a generic "round-trip through CycleDPI.SetDPIs". Add any new .cpp to logiops-gui-lib.
  </action>
  <verify>
    <automated>cmake --build build && ctest --test-dir build -R 'phase3_dpi|phase3_save_roundtrip' --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `ctest -R phase3_dpi_cycle` exits 0; asserts add/remove/relabel, dpi clamp to [min,max], AND that a preset edit emits the chosen-path payload: option-a -> a .DPI.SetPresets call carrying the full {value,label} list; option-b -> a CycleDPI.SetDPIs call on the linked button PLUS a QSettings label write. (No assertion may stop at "round-trip through CycleDPI.SetDPIs" without naming the node.)
    - If option-a: `grep -q "preset\|Preset" src/logid/config/schema.h` within the chosen struct (config::DPI or config::Profile) AND a labels/label field is registered AND the EXTENDED `phase3_save_roundtrip` asserts BOTH preset values AND labels survive Save; daemon builds -Werror clean.
    - If option-b: the DpiCycleTest asserts the SetDPIs payload targets the explicitly-linked button action node AND a QSettings label store, and DeviceController exposes which button the editor is bound to.
    - `grep -q "addPreset\|dpiPresets" src/logiops-gui/DeviceController.h`.
    - `grep -q "SetDPI" src/logiops-gui/DeviceController.cpp` and no GUI-side rounding of dpi values.
  </acceptance_criteria>
  <done>DeviceController manages the labeled DPI-preset list; cycle VALUES persist via the Task-0-chosen node (option-a: device-scoped .DPI.SetPresets in the schema; option-b: an explicitly-linked CycleDPI button action with the UI surfacing that linkage), LABELS persist per Task 0, the DPI test asserts the actual persistence path, and (option-a) the round-trip test proves both values and labels persist through Save.</done>
</task>

<task type="auto">
  <name>Task 2: Pointer tab (DPI slider + cycle editor) — DPI-01/02/03</name>
  <files>src/logiops-gui/qml/config/PointerTab.qml, src/logiops-gui/qml/config/DpiCycleEditor.qml, src/logiops-gui/CMakeLists.txt</files>
  <read_first>
    - .planning/phases/03-core-config-ui-persistence/03-UI-SPEC.md (Pointer/DPI section: bounded slider tokens, tabular-stable readout with "DPI" unit, cycle chips with editable labels, "+ Add preset" secondary button, destructive remove, accentTint active chip, empty-cycle helper copy)
    - src/logiops-gui/qml/Theme.qml (controlRowHeight, accent track-fill, hairline track, destructive, accentTint; motionFast)
    - src/logiops-gui/qml/DeviceDelegate.qml (TextMetrics fixed-width numeric readout pattern; Theme bindings; qsTr)
    - .planning/phases/03-core-config-ui-persistence/03-RESEARCH.md (Code Examples: bound DPI slider snippet; "onMoved not per-pixel" to avoid D-Bus storms)
  </read_first>
  <action>
    PointerTab.qml: a Slider with from deviceController.dpiMin, to deviceController.dpiMax, stepSize deviceController.dpiStep, value deviceController.dpi, and onMoved calling deviceController.setDpi(value) (call the setter on onMoved/onPressedChanged, NOT every pixel). Track unfilled hairline, filled accent, thumb accent with accent focus ring on keyboard focus; live numeric readout (Body 16, TextMetrics tabular-stable) to the right showing the value plus qsTr(" DPI"). Below it, DpiCycleEditor.qml: a list of preset rows/chips bound to deviceController.dpiPresets, each a DPI value plus an editable label TextField (Label 14); "+ Add preset" secondary button calling deviceController.addPreset; per-row remove with a destructive-colored affordance calling removePreset; the active preset chip uses accentTint bg + accent text. IF Task 0 selected option-b, also render the linkage hint copy qsTr("These presets are used by buttons set to Cycle DPI.") so the user knows the editor edits a button action's list. Empty-state helper copy: qsTr("No DPI presets yet. Add one to cycle through sensitivities."). Reordering out of scope. Replace the Plan 01 Pointer placeholder in ConfigTabs. Add both QML files to qt_add_qml_module QML_FILES.
  </action>
  <verify>
    <automated>cmake --build build --target logiops-gui 2>&1 | grep -qiv error && grep -q "dpiMin\|setDpi" src/logiops-gui/qml/config/PointerTab.qml && grep -q "addPreset\|Add preset" src/logiops-gui/qml/config/DpiCycleEditor.qml</automated>
  </verify>
  <acceptance_criteria>
    - GUI builds clean with both QML files in QML_FILES.
    - `grep -q "dpiMin" src/logiops-gui/qml/config/PointerTab.qml` (bounds from device, not hardcoded).
    - `grep -q "onMoved" src/logiops-gui/qml/config/PointerTab.qml` (no per-pixel D-Bus storm).
    - `grep -q "No DPI presets yet" src/logiops-gui/qml/config/DpiCycleEditor.qml` (exact empty-cycle copy).
    - `grep -q "destructive" src/logiops-gui/qml/config/DpiCycleEditor.qml` (remove affordance).
    - If Task 0 selected option-b: `grep -q "buttons set to Cycle DPI" src/logiops-gui/qml/config/DpiCycleEditor.qml` (linkage surfaced).
  </acceptance_criteria>
  <done>Pointer tab renders a device-bounded DPI slider with a stable numeric readout and a labeled add/remove cycle editor bound to the Task-0-chosen persistence path; values ride the daemon (which snaps). Live/on-hardware DPI behavior is in the manual checklist.</done>
</task>

<task type="auto">
  <name>Task 3: Scroll tab (SmartShift + hi-res + thumbwheel, all capability-gated) — SCR-01/02/03</name>
  <files>src/logiops-gui/qml/config/ScrollTab.qml, src/logiops-gui/CMakeLists.txt</files>
  <read_first>
    - .planning/phases/03-core-config-ui-persistence/03-UI-SPEC.md (Scroll section: SmartShift toggle+threshold+torque with sliders greyed while OFF; hi-res toggle+invert; thumbwheel divert/invert + tap reusing the action picker; the gating rule "unsupported feature section -> hide whole section")
    - src/logiops-gui/DeviceController.h (hasSmartShift/hasHires/hasThumbwheel/hasTorque plus smartShift*/hires*/thumb* properties + setters from Plan 01)
    - src/logiops-gui/qml/config/ReassignPanel.qml (Plan 02, the action-picker entry point thumbwheel tap reuses; depends_on includes 02 so this file should be present; if absent at execution time, mirror its categorized-picker structure)
    - src/logiops-gui/qml/Theme.qml (accent ON-state toggles, controlRowHeight, motionFast)
  </read_first>
  <action>
    ScrollTab.qml with three capability-gated sections (each wrapped with visible bound to deviceController.hasX, hidden entirely when absent, mirroring daemon UnsupportedFeature): (1) SmartShift — on/off Switch (accent when ON, bound smartShiftActive/setSmartShiftActive) + threshold Slider + torque Slider; sliders disabled/greyed while the toggle is OFF; torque slider visible bound to deviceController.hasTorque. (2) Hi-res scroll — on/off Switch (hiresOn/setHires) + invert-direction CheckBox (hiresInvert/setHiresInvert). (3) Thumbwheel — divert + invert Switches (thumbDivert/setThumbDivert, thumbInvert/setThumbInvert) + a tap action mapping reusing the same categorized action-picker entry point as the Buttons reassign panel (ReassignPanel from Plan 02), via the simple-action path .ThumbWheel.SetTap(type) (VERIFIED simple action works). All toggles use accent ON state per the UI-SPEC reserved-accent list. Replace the Plan 01 Scroll placeholder in ConfigTabs. Add ScrollTab.qml to QML_FILES.
  </action>
  <verify>
    <automated>cmake --build build --target logiops-gui 2>&1 | grep -qiv error && grep -q "hasSmartShift" src/logiops-gui/qml/config/ScrollTab.qml && grep -q "hasThumbwheel" src/logiops-gui/qml/config/ScrollTab.qml</automated>
  </verify>
  <acceptance_criteria>
    - GUI builds clean with ScrollTab.qml in QML_FILES.
    - `grep -q "hasSmartShift" src/logiops-gui/qml/config/ScrollTab.qml` and the section uses it for `visible` (capability-gated hide).
    - `grep -q "hasTorque" src/logiops-gui/qml/config/ScrollTab.qml` (torque slider gated).
    - `grep -q "SetTap\|setTap" src/logiops-gui/qml/config/ScrollTab.qml` (thumbwheel tap simple action).
  </acceptance_criteria>
  <done>Scroll tab renders SmartShift (with torque gated), hi-res toggle+invert, and thumbwheel divert/invert+tap, each section hidden when unsupported. On-hardware scroll behavior is in the manual checklist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: On-hardware Pointer + Scroll verification</name>
  <what-built>Pointer tab (bounded DPI slider + labeled cycle editor) and Scroll tab (capability-gated SmartShift/hi-res/thumbwheel) — DPI-01..03, SCR-01..03.</what-built>
  <how-to-verify>
    With a paired Logitech mouse and the daemon running:
    1. Pointer tab: drag the DPI slider to both extremes; confirm it clamps to the device min/max and the readout shows the snapped value; pointer speed changes live.
    2. Add three DPI cycle presets with labels (e.g. "Low/Med/High"); confirm they persist in the editor; Save and restart the daemon -> confirm the presets AND labels survive (option-a) or the linked button's cycle values survive with GUI-local labels intact (option-b); (if assigned to a button) cycling steps through them.
    3. Scroll tab: confirm only the sections the device supports are shown. Toggle SmartShift and adjust threshold/torque; toggle hi-res and invert; set thumbwheel divert/invert and a tap action. Verify each affects scrolling on the device.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which control, observed vs expected)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI (non-root) -> root logid daemon over D-Bus | DPI/SmartShift/hires/thumbwheel setters cross into the root daemon |
| Hand-edited cfg -> save() (if option-a adds a schema field) | New device-scoped preset list + labels field must round-trip without loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-03-01 | Tampering | out-of-range DPI value sent to SetDPI | mitigate | Slider bounded by device min/max; values clamp to [dpiMin,dpiMax] before emit; daemon also snaps via getClosestDPI (defense in depth) |
| T-3-03-02 | Denial of Service | per-pixel slider drag floods the daemon with SetDPI calls | mitigate | Setter fires on onMoved/onPressedChanged only, not per-pixel (RESEARCH; asserted by code shape) |
| T-3-03-03 | Tampering | option-a device-scoped preset list + labels field breaks save() round-trip (silent value loss) | mitigate | Plan 00 round-trip test EXTENDED to assert preset VALUES and LABELS survive; re-run before phase gate |
| T-3-03-04 | Tampering | calling a setter on an absent feature interface -> daemon error | mitigate | Every section gated on the introspected capability flag; absent -> section hidden, no calls issued |
</threat_model>

<verification>
- `ctest --test-dir build -R phase3 --output-on-failure` GREEN (DpiCycleTest + extended round-trip if option-a).
- `cmake --build build` clean (daemon -Werror clean if option-a touched schema/.DPI IPC).
- On-hardware checklist (Task 4) approved.
</verification>

<success_criteria>
- DPI-01: device-bounded slider, snapped live value.
- DPI-02/03: add/remove cycle presets with persisted VALUES and LABELS via the Task-0-chosen node (option-a device-scoped schema + .DPI IPC recommended; option-b linked button action + GUI-local labels with the linkage surfaced).
- SCR-01: SmartShift on/off + threshold + torque (torque gated).
- SCR-02: hi-res toggle + invert.
- SCR-03: thumbwheel divert/invert + tap simple action; all scroll sections capability-gated.
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-config-ui-persistence/03-03-SUMMARY.md`
</output>
</content>
