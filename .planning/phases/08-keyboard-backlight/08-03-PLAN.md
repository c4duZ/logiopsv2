---
phase: 08-keyboard-backlight
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - src/logiops-gui/dbus/logid_backlight.xml
  - src/logiops-gui/DeviceController.h
  - src/logiops-gui/DeviceController.cpp
  - src/logiops-gui/CMakeLists.txt
  - src/logiops-gui/qml/DetailPane.qml
  - src/logiops-gui/qml/config/ConfigTabs.qml
  - src/logiops-gui/qml/config/BacklightTab.qml
autonomous: false
requirements: [LIGHT-01]
must_haves:
  truths:
    - "On a supported device the GUI shows a Backlight tab and changing its controls changes the hardware"
    - "On a device without the LED feature the Backlight tab is omitted entirely (no error, no empty tab)"
    - "Backlight controls are gated on the hasBacklight capability discovered via Introspect"
    - "The GUI builds and loads QML clean; backlight setters dirty the global Unsaved-changes pill"
  artifacts:
    - path: "src/logiops-gui/dbus/logid_backlight.xml"
      provides: "Typed D-Bus proxy XML for pizza.pixl.LogiOps.Backlight"
      contains: "pizza.pixl.LogiOps.Backlight"
    - path: "src/logiops-gui/qml/config/BacklightTab.qml"
      provides: "Backlight/RGB control UI gated on capability"
      min_lines: 30
  key_links:
    - from: "src/logiops-gui/DeviceController.cpp"
      to: "pizza.pixl.LogiOps.Backlight present-set"
      via: "hasBacklight() checks _present.contains, proxy built lazily (mirror hasSmartShift)"
      pattern: "hasBacklight"
    - from: "src/logiops-gui/qml/DetailPane.qml tabKeys"
      to: "BacklightTab"
      via: "if (c && c.hasBacklight) keys.push(backlight)"
      pattern: "hasBacklight"
    - from: "src/logiops-gui/qml/config/BacklightTab.qml controls"
      to: "DeviceController setters"
      via: "controller.setBrightness / setEffect (optimistic + markDirty)"
      pattern: "controller\\.(setBrightness|setBacklight|setEffect)"
---

<objective>
Build the GUI half of LIGHT-01: a typed D-Bus proxy for the daemon's .Backlight interface, a
hasBacklight capability flag on DeviceController (discovered via the existing Introspect path),
optimistic live-apply setters, and a Backlight tab in the tabbed config UI — shown only on
devices that expose the feature and omitted entirely otherwise. Closes with an on-hardware UAT.

Purpose: Deliver the user-visible outcome — "change the keyboard backlight/RGB and see the
hardware respond" — while honoring the UI-SPEC omission rule (capability-gated, no empty tabs).
Output: proxy XML + DeviceController capability/setters + BacklightTab.qml + tab wiring, gated
on capability, verified live.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-keyboard-backlight/08-SPIKE.md
@.planning/phases/08-keyboard-backlight/08-02-SUMMARY.md

# The daemon interface this plan drives (the EXACT method/property names Plan 02 exported):
@src/logid/features/Backlight.cpp

# Capability-discovery + optimistic-setter pattern to MIRROR (hasSmartShift / seedSmartShift /
# setSmartShiftActive + the Introspect present-set + lazy proxy construction):
@src/logiops-gui/DeviceController.h
@src/logiops-gui/DeviceController.cpp

# Proxy XML shape to MIRROR + the qt_add_dbus_interface wiring + QML_FILES list:
@src/logiops-gui/dbus/logid_smartshift.xml
@src/logiops-gui/CMakeLists.txt

# Tab gating sites (tabKeys + tabLabel + the StackLayout component switch):
@src/logiops-gui/qml/DetailPane.qml
@src/logiops-gui/qml/config/ConfigTabs.qml
# A real tab to MIRROR for layout/binding idioms:
@src/logiops-gui/qml/config/ScrollTab.qml
@src/logiops-gui/qml/Theme.qml

<interfaces>
DeviceController capability + setter idiom (mirror these exactly):
  // capability flag (DeviceController.h 84-95):
  Q_PROPERTY(bool hasBacklight READ hasBacklight NOTIFY capabilitiesChanged)
  bool hasBacklight() const { return _present.contains(QStringLiteral("pizza.pixl.LogiOps.Backlight")); }
  // lazy proxy (DeviceController.cpp applyIntrospection 108-116):
  if (hasBacklight() && _backlightProxy == nullptr)
      _backlightProxy = new PizzaPixlLogiOpsBacklightInterface(svc, _devicePath, _bus, this);
  // optimistic setter (mirror setSmartShiftActive 361-369): update cache, emit changed,
  // async D-Bus call, markDirty().

DetailPane tabKeys (DetailPane.qml 88-96) — add backlight where it belongs:
  if (c && c.hasBacklight) keys.push("backlight");
  // tabLabel: case "backlight": return qsTr("Backlight");
ConfigTabs StackLayout (ConfigTabs.qml 60-68) — add: case "backlight": return backlightTabComponent;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backlight D-Bus proxy + DeviceController capability flag, value seed, and setters</name>
  <read_first>
    src/logid/features/Backlight.cpp (the EXACT exported method/property names + arg types —
    transcribe them into the XML; do not guess). dbus/logid_smartshift.xml (XML shape +
    type codes y/b). DeviceController.h 84-143 (capability flags + value getters), .cpp
    applyIntrospection 95-116 (lazy proxy build), readInitialValues 161-218 (async seed reads),
    setSmartShiftActive 361-369 (optimistic setter + markDirty). CMakeLists 44-47
    (qt_add_dbus_interface) + 69-82 (lib sources).
  </read_first>
  <action>
    1. Create src/logiops-gui/dbus/logid_backlight.xml describing pizza.pixl.LogiOps.Backlight
       with EXACTLY the methods/properties Plan 02 exported (GetState + the setters + capability
       props). Match arg names + type codes to features/Backlight.cpp. AVOID `--` and `<` inside
       XML comments (Phase 2 caught those silently breaking qdbusxml2cpp). Register it in
       CMakeLists.txt: qt_add_dbus_interface(GUI_DBUS_SRCS dbus/logid_backlight.xml logid_backlight_proxy)
       next to the smartshift line (44-47).
    2. In DeviceController.h: forward-declare PizzaPixlLogiOpsBacklightInterface; add
       Q_PROPERTY(bool hasBacklight ...) + bool hasBacklight() const (contains
       "pizza.pixl.LogiOps.Backlight"); add value Q_PROPERTYs for the live state the tab binds
       (e.g. backlightOn, backlightBrightness, backlightEffect) with their getters + a
       backlightChanged() signal; add Q_INVOKABLE setters (setBacklightOn/setBrightness/setEffect);
       add a seedBacklight(...) test/seed seam; add the _backlightProxy member + cache fields.
    3. In DeviceController.cpp: #include "logid_backlight_proxy.h"; in applyIntrospection build
       _backlightProxy lazily when hasBacklight() (mirror 108-116); in readInitialValues issue
       the async GetState read and call seedBacklight on reply (mirror the SmartShift GetConfig
       seed 161-218); implement each setter optimistically (update cache -> emit backlightChanged
       -> async proxy call -> markDirty()), mirroring setSmartShiftActive 361-369 + clamping
       values defensively (the daemon also clamps).
    4. Build the GUI lib + executable; confirm the generated proxy header exists and links.
  </action>
  <verify>
    <automated>cd /home/c4duz/Primor/Projetos/logiopsv2/build && cmake . >/dev/null 2>&1 ; cmake --build . --target logiops-gui -j 2>&1 | tee /tmp/p08gui.log | grep -Ei 'error' ; echo "---checks---" && test -f /home/c4duz/Primor/Projetos/logiopsv2/src/logiops-gui/dbus/logid_backlight.xml && grep -q 'pizza.pixl.LogiOps.Backlight' /home/c4duz/Primor/Projetos/logiopsv2/src/logiops-gui/dbus/logid_backlight.xml && grep -q 'hasBacklight' /home/c4duz/Primor/Projetos/logiopsv2/src/logiops-gui/DeviceController.h && grep -q 'logid_backlight.xml' /home/c4duz/Primor/Projetos/logiopsv2/src/logiops-gui/CMakeLists.txt</automated>
  </verify>
  <done>logid_backlight.xml mirrors the daemon interface exactly and is registered; DeviceController has hasBacklight (Introspect-driven), a lazily-built proxy, async value seed, and optimistic markDirty setters; the GUI target builds with no `error` lines.</done>
</task>

<task type="auto">
  <name>Task 2: BacklightTab.qml gated into the tabbed config UI (capability omission)</name>
  <read_first>
    DetailPane.qml 84-106 (tabKeys push order + tabLabel switch). ConfigTabs.qml 40-126
    (the Loader sourceComponent switch + the per-tab Component definitions). ScrollTab.qml
    (a real capability-gated tab to mirror for ColumnLayout + Theme tokens + controller
    bindings). Theme.qml (tokens/glyphs). CMakeLists 109-127 (QML_FILES list — the tab must
    be registered there) + icons block 141-143 (add a backlight glyph if the tab needs one).
    DeviceController hasBacklight + the value props/setters from Task 1.
  </read_first>
  <action>
    1. Create src/logiops-gui/qml/config/BacklightTab.qml: a ColumnLayout mirroring ScrollTab's
       structure + Theme tokens, binding the controller's backlight value props two-way to the
       controls the SPIKE/daemon support (e.g. an on/off toggle, a brightness slider bounded to
       the device's range, and — only when the daemon reports RGB support — an effect/color
       picker). Each control calls the matching controller.setX setter (optimistic; the setter
       already markDirty's). Deliver the FULL control surface for whatever the daemon exposed —
       do NOT ship a brightness-only stub if the daemon exposed RGB. Gate any RGB-only controls
       on the daemon-reported capability property (e.g. controller.backlightSupportsRGB) so a
       white-backlight device shows only brightness.
    2. Register BacklightTab.qml in CMakeLists.txt QML_FILES (after ScrollTab.qml, ~line 124).
    3. In DetailPane.qml: add `if (c && c.hasBacklight) keys.push("backlight");` to tabKeys
       (place it after "scroll", before "profiles") and add `case "backlight": return qsTr("Backlight");`
       to tabLabel.
    4. In ConfigTabs.qml: add `case "backlight": return backlightTabComponent;` to the Loader
       sourceComponent switch and define a `Component { id: backlightTabComponent; BacklightTab { ... } }`
       passing the same controller/configState bindings the other tabs receive.
    5. Build + launch the GUI headless enough to confirm QML loads with no parse/type errors
       (mirror however prior phases asserted clean QML load — e.g. qmllint on the module or a
       smoke run). On a device WITHOUT backlight, the tab must NOT appear (hasBacklight false).
  </action>
  <verify>
    <automated>cd /home/c4duz/Primor/Projetos/logiopsv2/build && cmake --build . --target logiops-gui -j 2>&1 | grep -Ei 'error' ; echo "---checks---" && test -f /home/c4duz/Primor/Projetos/logiopsv2/src/logiops-gui/qml/config/BacklightTab.qml && grep -q 'hasBacklight' /home/c4duz/Primor/Projetos/logiopsv2/src/logiops-gui/qml/DetailPane.qml && grep -q 'backlightTabComponent' /home/c4duz/Primor/Projetos/logiopsv2/src/logiops-gui/qml/config/ConfigTabs.qml && grep -q 'BacklightTab.qml' /home/c4duz/Primor/Projetos/logiopsv2/src/logiops-gui/CMakeLists.txt</automated>
  </verify>
  <done>BacklightTab.qml exists, is registered in QML_FILES, bound to DeviceController's backlight props/setters, RGB controls gated on the daemon capability; the tab is pushed into tabKeys only when hasBacklight and wired into the ConfigTabs switch; GUI builds with no `error` lines and QML loads clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-hardware UAT — backlight changes hardware, tab gated, persists</name>
  <action>Operator-driven verification of the full LIGHT-01 slice on the confirmed target device (see how-to-verify steps).</action>
  <what-built>
    The full LIGHT-01 vertical slice: daemon .Backlight interface (Plan 02) + GUI Backlight tab
    (Tasks 1-2) driving it over D-Bus on the confirmed target device from 08-SPIKE.md.
  </what-built>
  <how-to-verify>
    1. Build + install the daemon and run it: `cd build && cmake --build . -j && sudo ./logid -v`
       (or restart the systemd unit if installed). Confirm it loads with no errors.
    2. Launch the GUI: run the logiops-gui binary from build/.
    3. Select the confirmed target keyboard/device. CONFIRM a "Backlight" tab appears.
    4. In the Backlight tab: toggle the backlight on/off, drag the brightness slider, and (if the
       device is RGB) change the effect/color. CONFIRM the HARDWARE responds in real time each
       time (LIGHT-01 success criterion 1).
    5. Select a device WITHOUT the LED feature (e.g. a plain mouse). CONFIRM NO Backlight tab
       appears and the app shows no error (success criterion 2).
    6. Make a backlight change, confirm the "Unsaved changes" pill appears, click Save, complete
       the polkit prompt, and confirm /etc/logid.cfg now contains a backlight block under the
       device's profile (persistence round-trip).
  </how-to-verify>
  <resume-signal>Type "approved" if backlight changes the hardware on the supported device, the tab is hidden on unsupported devices, and the setting persists through Save; otherwise describe what failed (which step, which device, observed vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI -> daemon (D-Bus) | The GUI sends backlight values to the polkit/group-gated daemon; the daemon (Plan 02) validates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering | GUI backlight setters | mitigate | GUI clamps values before the async call (defense in depth); the daemon IPC (T-08-02) is the authoritative clamp/validate. |
| T-08-05 | DoS | qdbusxml2cpp proxy generation | mitigate | Keep `--`/`<` out of XML comments (Phase 2 lesson: silently produces an empty proxy); verify the generated proxy header is non-empty by linking against it. |
</threat_model>

<verification>
- GUI builds clean; QML loads with no parse/type errors.
- Backlight tab present only when hasBacklight (Introspect-driven); omitted otherwise.
- On-hardware UAT confirms live hardware response on a supported device, no tab on unsupported.
- Backlight change dirties the pill and persists through the polkit-gated Save.
</verification>

<success_criteria>
LIGHT-01 met end-to-end: on the confirmed target device the user changes backlight/RGB in the
GUI and the hardware responds; on devices without the LED feature the controls are hidden with
no error; the setting persists via Save.
</success_criteria>

<output>
After completion, create `.planning/phases/08-keyboard-backlight/08-03-SUMMARY.md`.
</output>
