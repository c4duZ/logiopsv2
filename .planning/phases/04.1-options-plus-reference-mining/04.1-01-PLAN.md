---
phase: 04.1-options-plus-reference-mining
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/intel/options-plus/vocabulary.md
  - .planning/intel/options-plus/legal-boundary.md
  - .planning/intel/options-plus/device-db-limitation.md
autonomous: true
requirements: [REF-01, REF-03]

must_haves:
  truths:
    - "A vocabulary spec we own captures the gesture/scroll/smart-action terms (pt-BR + en) distilled from Options+ strings"
    - "The hybrid/reference-only legal boundary is written down as a Phase 9 packaging gate"
    - "The encrypted device DB is documented as unusable with the live-HID++-enumeration fallback recorded"
  artifacts:
    - path: ".planning/intel/options-plus/vocabulary.md"
      provides: "Owned gesture/scroll/smart-action string table, pt-BR + en, with our intended re-implementation labels"
      contains: "SEGURAR + MOVER"
    - path: ".planning/intel/options-plus/legal-boundary.md"
      provides: "Hybrid/reference-only rule + list of forbidden-to-bundle proprietary assets + Phase 9 gate"
      contains: "reference-only"
    - path: ".planning/intel/options-plus/device-db-limitation.md"
      provides: "Why devices_*.json is unusable + live-enumeration fallback"
      contains: "live HID++ enumeration"
  key_links:
    - from: ".planning/intel/options-plus/vocabulary.md"
      to: "LogiOptionsPlus/data/strings/pt-BR.yaml"
      via: "cited source keys (e.g. GESTURE_ACTION_HOLD_MOVE_UP)"
      pattern: "GESTURE_ACTION_HOLD_MOVE"
---

<objective>
Produce three owned reference specs from the local Logi Options+ install: the gesture/scroll/smart-action **vocabulary** (pt-BR + en), the **legal boundary** (hybrid/reference-only), and the **device-DB limitation** (encrypted, unusable; live-enumeration fallback).

Purpose: Phase 4.2 (gesture UX alignment) and Phases 5–8 need the exact Options+ wording to re-implement faithfully in our own QML/strings, and every forward phase + Phase 9 packaging needs the legal boundary on record. The device-DB note prevents anyone from planning work that depends on the encrypted descriptors.

Output: `.planning/intel/options-plus/vocabulary.md`, `legal-boundary.md`, `device-db-limitation.md` — our own distilled specs, NOT copies of Logitech assets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-options-plus-reference-mining/CONTEXT.md

<source_material>
Local Logi Options+ Windows install (gitignored, 531MB) at repo root: `LogiOptionsPlus/`
- Strings: `LogiOptionsPlus/data/strings/pt-BR.yaml`, `LogiOptionsPlus/data/strings/en-US.yaml` (5421 lines each, flat `KEY: value` YAML)
- Encrypted device DB: `LogiOptionsPlus/data/devices/devices_*.json` (46 files; header `{"key-id":..., "file-sha":...}` + binary blob; keys `LogiOptionsPlus/data/firmware.pem`, `LogiOptionsPlus/data/logitech-lap-public.pem`)

Confirmed gesture string keys present in pt-BR.yaml (use as grep anchors / citations):
- `GESTURE_ACTION_HOLD_MOVE_LEFT: "SEGURAR + MOVER PARA ESQUERDA"`
- `GESTURE_ACTION_HOLD_MOVE_RIGHT: "SEGURAR + MOVER PARA DIREITA"`
- `GESTURE_ACTION_HOLD_MOVE_UP: "SEGURAR + MOVER PARA CIMA"`
- `GESTURE_ACTION_HOLD_MOVE_DOWN: "SEGURAR + MOVER PARA BAIXO"`
- `ASSIGNMENT_NAME_GESTURE: Gestos`
- `ASSIGNMENT_NAME_CUSTOM_GESTURE: Personalizada`
- `GESTURE_CARD_DESCRIPTION: Escolha uma predefinição ou selecione uma personalizada...`
- `SLOT_NAME_GESTURE_BUTTON: Botão de gesto`
- `GESTURE_INFO_HOLD_MOVE_DESCRIPTION: Segure o botão e mova o mouse`
- `GESTURE_INFO_CLICK_DESCRIPTION: Clique no botão`
</source_material>

<legal_constraint>
LOCKED DECISION (CONTEXT.md): HYBRID / reference-only. We adapt behavior + vocabulary and re-implement in OUR OWN QML/strings. We do NOT bundle Logitech proprietary assets (app.asar content, verbatim Options+ string resources, icons, device data) into the shipped app (`src/`). Extraction artifacts live ONLY under `.planning/intel/options-plus/`. NO task in this plan may write into `src/`.
</legal_constraint>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Distill the gesture/scroll/smart-action vocabulary spec</name>
  <files>.planning/intel/options-plus/vocabulary.md</files>
  <read_first>
    LogiOptionsPlus/data/strings/pt-BR.yaml and LogiOptionsPlus/data/strings/en-US.yaml — grep the gesture, scroll, smartshift, hi-res, and smart-action term families. Suggested greps:
    `grep -iE "GESTURE|SEGURAR|predefin|personalizada|SMARTSHIFT|HIRES|SCROLL|THUMBWHEEL|ASSIGNMENT_NAME" LogiOptionsPlus/data/strings/pt-BR.yaml`
    Cross-reference the same keys in en-US.yaml for the English column.
  </read_first>
  <action>
    Create `.planning/intel/options-plus/vocabulary.md` as OUR OWN distilled string table (per REF-01). Structure:
    1. A "Gestures" section: a markdown table with columns `Concept | Options+ key | pt-BR | en | Our intended label`. Include at minimum the HOLD+MOVE directions (up/down/left/right), the gesture/custom-gesture/predefinição distinction (`ASSIGNMENT_NAME_GESTURE`, `ASSIGNMENT_NAME_CUSTOM_GESTURE`, `GESTURE_CARD_DESCRIPTION`), the gesture-button slot names (`SLOT_NAME_GESTURE_*`), and the HOLD+MOVE / CLICK info descriptions. The "Our intended label" column is what WE will ship (may match or paraphrase — this is the re-implementation target Phase 4.2 consumes).
    2. A "Scroll / SmartShift / Hi-Res / Thumbwheel" section: same table shape for the SmartShift, hi-res, and thumbwheel terms found in the yaml.
    3. A "Smart Actions (preview)" section: the top-level smart-action/macro UI terms (a stub — the full schema is Plan 02; here only the user-facing labels).
    4. A short "Predefinição vs Personalizada model" prose note explaining the Options+ mental model (pick a preset OR build a custom one) that Phase 4.2 must mirror.
    Cite the exact Options+ key for every row. Do NOT paste the full 5421-line yaml; distill only the relevant families. Header note at top: "Reference-only distillation — see legal-boundary.md. Source: LogiOptionsPlus/data/strings/*.yaml (not bundled)."
  </action>
  <verify>
    <automated>test -f .planning/intel/options-plus/vocabulary.md && grep -q "SEGURAR + MOVER" .planning/intel/options-plus/vocabulary.md && grep -q "GESTURE_ACTION_HOLD_MOVE_UP" .planning/intel/options-plus/vocabulary.md && grep -qi "predefini" .planning/intel/options-plus/vocabulary.md && grep -qi "thumbwheel\|smartshift\|hi-res\|hires" .planning/intel/options-plus/vocabulary.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/intel/options-plus/vocabulary.md` exists
    - File contains `SEGURAR + MOVER` (pt-BR HOLD+MOVE term)
    - File contains the source key `GESTURE_ACTION_HOLD_MOVE_UP` (citation present)
    - File contains a predefinição/Personalizada model note (grep `predefini`)
    - File contains scroll-family terms (grep matches one of thumbwheel/smartshift/hi-res/hires)
    - File has an "Our intended label" column header for the gesture table
    - No content written under `src/`
  </acceptance_criteria>
  <done>vocabulary.md exists with a gesture table (Options+ key + pt-BR + en + our-label columns), a scroll/smartshift section, a smart-action preview section, and a predefinição/personalizada model note — all citing source keys, nothing copied into src/.</done>
</task>

<task type="auto">
  <name>Task 2: Write the legal boundary and device-DB limitation specs</name>
  <files>.planning/intel/options-plus/legal-boundary.md, .planning/intel/options-plus/device-db-limitation.md</files>
  <read_first>
    CONTEXT.md "Decisions (2026-05-31)" and "Asset inventory ❌ Not usable" sections; ROADMAP.md Phase 9 "Legal gate (from 4.1)" line. Confirm the encrypted header shape: `head -c 200 LogiOptionsPlus/data/devices/devices_0000001.json` (shows `{"key-id":..., "file-sha":...}` + binary).
  </read_first>
  <action>
    Create TWO files:

    (a) `.planning/intel/options-plus/legal-boundary.md` (REF-03): document the HYBRID / reference-only rule.
    - State the rule: we adapt behavior + vocabulary and study the UI design, but re-implement everything in our own QML/strings and ship ONLY our own assets.
    - A "Forbidden to bundle" table listing the proprietary asset categories that MUST NOT reach `src/` or the `.deb`: app.asar content (HTML/CSS/JS), verbatim Options+ string resources, Logitech icons/art (`overlay/icons/`, `resources/icon_kiros.png`), device descriptor data, `resources.pak`.
    - An "Allowed" list: our own distilled specs under `.planning/intel/options-plus/`, our own re-implemented QML/strings, behavior/vocabulary adaptation.
    - A "Phase 9 packaging gate" subsection: the `.deb` build must verify no Logitech proprietary asset is bundled (cross-link to ROADMAP Phase 9 PKG gate). Make this an explicit release blocker.

    (b) `.planning/intel/options-plus/device-db-limitation.md` (REF-03): document why the per-device DB is unusable.
    - Describe `LogiOptionsPlus/data/devices/devices_*.json` (46 files): encrypted/signed (header `{"key-id":..., "file-sha":...}` + binary blob; keys `firmware.pem`, `logitech-lap-public.pem`). State we do NOT attempt decryption (legal + futile).
    - Record the fallback: device capability comes from LIVE HID++ enumeration (the daemon already does this — feature probing via `_addFeature`) plus public DBs (Solaar, libratbag) where a static descriptor is genuinely needed.
    - Add a one-line forward note: no Phase (4.2, 5–8) may plan work that depends on reading these files.
  </action>
  <verify>
    <automated>test -f .planning/intel/options-plus/legal-boundary.md && test -f .planning/intel/options-plus/device-db-limitation.md && grep -qi "reference-only" .planning/intel/options-plus/legal-boundary.md && grep -qi "app.asar" .planning/intel/options-plus/legal-boundary.md && grep -qi "Phase 9" .planning/intel/options-plus/legal-boundary.md && grep -qi "live HID++ enumeration" .planning/intel/options-plus/device-db-limitation.md && grep -qi "key-id\|encrypted" .planning/intel/options-plus/device-db-limitation.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/intel/options-plus/legal-boundary.md` exists
    - legal-boundary.md contains `reference-only`, references `app.asar`, and names the `Phase 9` packaging gate
    - legal-boundary.md has a "Forbidden to bundle" list and an "Allowed" list
    - `.planning/intel/options-plus/device-db-limitation.md` exists
    - device-db-limitation.md contains `live HID++ enumeration` and notes the encrypted `key-id` header
    - device-db-limitation.md states decryption is NOT attempted
    - No content written under `src/`
  </acceptance_criteria>
  <done>Both legal-boundary.md and device-db-limitation.md exist: the legal spec states the hybrid/reference-only rule with forbidden/allowed lists and the Phase 9 release-blocker gate; the device-DB spec documents the encryption, the no-decryption stance, and the live-HID++-enumeration fallback.</done>
</task>

</tasks>

<verification>
- All three files exist under `.planning/intel/options-plus/`
- `grep -rl "" src/ | xargs grep -l "GESTURE_ACTION_HOLD_MOVE\|key-id" 2>/dev/null` returns nothing (no Logitech assets leaked into src/)
- vocabulary.md cites Options+ keys; legal-boundary.md names the Phase 9 gate; device-db-limitation.md records the live-enumeration fallback
</verification>

<success_criteria>
- REF-01 partially satisfied (vocabulary.md owned spec produced from strings/*.yaml, pt-BR + en)
- REF-03 satisfied (legal boundary + encrypted device-DB limitation both documented)
- No proprietary asset copied into `src/`
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-options-plus-reference-mining/04.1-01-SUMMARY.md`
</output>
