---
phase: 04.1-options-plus-reference-mining
plan: 04
subsystem: ui
tags: [design-spec, options-plus, qml, overlay, osd, action-wheel, gestures, reference-only]

# Dependency graph
requires:
  - phase: 04.1-03
    provides: "Extracted+mapped app.asar (asar-inventory.md) — the navigation map into the minified renderer"
  - phase: 04.1-01
    provides: "legal-boundary.md (hybrid/reference-only rule) + vocabulary.md (owned string distillation)"
provides:
  - "ui-design-spec.md — owned tab/layout/interaction re-implementation target with alignment deltas vs our current QML"
  - "overlay-osd-spec.md — owned radial action-wheel + OSD model (WHEEL-01/02/03) with X11/Wayland degradation"
affects: [04.2-gesture-live-apply, 05-per-app-profiles, 06-action-wheel, 07-smart-actions, 08-backlight, 09-packaging-legal-gate]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Design specs framed as alignment deltas against shipped QML (concrete consumer value, not abstract description)"
    - "Schema-vs-content boundary: adapt structure of Logitech overlay JSON, ship our own re-authored content/art"

key-files:
  created:
    - .planning/intel/options-plus/ui-design-spec.md
    - .planning/intel/options-plus/overlay-osd-spec.md
  modified: []

key-decisions:
  - "Keep our Pointer/Scroll split (vs Options+ combined Point&Scroll tab) and our non-modal ReassignPanel (vs Options+ modal) as intentional, documented divergences"
  - "Gesture builder must add the preset-or-custom (predefinicao/personalizada) card before the per-direction editor — deferred to Phase 4.2 (04.2-03)"
  - "Action wheel: daemon computes active slice from raw XY (flick-angle) + fires on release + emits ActiveSlice; GUI renders a transparent click-through follow-cursor RadialOverlay only"
  - "Mandate a centered-pop-up fallback for the radial/OSD where a compositor (e.g. GNOME-Mutter) cannot render a follow-cursor click-through overlay — the slice mechanic still works on relative deltas"

patterns-established:
  - "Overlay backend shared between the action wheel (Phase 6) and the gesture OSD (Phase 4.2)"

requirements-completed: [REF-01, REF-02]

# Metrics
duration: 3min
completed: 2026-06-01
---

# Phase 04.1 Plan 04: Options+ UI & Overlay/OSD Design Specs Summary

**Two owned design specs — a tab/layout/interaction re-implementation target framed as alignment deltas vs our shipped QML, and a radial action-wheel + OSD model (WHEEL-01/02/03, X11/Wayland degradation) — mined from the extracted app.asar + readable overlay JSON, with zero Logitech assets entering `src/`.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-06-01T03:46:52Z
- **Completed:** 2026-06-01T03:50:09Z
- **Tasks:** 2
- **Files modified:** 2 (both created)

## Accomplishments
- `ui-design-spec.md` (REF-02): app shell, capability-driven tab model, the marquee gesture interaction flow, an **alignment-deltas table** mapping each Options+ behavior to our current QML and the phase that closes the gap (4.2/5–8), and a visual-language note in our own words.
- `overlay-osd-spec.md` (REF-01/02): the OSD resource model distilled from `osd_resources.json` + `notification_resources.json`, the radial action-wheel mechanic (flick-toward-slice → release-to-fire) mapped to WHEEL-01/02/03 with the daemon-detects/GUI-renders split, the gesture OSD, and the graceful-degradation (centered pop-up) rule for compositors that can't follow the cursor.
- Confirmed grep anchors in the extracted (gitignored) bundles — `GestureConfiguration`/`closeGestureConfiguration`/`buttonsTab`/`DeviceTab` in `app.min.js` — used to verify which screen is which, per the inventory's warning that wheel/osd tokens in the minified chunks are mangled noise.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the UI design spec (tab/layout/interaction model)** - `b786170` (docs)
2. **Task 2: Write the overlay / OSD spec (radial + OSD interaction and layout)** - `7725340` (docs)

**Plan metadata:** (final docs commit — STATE/ROADMAP/REQUIREMENTS + this SUMMARY)

## Files Created/Modified
- `.planning/intel/options-plus/ui-design-spec.md` - Owned tab/layout/interaction re-implementation target; alignment-deltas table vs shipped QML; cites the extracted app.asar tree (study-only).
- `.planning/intel/options-plus/overlay-osd-spec.md` - Owned radial-wheel + OSD model; distills the overlay JSON; maps to WHEEL-01/02/03; X11/Wayland degradation + asset boundary.

## Decisions Made
- Pointer/Scroll split and non-modal ReassignPanel are intentional divergences from Options+, documented rather than "fixed".
- The preset-or-custom gesture card is a real gap → assigned to Phase 4.2 (`04.2-03`), consistent with the existing 4.2 plan set and `gesture-live-apply-and-save.md`.
- Action-wheel responsibility split (daemon detection/execution + `ActiveSlice`; GUI render-only) restated as the spec's binding contract, matching ROADMAP `06-02`/`06-04`.
- Centered-pop-up fallback mandated as the degradation floor (covers GNOME-Mutter / no-cursor-position cases).

## Deviations from Plan

None - plan executed exactly as written. Both tasks' automated verifications passed on first run; no auto-fixes (Rules 1–3) and no architectural escalations (Rule 4) were needed. `src/` remained clean throughout (no Logitech asset written), satisfying the legal constraint.

## Issues Encountered
None. The minified bundles are unreadable for layout (as the inventory warned), so both specs were written from behavior + the readable `data/overlay/` JSON + our own shipped QML — exactly the study method the plan prescribed.

## Known Stubs
None. Both deliverables are complete owned specs; they intentionally **defer** (not stub) downstream work to the phases that consume them (4.2 gesture preset card, 5 per-app, 6 wheel, 7 smart actions, 8 backlight), each explicitly cited.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Phase 4.2 (gesture live-apply + UX alignment) has its UX target: the preset-or-custom card + HOLD+MOVE vocabulary deltas are written down with the specific plan IDs that close them.
- Phase 6 (action wheel) has its interaction + degradation spec and the OSD resource model to re-create in our own QML.
- Phase 9 legal gate: both specs are planning artifacts (cite, don't copy); no proprietary asset reached `src/`, keeping the BLOCKING legal-asset audit satisfiable.

## Self-Check: PASSED

- FOUND: `.planning/intel/options-plus/ui-design-spec.md`
- FOUND: `.planning/intel/options-plus/overlay-osd-spec.md`
- FOUND: `.planning/phases/04.1-options-plus-reference-mining/04.1-04-SUMMARY.md`
- FOUND commit: `b786170` (Task 1)
- FOUND commit: `7725340` (Task 2)

---
*Phase: 04.1-options-plus-reference-mining*
*Completed: 2026-06-01*
