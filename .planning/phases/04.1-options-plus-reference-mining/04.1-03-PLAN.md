---
phase: 04.1-options-plus-reference-mining
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04.1-options-plus-reference-mining/asar-extract/.extracted
  - .planning/intel/options-plus/asar-inventory.md
autonomous: true
requirements: [REF-02]

must_haves:
  truths:
    - "The Electron app.asar is extracted to a scratch dir OUTSIDE src/ for design study"
    - "An inventory map records where the UI source (tabs/layout/interaction views) lives inside the extracted archive so the Plan-04 spec author can navigate it"
  artifacts:
    - path: ".planning/intel/options-plus/asar-inventory.md"
      provides: "Map of the extracted app.asar: top-level dirs, where the renderer/UI/QML-or-HTML views live, gesture/scroll/overlay screens, with a 'study-only, do not bundle' header"
      contains: "study-only"
    - path: ".planning/phases/04.1-options-plus-reference-mining/asar-extract/.extracted"
      provides: "Marker proving extraction completed into the scratch dir (NOT src/)"
  key_links:
    - from: ".planning/intel/options-plus/asar-inventory.md"
      to: ".planning/phases/04.1-options-plus-reference-mining/asar-extract/"
      via: "documented paths into the extracted tree"
      pattern: "asar-extract"
---

<objective>
Extract `LogiOptionsPlus/resources/app.asar` (83MB Electron archive) into a scratch directory for DESIGN STUDY, and produce an inventory map of where the UI source lives so the Plan-04 author can write `ui-design-spec.md` and `overlay-osd-spec.md` without re-exploring 83MB.

Purpose: Plan 04 (UI design spec + overlay/OSD spec) depends on the extracted, navigable archive. This plan is the extraction + navigation map only — it does NOT write the design spec (that's Plan 04, Wave 2).

Output: extracted tree under `.planning/phases/04.1-options-plus-reference-mining/asar-extract/` (scratch, NOT src/) + `.planning/intel/options-plus/asar-inventory.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04.1-options-plus-reference-mining/CONTEXT.md

<source_material>
- `LogiOptionsPlus/resources/app.asar` (82,945,069 bytes — Electron asar archive)
- Tooling confirmed available: `npx` (npm 11.11.0) and `node v24.14.1` at `/home/c4duz/.nvm/...`. Extract with `npx @electron/asar extract <archive> <dest>` (or `npx asar extract`).
</source_material>

<legal_constraint>
LOCKED DECISION: HYBRID / reference-only — app.asar is extracted for DESIGN STUDY ONLY. The extracted tree MUST live in a scratch dir under the phase dir (`.planning/phases/04.1-options-plus-reference-mining/asar-extract/`), NEVER under `src/`. We will write our OWN spec (Plan 04) describing the tab/layout/interaction model — we do NOT bundle the extracted HTML/CSS/JS/icons into the shipped app. The extracted scratch tree is gitignored study material, not a deliverable to ship. NO task writes extracted Logitech content into `src/`.
</legal_constraint>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract app.asar to the scratch study directory</name>
  <files>.planning/phases/04.1-options-plus-reference-mining/asar-extract/.extracted</files>
  <read_first>
    Confirm the archive exists and the tool resolves: `ls -la LogiOptionsPlus/resources/app.asar` and `npx --yes @electron/asar --version` (or fall back to `npx --yes asar --version`). Confirm `.planning/phases/04.1-options-plus-reference-mining/` exists.
  </read_first>
  <action>
    1. Create the scratch dir: `mkdir -p .planning/phases/04.1-options-plus-reference-mining/asar-extract`.
    2. Extract the archive into it: `npx --yes @electron/asar extract LogiOptionsPlus/resources/app.asar .planning/phases/04.1-options-plus-reference-mining/asar-extract` (if `@electron/asar` fails, fall back to `npx --yes asar extract <archive> <dest>`).
    3. Verify extraction produced a non-empty tree (e.g. a `package.json` and/or a renderer/build/dist dir). If the archive contains a nested `app.asar.unpacked` reference or a build dir, that's expected.
    4. Ensure the extract path is gitignored so the 83MB of proprietary content is never committed: append `/.planning/phases/04.1-options-plus-reference-mining/asar-extract/` to the repo `.gitignore` if not already covered. (Confirm with `git check-ignore .planning/phases/04.1-options-plus-reference-mining/asar-extract/x`.)
    5. Write a marker file `.planning/phases/04.1-options-plus-reference-mining/asar-extract/.extracted` containing the extraction date and the tool/version used.
    DO NOT copy any extracted file into `src/`.
  </action>
  <verify>
    <automated>test -f .planning/phases/04.1-options-plus-reference-mining/asar-extract/.extracted && test "$(find .planning/phases/04.1-options-plus-reference-mining/asar-extract -type f | head -5 | wc -l)" -ge 2 && git check-ignore .planning/phases/04.1-options-plus-reference-mining/asar-extract/probe >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/04.1-options-plus-reference-mining/asar-extract/` exists and contains extracted files (find shows >= 2 files)
    - Marker `.../asar-extract/.extracted` exists with date + tool version
    - The asar-extract path is git-ignored (`git check-ignore` succeeds — proprietary content not committed)
    - No extracted file copied into `src/`
  </acceptance_criteria>
  <done>app.asar is extracted into the gitignored scratch dir with an `.extracted` marker; nothing landed in src/ and nothing extracted is staged for commit.</done>
</task>

<task type="auto">
  <name>Task 2: Map the extracted UI source into an inventory</name>
  <files>.planning/intel/options-plus/asar-inventory.md</files>
  <read_first>
    Explore the extracted tree to locate the renderer/UI source. Suggested: `ls .planning/phases/04.1-options-plus-reference-mining/asar-extract/`, then `find .../asar-extract -maxdepth 3 -type d | head -50`, and grep for screen/view names tied to our phases: `grep -rilE "gesture|smartshift|thumbwheel|overlay|osd|radial|wheel|profile" .../asar-extract --include=*.js --include=*.html --include=*.json -l | head -40` (renderer is often minified/bundled — record bundle file paths + any readable view/route/template names you can find).
  </read_first>
  <action>
    Create `.planning/intel/options-plus/asar-inventory.md` — a navigation map (NOT a design spec; that's Plan 04). Structure:
    1. Header: "Study-only inventory of extracted app.asar — see legal-boundary.md. Source not bundled."
    2. "Top-level layout" — the top-level dirs/files of the extracted tree with a one-line purpose each.
    3. "Where the UI lives" — the path(s) to the renderer/UI source (build/dist/renderer bundles, HTML entry points, any readable component/route names). Note if the renderer is minified-bundled and where the largest JS bundles are.
    4. "Screens relevant to our phases" — a table mapping our concerns to located paths/anchors: gesture builder, button-assign/cards, scroll/SmartShift, overlay/OSD/radial, profiles. For each, record the file path(s) and any readable string/route/class names found (or "minified — study via running app / strings").
    5. "How to study" — a one-line note for the Plan-04 author on how to inspect (e.g. open HTML in a browser, grep bundles for known string keys from vocabulary.md like `GESTURE_ACTION_HOLD_MOVE_UP`).
    Reference paths INTO `.planning/phases/04.1-options-plus-reference-mining/asar-extract/`. Do NOT copy file contents into src/.
  </action>
  <verify>
    <automated>test -f .planning/intel/options-plus/asar-inventory.md && grep -qi "study-only" .planning/intel/options-plus/asar-inventory.md && grep -q "asar-extract" .planning/intel/options-plus/asar-inventory.md && grep -qi "gesture\|overlay\|profile" .planning/intel/options-plus/asar-inventory.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/intel/options-plus/asar-inventory.md` exists
    - File has a "study-only" header (grep `study-only`)
    - File documents paths into the `asar-extract` tree (grep `asar-extract`)
    - File has a "screens relevant to our phases" section covering gesture/overlay/profile (grep matches)
    - File tells the Plan-04 author how to study the UI source
    - No extracted content copied into `src/`
  </acceptance_criteria>
  <done>asar-inventory.md exists mapping the extracted tree's top-level layout, where the UI source lives, the per-concern screen paths/anchors, and a how-to-study note — pointing into the scratch dir, nothing copied into src/.</done>
</task>

</tasks>

<verification>
- Extracted tree exists under the phase scratch dir and is gitignored (not committed)
- asar-inventory.md maps the UI source for Plan 04
- No extracted Logitech content under `src/`
</verification>

<success_criteria>
- REF-02 partially satisfied (app.asar extracted for study + navigable inventory produced; the written design spec is Plan 04)
- The 83MB proprietary archive content is NOT committed and NOT in src/
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-options-plus-reference-mining/04.1-03-SUMMARY.md`
</output>
