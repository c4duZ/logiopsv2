---
phase: 04.1-options-plus-reference-mining
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified:
  - .planning/intel/options-plus/ui-design-spec.md
  - .planning/intel/options-plus/overlay-osd-spec.md
autonomous: true
requirements: [REF-01, REF-02]

must_haves:
  truths:
    - "A UI design spec we own captures the Options+ tab/layout/interaction model we will re-implement in our own QML"
    - "An overlay/OSD spec we own captures the radial/OSD interaction + layout for the action wheel and gesture OSD"
  artifacts:
    - path: ".planning/intel/options-plus/ui-design-spec.md"
      provides: "Owned tab/layout/interaction reference for our QML re-implementation (study of extracted app.asar, not bundled assets)"
      contains: "tab"
    - path: ".planning/intel/options-plus/overlay-osd-spec.md"
      provides: "Owned radial/OSD interaction + layout spec from overlay JSON + app.asar study"
      contains: "OSD"
  key_links:
    - from: ".planning/intel/options-plus/ui-design-spec.md"
      to: ".planning/phases/04.1-options-plus-reference-mining/asar-extract/"
      via: "study of the extracted renderer source mapped by asar-inventory.md"
      pattern: "asar"
    - from: ".planning/intel/options-plus/overlay-osd-spec.md"
      to: "LogiOptionsPlus/data/overlay/osd_resources.json"
      via: "cited OSD resource structure"
      pattern: "osd_resources\\|notification_resources"
---

<objective>
Produce two owned design specs: the **UI design spec** (Options+ tab/layout/interaction model we re-implement in QML) from the extracted app.asar study, and the **overlay/OSD spec** (radial/OSD interaction + layout for the Phase 6 action wheel and gesture OSD) from the overlay JSON + app.asar study.

Purpose: These are the re-implementation targets for every GUI phase. Phase 4.2 + 5–8 build OUR QML against this spec; Phase 6 (action wheel) needs the radial/OSD feel captured. This runs in Wave 2 because it depends on Plan 03's extracted-and-mapped app.asar.

Output: `.planning/intel/options-plus/ui-design-spec.md`, `overlay-osd-spec.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-options-plus-reference-mining/CONTEXT.md

<dependencies>
This plan depends on Plan 03 (Wave 1). Before starting, read:
- `.planning/intel/options-plus/asar-inventory.md` — the navigation map into the extracted app.asar (where the UI source / screens live).
- The extracted tree at `.planning/phases/04.1-options-plus-reference-mining/asar-extract/` — study the renderer/UI source per the inventory map.
Also read the Wave-1 vocabulary output if present: `.planning/intel/options-plus/vocabulary.md` (for consistent terminology) and `legal-boundary.md`.
</dependencies>

<source_material>
- Extracted app.asar tree: `.planning/phases/04.1-options-plus-reference-mining/asar-extract/` (mapped by asar-inventory.md) — STUDY ONLY.
- `LogiOptionsPlus/data/overlay/osd_resources.json` — top-level `lock_notifications[]` etc.; each entry `id`, optional `platform`, `on`/`off` objects with `title`, `subtitle`, `icon`.
- `LogiOptionsPlus/data/overlay/notification_resources.json` — notification resource model.
- `LogiOptionsPlus/data/overlay/icons/` — Logitech overlay icons (study only, NOT bundled).
- Our existing UI: the Phase 2/3/4 QML lives under `src/` (e.g. tabbed DetailPane, Theme tokens, GestureBuilder.qml) — the spec should target alignment with what WE already built.

Confirmed grep anchors: osd_resources.json contains `lock_notifications`, `title`, `subtitle`, `icon`, `platform`.
</source_material>

<legal_constraint>
LOCKED DECISION: HYBRID / reference-only. These specs describe the tab/layout/interaction + overlay model in OUR OWN words for re-implementation in OUR OWN QML/strings. We do NOT bundle the extracted HTML/CSS/JS, the overlay icons, or `osd_resources.json` into `src/` or the `.deb`. NO task writes Logitech assets into `src/`. (Writing OUR OWN spec content that merely cites Options+ structure is allowed; copying their files/art is not.)
</legal_constraint>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the UI design spec (tab/layout/interaction model)</name>
  <files>.planning/intel/options-plus/ui-design-spec.md</files>
  <read_first>
    `.planning/intel/options-plus/asar-inventory.md` (the Plan-03 map), then study the extracted UI source it points to under `.planning/phases/04.1-options-plus-reference-mining/asar-extract/` — focus on the screens the inventory tagged for gesture/buttons/scroll/profiles. Cross-reference our existing QML under `src/` (DetailPane/tabs/Theme) so the spec is framed as alignment deltas. Grep the bundles for vocabulary anchors (e.g. `GESTURE_ACTION_HOLD_MOVE_UP`, `ASSIGNMENT_NAME_GESTURE`) to confirm which screen is which.
  </read_first>
  <action>
    Create `.planning/intel/options-plus/ui-design-spec.md` (REF-02) — OUR re-implementation target. Structure:
    1. Header: "Reference-only design study — see legal-boundary.md. Our own re-implementation target; no Logitech assets bundled."
    2. "App shell / navigation" — the top-level layout (device sidebar/list, detail pane, tab strip) and how Options+ organizes it.
    3. "Tab model" — the per-device tabs (e.g. Buttons / Pointer & Scroll / Gestures / etc. as the extracted UI reveals), the order, and what each contains.
    4. "Interaction model" — key interactions: clicking a button on the device render → assignment panel/cards; the gesture builder flow (direction → HOLD+MOVE / CLICK → action, predefinição vs personalizada per vocabulary.md); the card/assignment picker.
    5. "Alignment deltas vs our current QML" — a table: for each screen, "Options+ does X" vs "we currently do Y" vs "action for Phase 4.2/5–8". This is the concrete consumer value.
    6. "Visual language note" — spacing/accent/animation feel in our OWN words (no pixel-copying of assets; describe the feel so we re-create it).
    Cite paths into the extracted tree per the inventory. Do NOT embed extracted HTML/CSS/JS verbatim and do NOT write into src/.
  </action>
  <verify>
    <automated>test -f .planning/intel/options-plus/ui-design-spec.md && grep -qi "tab" .planning/intel/options-plus/ui-design-spec.md && grep -qi "gesture" .planning/intel/options-plus/ui-design-spec.md && grep -qi "reference-only\|study" .planning/intel/options-plus/ui-design-spec.md && grep -qi "asar\|extract" .planning/intel/options-plus/ui-design-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/intel/options-plus/ui-design-spec.md` exists
    - File has a reference-only/study header
    - File documents the tab model (grep `tab`) and the gesture interaction flow (grep `gesture`)
    - File cites the extracted app.asar tree (grep `asar` or `extract`)
    - File includes an "alignment deltas vs our current QML" section
    - No extracted content / Logitech asset written into `src/`
  </acceptance_criteria>
  <done>ui-design-spec.md exists describing the app shell, tab model, interaction model, alignment deltas vs our current QML, and visual-language feel in our own words — citing the extracted tree, nothing copied into src/.</done>
</task>

<task type="auto">
  <name>Task 2: Write the overlay / OSD spec (radial + OSD interaction and layout)</name>
  <files>.planning/intel/options-plus/overlay-osd-spec.md</files>
  <read_first>
    `LogiOptionsPlus/data/overlay/osd_resources.json` (read head + structure) and `notification_resources.json`; list `LogiOptionsPlus/data/overlay/icons/` to know what icon families exist (do NOT copy them). Then study any overlay/OSD/radial screens located via `asar-inventory.md` in the extracted tree. Re-read ROADMAP Phase 6 "Adapt from 4.1" note (slice-selection mechanic, radial layout, OSD feel).
  </read_first>
  <action>
    Create `.planning/intel/options-plus/overlay-osd-spec.md` (REF-01/REF-02) — OUR radial/OSD interaction + layout reference for Phase 6 (action wheel) and the gesture OSD. Structure:
    1. Header: "Reference-only design study — see legal-boundary.md. No Logitech overlay art bundled."
    2. "OSD resource model" — distill `osd_resources.json` structure: entry shape (`id`, `platform`, `on`/`off` with `title`/`subtitle`/`icon`), what these OSDs are for (lock-state/feedback notifications), and the `notification_resources.json` model. Cite the source.
    3. "Radial / action-wheel interaction" — from the app.asar study: how the wheel is triggered, the flick-toward-slice → release-to-fire mechanic, slice count/layout, and the follow-the-cursor placement. Frame as our WHEEL-01/02/03 re-implementation target (daemon detects/highlights from HID++ deltas, GUI renders only).
    4. "Gesture OSD" — the on-screen feedback shown during a gesture (ties to the gesture vocabulary and Phase 4.2).
    5. "Graceful degradation" — note where a compositor can't render a follow-pointer overlay (centered pop-up fallback) — the X11/Wayland reality Phase 6 must handle.
    6. "Asset boundary" — explicit: we re-create overlay visuals in our own QML; we do NOT bundle `overlay/icons/` or `osd_resources.json`.
    Cite source files. Do NOT copy icons or JSON into src/.
  </action>
  <verify>
    <automated>test -f .planning/intel/options-plus/overlay-osd-spec.md && grep -qi "OSD" .planning/intel/options-plus/overlay-osd-spec.md && grep -qi "radial\|wheel\|slice" .planning/intel/options-plus/overlay-osd-spec.md && grep -qi "osd_resources\|notification_resources" .planning/intel/options-plus/overlay-osd-spec.md && grep -qi "WHEEL-01\|WHEEL-02\|WHEEL-03\|degrad" .planning/intel/options-plus/overlay-osd-spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/intel/options-plus/overlay-osd-spec.md` exists
    - File documents the OSD resource model citing `osd_resources` / `notification_resources`
    - File describes the radial/slice/wheel interaction (grep matches radial/wheel/slice)
    - File maps to WHEEL-01/02/03 and notes graceful degradation
    - File has an explicit asset-boundary note (no overlay art bundled)
    - No icons / JSON copied into `src/`
  </acceptance_criteria>
  <done>overlay-osd-spec.md exists with the OSD resource model (cited), the radial/action-wheel interaction mapped to WHEEL-01/02/03, the gesture OSD, a graceful-degradation note, and an asset boundary — citing source files, nothing copied into src/.</done>
</task>

</tasks>

<verification>
- Both specs exist under `.planning/intel/options-plus/`
- ui-design-spec.md captures tabs + interaction + alignment deltas; overlay-osd-spec.md captures OSD model + radial mechanic + degradation
- No extracted/overlay Logitech asset under `src/`
</verification>

<success_criteria>
- REF-02 satisfied (app.asar UI studied → written ui-design-spec.md re-implementation target)
- REF-01/REF-02 (overlay-osd-spec.md owned spec produced from overlay JSON + app.asar study)
- No proprietary asset copied into `src/`
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-options-plus-reference-mining/04.1-04-SUMMARY.md`
</output>
