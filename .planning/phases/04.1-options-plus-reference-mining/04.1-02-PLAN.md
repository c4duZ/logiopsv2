---
phase: 04.1-options-plus-reference-mining
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/intel/options-plus/smart-action-schema.md
  - .planning/intel/options-plus/app-match-model.md
autonomous: true
requirements: [REF-01]

must_haves:
  truths:
    - "A smart-action/macro schema spec we own captures the step/card vocabulary Phase 7 will re-implement"
    - "An app-match-model spec we own captures the window-class/executable match-rule schema Phase 5 will re-implement"
  artifacts:
    - path: ".planning/intel/options-plus/smart-action-schema.md"
      provides: "Owned macro step/card schema (macro.type vocabulary, card fields, categories, per-app integration cards)"
      contains: "APP_WINDOWS_MANAGEMENT"
    - path: ".planning/intel/options-plus/app-match-model.md"
      provides: "Owned app-match rule schema (applicationId, match by window-class/executable, card binding)"
      contains: "applicationId"
  key_links:
    - from: ".planning/intel/options-plus/smart-action-schema.md"
      to: "LogiOptionsPlus/data/macros/predefined_win.json"
      via: "cited macro.type values and card field names"
      pattern: "BRING_TO_FOREGROUND\\|APP_WINDOWS_MANAGEMENT"
    - from: ".planning/intel/options-plus/app-match-model.md"
      to: "LogiOptionsPlus/data/applications.json"
      via: "cited applicationId + card structure"
      pattern: "applicationId"
---

<objective>
Produce two owned reference specs from the Options+ readable JSON data: the **smart-action/macro schema** (step + card vocabulary, for Phase 7) and the **app-match model** (window-class/executable match-rule schema, for Phase 5).

Purpose: Phase 7 (Smart Actions / Macros) and Phase 5 (Per-App Profiles) currently have to guess Options+ data shapes. These two specs turn the readable `macros/`, `integrations/`, and `applications.json` data into schemas WE own so those phases adapt instead of guessing.

Output: `.planning/intel/options-plus/smart-action-schema.md`, `app-match-model.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-options-plus-reference-mining/CONTEXT.md

<source_material>
All under `LogiOptionsPlus/` (gitignored, not bundled):
- `data/macros/predefined_win.json`, `predefined_osx.json` — top-level `macro_infos[]` each with `id`, `name`, `description`, `state`, `originType`, `categories[]`, `cards[]`. Each card has `applicationId`, `attribute` (e.g. `MACRO_PLAYBACK`), `id`, `name`, `readOnly`, `tags[]`, and a `macro` object whose `type` is one of e.g. `APP_WINDOWS_MANAGEMENT` (with `appWindowsManagement.action` like `BRING_TO_FOREGROUND`), `DELAY` (with `delay.durationMs`), `MOUSE` (with `mouse.action`/`hidUsage`), etc.
- `data/macros/common_application_cards_win.json`, `extended_application_cards_map_win.json`, `devices_predefined_map_win.json` — card maps.
- `integrations/plugin_*/config.json` (plugin_photoshop, plugin_premiere_pro, plugin_illustrator, plugin_indesign, plugin_lightroom_classic, plugin_word, plugin_excel, plugin_powerpoint) — each has `supportedApplicationIds[]`, `name`, `actionSdk.actions[]` (each with `name`, `actionId`, `actionIcon`, `parameters[]`).
- `data/applications.json` (420KB) — top-level `applications[]`, each with `applicationId`, `cards[]` (each card: `attribute`, `continuous`, `id`, `macro{type, mouse{action,hidUsage}, ...}`, `name`, `nestedCards`, `readOnly`, `tags[]`, `taskId`).

Confirmed grep anchors:
- predefined_win.json contains `APP_WINDOWS_MANAGEMENT`, `BRING_TO_FOREGROUND`, `DELAY`, `durationMs`, `categories`, `cards`, `applicationId`
- applications.json contains `applicationId`, `application_id_google_chrome`, `taskId`, `nestedCards`
- plugin_photoshop/config.json contains `supportedApplicationIds`, `actionSdk`, `actionId`
</source_material>

<legal_constraint>
LOCKED DECISION: HYBRID / reference-only. Distill the SCHEMA (field names, vocabulary, structure) into our own spec. Do NOT copy the JSON files wholesale, do NOT copy Logitech icons or verbatim string resources into `src/`. Output lives ONLY under `.planning/intel/options-plus/`. NO task writes to `src/`.
</legal_constraint>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Distill the smart-action / macro schema spec</name>
  <files>.planning/intel/options-plus/smart-action-schema.md</files>
  <read_first>
    `LogiOptionsPlus/data/macros/predefined_win.json` (read the first ~200 lines + grep for all distinct `"type"` values: `grep -oE '"type": "[A-Z_]+"' LogiOptionsPlus/data/macros/predefined_win.json | sort | uniq -c`). Then `grep -oE '"action": "[A-Z_]+"' LogiOptionsPlus/data/macros/predefined_win.json | sort -u` for the action enum. Read one `integrations/plugin_photoshop/config.json` to capture the per-app `actionSdk.actions[]` shape. Read `data/macros/common_application_cards_win.json` head for the card-map structure.
  </read_first>
  <action>
    Create `.planning/intel/options-plus/smart-action-schema.md` (REF-01) — OUR own schema spec for Phase 7. Structure:
    1. "Step (macro) vocabulary" — a table of every distinct `macro.type` found in predefined_win.json (e.g. `APP_WINDOWS_MANAGEMENT`, `DELAY`, `MOUSE`, plus any others the grep surfaces), each with: the type, its payload sub-object name (e.g. `appWindowsManagement`, `delay`, `mouse`), the payload fields (e.g. `action`, `durationMs`, `hidUsage`), and a one-line "Our equivalent step" note mapping to how WE'll model it (e.g. DELAY → inter-step delay ms; MOUSE → media/mouse action; APP_WINDOWS_MANAGEMENT → window action). This is the MACRO-01 step model.
    2. "Action enum" — the distinct `action` values (e.g. `BRING_TO_FOREGROUND`, mouse `WIN_BACK`/`WIN_FORWARD`, etc.) grouped by step type.
    3. "Card model" — the card object fields (`applicationId`, `attribute`, `id`, `name`, `readOnly`, `tags[]`, `categories[]`) and what each means, with the `originType`/`state` lifecycle.
    4. "Per-app integration cards" — from `integrations/plugin_*/config.json`: the `supportedApplicationIds` + `actionSdk.actions[]` shape (`name`, `actionId`, `parameters[]`), and the list of the 8 bundled plugins (photoshop, premiere_pro, illustrator, indesign, lightroom_classic, word, excel, powerpoint) as evidence of the per-app card model Phase 7 will mirror.
    5. "Our re-implementation note" — short prose: we build an ordered step list (keystroke/text/media/delay + launch/open-URL routed to a non-root session helper per MACRO-02); we do NOT bundle Logitech's predefined macro JSON or icons.
    Cite source file + key for each section. Distill, do not paste whole files.
  </action>
  <verify>
    <automated>test -f .planning/intel/options-plus/smart-action-schema.md && grep -q "APP_WINDOWS_MANAGEMENT" .planning/intel/options-plus/smart-action-schema.md && grep -q "BRING_TO_FOREGROUND" .planning/intel/options-plus/smart-action-schema.md && grep -qi "DELAY" .planning/intel/options-plus/smart-action-schema.md && grep -qi "actionSdk\|plugin\|integration" .planning/intel/options-plus/smart-action-schema.md && grep -qi "MACRO-01\|MACRO-02\|step" .planning/intel/options-plus/smart-action-schema.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/intel/options-plus/smart-action-schema.md` exists
    - File contains `APP_WINDOWS_MANAGEMENT` and `BRING_TO_FOREGROUND` (cited macro vocabulary)
    - File documents the `DELAY` step with `durationMs`
    - File documents the per-app integration card model (grep matches actionSdk/plugin/integration) and lists the 8 plugins
    - File has a card-model section with `applicationId`, `attribute`, `categories`
    - File maps to MACRO-01/MACRO-02 (our re-implementation note)
    - No content written under `src/`
  </acceptance_criteria>
  <done>smart-action-schema.md exists with a step-type vocabulary table, an action enum, a card-model section, a per-app integration-card section listing the 8 plugins, and a re-implementation note tying to MACRO-01/02 — all citing source keys, nothing copied into src/.</done>
</task>

<task type="auto">
  <name>Task 2: Distill the app-match model spec</name>
  <files>.planning/intel/options-plus/app-match-model.md</files>
  <read_first>
    `LogiOptionsPlus/data/applications.json` — read the first ~120 lines for the `applications[].cards[]` shape, then `grep -oE '"(applicationId|taskId|continuous|attribute)"' LogiOptionsPlus/data/applications.json | sort | uniq -c` for field frequency, and `grep -c '"applicationId"' LogiOptionsPlus/data/applications.json` for app count. Inspect how an app is identified (e.g. `application_id_google_chrome`) and whether window-class/executable strings appear (`grep -iE "windowClass|executable|exe|bundleId|process" LogiOptionsPlus/data/applications.json | head`).
  </read_first>
  <action>
    Create `.planning/intel/options-plus/app-match-model.md` (REF-01) — OUR own app-match rule schema for Phase 5. Structure:
    1. "Application entry schema" — the `applications[]` entry fields (`applicationId`, `cards[]`, and any matching fields found). Note the count of apps (from the grep).
    2. "Match-rule model" — document HOW Options+ identifies an application (the `applicationId` token scheme like `application_id_google_chrome`, plus any window-class / executable / bundleId fields actually present in the JSON; if the file does NOT contain raw window-class/exe strings, say so explicitly and note that PROF-03 (match by window class / executable) is OUR rule layer to design, seeded by the Options+ applicationId taxonomy as the catalog). Be precise: report what the file actually contains, do not invent fields.
    3. "Card binding" — how cards attach per app (`attribute`, `continuous`, `macro.type`, `taskId`, `tags[]`) — the connection between an app match and the actions it enables.
    4. "Our re-implementation note" — our PROF-02/03 rule schema: a default profile + per-app overrides matched by window class (X11 `WM_CLASS`) / executable, switched by a non-root session focus-watcher calling the existing `ChangeProfile`. We reference the Options+ applicationId taxonomy as a catalog but do NOT bundle `applications.json`.
    Cite source file + keys. Distill, do not paste the 420KB file.
  </action>
  <verify>
    <automated>test -f .planning/intel/options-plus/app-match-model.md && grep -q "applicationId" .planning/intel/options-plus/app-match-model.md && grep -qi "window.?class\|executable\|WM_CLASS" .planning/intel/options-plus/app-match-model.md && grep -qi "PROF-02\|PROF-03\|focus" .planning/intel/options-plus/app-match-model.md && grep -qi "applications.json" .planning/intel/options-plus/app-match-model.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/intel/options-plus/app-match-model.md` exists
    - File contains `applicationId` (cited from applications.json)
    - File discusses matching by window class / executable (grep matches window-class/executable/WM_CLASS) and is precise about what the JSON actually contains vs what is our rule layer
    - File references `applications.json` as the source and states we do NOT bundle it
    - File maps to PROF-02/PROF-03 (our re-implementation note)
    - No content written under `src/`
  </acceptance_criteria>
  <done>app-match-model.md exists with an application-entry schema, an honest match-rule model (reporting actual JSON fields vs our rule layer), a card-binding section, and a PROF-02/03 re-implementation note — citing applications.json, nothing copied into src/.</done>
</task>

</tasks>

<verification>
- Both files exist under `.planning/intel/options-plus/`
- smart-action-schema.md cites `APP_WINDOWS_MANAGEMENT`/`BRING_TO_FOREGROUND`; app-match-model.md cites `applicationId`
- No `LogiOptionsPlus/` JSON copied verbatim into `src/`
</verification>

<success_criteria>
- REF-01 partially satisfied (smart-action schema + app-match model owned specs produced from macros/, integrations/, applications.json)
- No proprietary asset copied into `src/`
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-options-plus-reference-mining/04.1-02-SUMMARY.md`
</output>
