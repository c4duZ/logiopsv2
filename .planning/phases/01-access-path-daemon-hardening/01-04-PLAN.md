---
phase: 01-access-path-daemon-hardening
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/logid/backend/hidpp/Report.h
  - src/logid/backend/hidpp/Device.cpp
  - src/logid/backend/hidpp10/ReceiverMonitor.cpp
  - src/logid/backend/hidpp/Report.cpp
  - src/logid/util/ExceptionHandler.cpp
  - src/logid/logid.cpp
  - src/logid/backend/raw/RawDevice.cpp
autonomous: true
requirements: [ACCESS-04]
must_haves:
  truths:
    - "A short/empty HID report is rejected before any header field is indexed (no OOB read)"
    - "The shared hasHidppHeader() helper guards all three raw filter lambdas"
    - "ExceptionHandler::Default rethrows the active exception preserving dynamic type"
    - "No runtime-controlled string is passed as a logPrintf format argument"
    - "Read-length validation holds in release builds (no assert)"
  artifacts:
    - path: "src/logid/backend/hidpp/Report.h"
      provides: "hasHidppHeader() bounds helper in namespace logid::backend::hidpp"
      contains: "hasHidppHeader"
    - path: "src/logid/util/ExceptionHandler.cpp"
      provides: "Bare-throw rethrow preserving dynamic type (CONCERNS #1)"
  key_links:
    - from: "src/logid/backend/hidpp/Device.cpp"
      to: "src/logid/backend/hidpp/Report.h"
      via: "hasHidppHeader() guard at the raw filter lambda"
      pattern: "hasHidppHeader"
    - from: "test/hidpp_bounds_test.cpp"
      to: "src/logid/backend/hidpp/Report.h"
      via: "the unit turns GREEN once hasHidppHeader() exists"
      pattern: "hasHidppHeader"
---

<objective>
Close the untrusted-HID out-of-bounds-read surface (ACCESS-04 / CONCERNS #3) by length-checking
report fields before indexing, via one shared `hasHidppHeader()` helper applied at the three raw
filter lambdas, plus a defense-in-depth guard in `Report::isError10/20`. Bundle the three
same-theme security bug fixes: CONCERNS #1 (sliced exception), #5 (format-string log), #8
(release-mode assert on read length). This plan's helper also turns Plan 01's RED `hidpp_bounds`
unit GREEN.

Purpose: ACCESS-04 — hardware input treated as untrusted; security checks survive release builds.
Output: A `hasHidppHeader()` helper + guarded lambdas + three CONCERNS fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-access-path-daemon-hardening/01-RESEARCH.md
@.planning/phases/01-access-path-daemon-hardening/01-CONTEXT.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
<!-- VERIFIED in-tree this session. -->
<!-- src/logid/backend/hidpp/Report.h -->
- Guard LOGID_BACKEND_HIDPP_REPORT_H; namespace logid::backend::hidpp
- static constexpr std::size_t Report::HeaderLength = 4; (Report.h:130)
- Offset::Type=0, DeviceIndex=1, SubID=2, Feature=2, Address=3, Function=3, Parameters=4 (Report.h:35-43)
<!-- Reports arrive as const std::vector<uint8_t>& built from exactly read() length:
     std::vector<uint8_t> report(buf, buf+len) in RawDevice::_readReports (RawDevice.cpp:228-244).
     A short report yields a short vector indexed before any length check. -->
<!-- ExceptionHandler::Default(std::exception&) is CALLED FROM inside a catch (so bare throw; is valid)
     — RESEARCH A10 says VERIFY all callers (util/task.cpp etc.) are inside a catch. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hasHidppHeader() helper and guard the three raw filter lambdas (ACCESS-04)</name>
  <read_first>
    - src/logid/backend/hidpp/Report.h (lines 20-21 guard, 35-43 Offsets, 130 HeaderLength)
    - src/logid/backend/hidpp/Device.cpp (lines 110-135 — the _raw_handler filter lambda)
    - src/logid/backend/hidpp10/ReceiverMonitor.cpp (lines 30-55 connect filter; 155-179 waitForDevice filter)
    - test/hidpp_bounds_test.cpp (the RED unit from Plan 01 that calls hasHidppHeader)
  </read_first>
  <behavior>
    - hasHidppHeader({}) == false ; hasHidppHeader({0x11}) == false ; hasHidppHeader(3 bytes) == false
    - hasHidppHeader(exactly 4 bytes) == true ; hasHidppHeader(7-byte short report) == true
    - Each guarded filter lambda returns false (drops the report) for a <4-byte vector instead of indexing it
  </behavior>
  <files>src/logid/backend/hidpp/Report.h, src/logid/backend/hidpp/Device.cpp, src/logid/backend/hidpp10/ReceiverMonitor.cpp</files>
  <action>
    In `src/logid/backend/hidpp/Report.h`, inside `namespace logid::backend::hidpp` (after the Report
    class or near the Offsets), add a free inline (snake_case free fn per CONVENTIONS):
    ```cpp
    // Returns true if `report` is long enough to safely read the HID++ header.
    inline bool hasHidppHeader(const std::vector<uint8_t>& report) {
        return report.size() >= Report::HeaderLength;   // 4 bytes: Type,Index,SubID/Feature,Addr/Func
    }
    ```
    Ensure `<vector>` and `<cstdint>` are included by Report.h (add if missing).

    Site 1 — src/logid/backend/hidpp/Device.cpp:121-126 (the `_raw_handler` filter lambda). Add at the
    TOP of the lambda body, before any `report[...]` index:
    ```cpp
    if (!hasHidppHeader(report))
        return false;
    ```

    Site 2 — src/logid/backend/hidpp10/ReceiverMonitor.cpp:39 (connect filter lambda). Same guard at
    the top of the lambda, before indexing Offset::Type / Offset::SubID.

    Site 3 — src/logid/backend/hidpp10/ReceiverMonitor.cpp:165 (waitForDevice filter lambda). Same
    guard at the top, before indexing Offset::SubID / Offset::DeviceIndex.

    Use `hasHidppHeader` (qualified as needed for the namespace) at every site for consistency. NO
    `assert` anywhere (locked decision). This makes Plan 01's `hidpp_bounds` unit compile and pass.
  </action>
  <verify>
    <automated>grep -q 'hasHidppHeader' src/logid/backend/hidpp/Report.h && grep -q 'hasHidppHeader' src/logid/backend/hidpp/Device.cpp && [ "$(grep -c 'hasHidppHeader' src/logid/backend/hidpp10/ReceiverMonitor.cpp)" -ge 2 ] && cmake --build build --target hidpp_bounds_test >/dev/null 2>&1 && ctest --test-dir build -R hidpp_bounds --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'inline bool hasHidppHeader' src/logid/backend/hidpp/Report.h` succeeds
    - `hasHidppHeader` appears in Device.cpp once and ReceiverMonitor.cpp at least twice
    - `ctest --test-dir build -R hidpp_bounds` is GREEN (Plan 01's RED unit now passes)
    - No new `assert(` added at the three filter sites
    - Build is warning-clean under `-Wall -Wextra`
  </acceptance_criteria>
  <done>The shared bounds helper exists and guards all three raw filter lambdas; the hidpp_bounds unit passes.</done>
</task>

<task type="auto">
  <name>Task 2: Defense-in-depth guard in Report::isError10/isError20 (ACCESS-04)</name>
  <read_first>
    - src/logid/backend/hidpp/Report.cpp (lines 118-175 ctor padding; 269-294 isError10/isError20)
    - src/logid/backend/hidpp/Report.h (Offset::Parameters=4)
    - .planning/phases/01-access-path-daemon-hardening/01-RESEARCH.md (HID Length-Checks Site 4)
  </read_first>
  <files>src/logid/backend/hidpp/Report.cpp</files>
  <action>
    In `isError10` and `isError20` (Report.cpp:269-294), which read `_data[3]`, `_data[4]`, `_data[5]`,
    add an early guard before those reads:
    ```cpp
    if (_data.size() <= Offset::Parameters + 1)   // need indices 3,4,5 => size() >= 6
        return false;
    ```
    Document in the SUMMARY that the `Report(const std::vector<uint8_t>&)` ctor (Report.cpp:160-175)
    already resizes `_data` to `HeaderLength + LongParamLength` (=20), so these are belt-and-suspenders;
    the PRIMARY OOB surface is Sites 1-3 (raw vectors) handled in Task 1. NO assert.
  </action>
  <verify>
    <automated>grep -q 'Offset::Parameters' src/logid/backend/hidpp/Report.cpp && grep -Eq 'isError10|isError20' src/logid/backend/hidpp/Report.cpp && cmake --build build >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A `_data.size()` guard returning false precedes the `_data[3..5]` reads in isError10/isError20
    - `cmake --build build` succeeds warning-clean (-Wall -Wextra)
    - No `assert(` introduced in these functions
  </acceptance_criteria>
  <done>isError10/isError20 short-circuit on under-length _data before indexing; build is clean.</done>
</task>

<task type="auto">
  <name>Task 3: CONCERNS fixes #1 (sliced exception), #5 (format-string), #8 (release assert)</name>
  <read_first>
    - src/logid/util/ExceptionHandler.cpp (lines 26-38 — the throw error; slice)
    - src/logid/util/ExceptionHandler.h and any caller (util/task.cpp) to confirm Default is invoked from inside a catch (RESEARCH A10)
    - src/logid/logid.cpp (line 97 — logPrintf(WARN, e.what()))
    - src/logid/backend/raw/RawDevice.cpp (lines 227-244 — assert(len <= max_data_length); RawDevice.h:49 max_data_length=32)
    - .planning/phases/01-access-path-daemon-hardening/01-RESEARCH.md (CONCERNS Fixes)
  </read_first>
  <files>src/logid/util/ExceptionHandler.cpp, src/logid/logid.cpp, src/logid/backend/raw/RawDevice.cpp</files>
  <action>
    #1 — ExceptionHandler.cpp (CONCERNS #1, also called out in CLAUDE.md): VERIFY first that every
    caller invokes `ExceptionHandler::Default` from inside an active `catch` (check util/task.cpp).
    If confirmed, change `throw error;` to a bare `throw;` to rethrow the currently-handled exception
    preserving dynamic type, and switch the per-catch log args from `error.what()` to `e.what()`:
    ```cpp
    void ExceptionHandler::Default(std::exception& error) {
        try {
            throw;   // rethrow the active exception, preserving dynamic type
        } catch (backend::hidpp10::Error& e) {
            logPrintf(WARN, "HID++ 1.0 error ignored on task: %s", e.what());
        } catch (backend::hidpp20::Error& e) {
            logPrintf(WARN, "HID++ 2.0 error ignored on task: %s", e.what());
        } catch (std::system_error& e) {
            logPrintf(WARN, "System error ignored on task: %s", e.what());
        } catch (std::exception& e) {
            logPrintf(WARN, "Error ignored on task: %s", e.what());
        }
    }
    ```
    If a caller is found that invokes Default OUTSIDE an active exception, do NOT use bare `throw;`
    there (it would `std::terminate`); instead document the finding in the SUMMARY and keep the
    dynamic-type-preserving rethrow guarded. (RESEARCH A10 / Pitfall 5.)

    #5 — logid.cpp:97 (CONCERNS #5; CLAUDE.md warns explicitly): change
    `logPrintf(WARN, e.what());` to `logPrintf(WARN, "%s", e.what());`. Then sweep for siblings:
    `grep -rn "logPrintf([A-Z_]*, [a-z_]" src/` and fix any other call where the 2nd arg is a
    runtime expression rather than a string literal; list findings in the SUMMARY.

    #8 — RawDevice.cpp:233 (CONCERNS #8): replace the release-stripped `assert(len <= max_data_length);`
    in `_readReports()` with an explicit runtime guard that survives NDEBUG:
    ```cpp
    if (len < 0 || len > max_data_length) {
        logPrintf(WARN, "Ignoring HID read of unexpected length %zd on %s",
                  (ssize_t)len, _path.c_str());
        continue;   // do not construct a report from a bogus length
    }
    std::vector<uint8_t> report(buf, buf + len);
    ```
    Leave `Report::setParams`'s internal assert (Report.cpp:263) alone (not attacker-controlled).
    Do not remove `#include <cassert>` unless `-Werror`/unused warnings demand it.
  </action>
  <verify>
    <automated>grep -q 'throw;' src/logid/util/ExceptionHandler.cpp && ! grep -q 'throw error;' src/logid/util/ExceptionHandler.cpp && grep -q 'logPrintf(WARN, "%s", e.what())' src/logid/logid.cpp && grep -q 'Ignoring HID read of unexpected length' src/logid/backend/raw/RawDevice.cpp && ! grep -q 'assert(len <= max_data_length)' src/logid/backend/raw/RawDevice.cpp && cmake --build build >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'throw;' src/logid/util/ExceptionHandler.cpp` and `! grep -q 'throw error;'` both hold
    - `grep -q 'logPrintf(WARN, "%s", e.what())' src/logid/logid.cpp` succeeds
    - The `grep -rn "logPrintf([A-Z_]*, [a-z_]" src/` sweep findings are listed in the SUMMARY (or "none")
    - `! grep -q 'assert(len <= max_data_length)' src/logid/backend/raw/RawDevice.cpp` holds
    - RawDevice.cpp contains the explicit length guard with `continue;`
    - `cmake --build build` succeeds warning-clean across the default build type
  </acceptance_criteria>
  <done>The three CONCERNS security fixes land; exceptions preserve dynamic type, no runtime format strings, and read-length validation holds under NDEBUG.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HID hardware → daemon | Reports from USB/BT devices are untrusted; a short/malformed report must not OOB-read |
| error path → logger | Exception messages flowing into printf-style logging are runtime-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | I/D/T | OOB read on short HID report at the raw filter lambdas (CONCERNS #3) | mitigate | hasHidppHeader() guard at all three lambdas; verified by the hidpp_bounds CTest unit (Task 1). |
| T-01-04-02 | T (Tampering)/RCE-class | format-string injection via e.what() as printf format (CONCERNS #5) | mitigate | `logPrintf(WARN, "%s", e.what())` + tree-wide sweep for siblings (Task 3). |
| T-01-04-03 | defense-bypass | security check elided under NDEBUG via assert (CONCERNS #8) | mitigate | Replace assert with an explicit runtime guard + continue (Task 3). |
| T-01-04-04 | correctness→masking | sliced exception hides real error types (CONCERNS #1) | mitigate | Bare `throw;` preserves dynamic type so type-specific catches match (Task 3). |
| T-01-04-05 | D (DoS) | bare `throw;` outside an active exception → std::terminate | mitigate | Verify all ExceptionHandler::Default callers are inside a catch before applying (RESEARCH A10). |
</threat_model>

<verification>
- `ctest --test-dir build -R hidpp_bounds` GREEN (the OOB guard works end-to-end).
- grep assertions confirm all three CONCERNS fixes and the three guarded lambdas.
- `cmake --build build` warning-clean (-Wall -Wextra; CI adds -Werror).
- Recommended manual (per VALIDATION.md): an AddressSanitizer build feeding 1-3 byte vectors through
  the filter lambdas / Report ctor stays clean.
</verification>

<success_criteria>
- ACCESS-04: short/malformed reports are rejected before indexing; the hidpp_bounds unit passes.
- CONCERNS #1/#5/#8 fixed; checks survive release builds; no runtime format strings.
- Build warning-clean.
</success_criteria>

<output>
After completion, create `.planning/phases/01-access-path-daemon-hardening/01-04-SUMMARY.md`.
Record: the ExceptionHandler caller audit result (RESEARCH A10) and the logPrintf sweep findings.
</output>
