---
phase: 05-per-application-profiles-profile-sharing
plan: 04
type: execute
wave: 4
depends_on: ["05-02", "05-03"]
files_modified:
  - src/logiops-gui/AppRulesModel.h
  - src/logiops-gui/AppRulesModel.cpp
  - src/logiops-gui/ProfileSharing.h
  - src/logiops-gui/ProfileSharing.cpp
  - src/logiops-gui/main.cpp
  - src/logiops-gui/CMakeLists.txt
  - src/logiops-gui/qml/config/AppRulesTab.qml
  - src/logiops-gui/qml/config/ConfigTabs.qml
  - tests/phase5/app_rules_model_test.cpp
  - tests/phase5/CMakeLists.txt
autonomous: false
requirements: [PROF-02, PROF-03, PROF-04]

must_haves:
  truths:
    - "A user can add/edit/remove app-match rules (window-class or executable → profile) in the GUI and they persist (PROF-03)"
    - "A user can set the default profile that covers everything else (PROF-02)"
    - "A user can click Export to write a profile file and Import to load one via a file dialog (PROF-04)"
    - "The rules the GUI writes are the exact file the session helper reads (same app-profiles.json schema)"
  artifacts:
    - path: "src/logiops-gui/AppRulesModel.h"
      provides: "QAbstractListModel of app-match rules backed by the RuleStore schema (PROF-03)"
      contains: "class AppRulesModel"
    - path: "src/logiops-gui/qml/config/AppRulesTab.qml"
      provides: "The per-app rules management UI (rule list + add/edit + default profile + export/import)"
    - path: "src/logiops-gui/ProfileSharing.cpp"
      provides: "QObject bridge: export selected profile / import a file via ProfileIO (PROF-04)"
  key_links:
    - from: "src/logiops-gui/AppRulesModel.cpp"
      to: "$XDG_CONFIG_HOME/logiops/app-profiles.json"
      via: "RuleStore load/save (same file the helper reads)"
      pattern: "app-profiles\\.json|RuleStore"
    - from: "src/logiops-gui/qml/config/ConfigTabs.qml"
      to: "AppRulesTab.qml"
      via: "new tab in the StackLayout"
      pattern: "AppRulesTab"
---

<objective>
Give the user the Options+-style UI to manage per-application profile switching:
add/edit/remove app-match rules (window-class or executable → profile), pick the
default profile, and export/import profiles as shareable files. The model writes the
SAME app-profiles.json the Plan-02 session helper reads, closing the PROF-02/03 loop;
the export/import buttons drive Plan-03 ProfileIO (PROF-04). Ends with an on-hardware
UAT checkpoint because focus-following + file sharing are inherently runtime/visual.

Purpose: Surface the phase's functionality to the user in a polished tab.
Output: AppRulesTab + AppRulesModel + ProfileSharing bridge + UAT verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The rule schema/store the GUI must write identically (so the helper reads it):
@src/logiops-session-helper/RuleStore.h
@src/logiops-session-helper/AppMatchRule.h
# The export/import logic the buttons call:
@src/logiops-gui/ProfileIO.h
# Patterns to mirror: existing tab + model wiring + theme.
@src/logiops-gui/qml/config/ProfilesTab.qml
@src/logiops-gui/qml/config/ConfigTabs.qml
@src/logiops-gui/ProfilesModel.h
@src/logiops-gui/main.cpp
@src/logiops-gui/qml/Theme.qml
# Reference-only visual fidelity target (read, do not bundle):
@.planning/intel/options-plus/ui-design-spec.md
@.planning/intel/options-plus/app-match-model.md

<interfaces>
<!-- AppRulesModel mirrors ProfilesModel's QAbstractListModel + context-property style.
     It must serialize VIA the SAME RuleStore schema the helper uses (Plan 02) so the
     file the GUI writes is the file the helper reads. If RuleStore is daemon-helper-only,
     re-use its JSON shape exactly (version/defaultProfile/rules[field,pattern,profile]). -->
class AppRulesModel : public QAbstractListModel {
  // roles: FieldRole (windowClass|executable), PatternRole (s), ProfileRole (s)
  Q_INVOKABLE void addRule(const QString& field, const QString& pattern, const QString& profile);
  Q_INVOKABLE void updateRule(int row, const QString& field, const QString& pattern, const QString& profile);
  Q_INVOKABLE void removeRule(int row);
  Q_PROPERTY(QString defaultProfile ...)  // set/get; persisted
  // every mutation persists to app-profiles.json AND notifies (the helper hot-reloads)
};
class ProfileSharing : public QObject {  // PROF-04 bridge
  Q_INVOKABLE bool exportProfile(const QString& profileName, const QUrl& toFile);
  Q_INVOKABLE QString importProfile(const QUrl& fromFile);  // returns imported name or "" + sets lastError
  Q_PROPERTY(QString lastError ...)
};
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppRulesModel + ProfileSharing C++ bridges (RuleStore-backed + ProfileIO-backed)</name>
  <read_first>src/logiops-gui/ProfilesModel.h/.cpp (QAbstractListModel + roles + Q_INVOKABLE mutators + context-property pattern), src/logiops-session-helper/RuleStore.h (the JSON schema to write identically), src/logiops-gui/ProfileIO.h (export/import signatures)</read_first>
  <files>src/logiops-gui/AppRulesModel.h, src/logiops-gui/AppRulesModel.cpp, src/logiops-gui/ProfileSharing.h, src/logiops-gui/ProfileSharing.cpp, src/logiops-gui/main.cpp, src/logiops-gui/CMakeLists.txt</files>
  <action>
    Create `AppRulesModel` (QAbstractListModel) exposing FieldRole/PatternRole/ProfileRole
    rows + addRule/updateRule/removeRule + a `defaultProfile` Q_PROPERTY. Persist EVERY
    mutation to `$XDG_CONFIG_HOME/logiops/app-profiles.json` using the SAME schema as
    Plan-02 RuleStore (reuse RuleStore directly if it is compiled into a shared lib; else
    replicate its exact JSON shape — version/defaultProfile/rules[]). The helper hot-reloads
    via its QFileSystemWatcher, so writing the file IS the wiring.
    Create `ProfileSharing` (QObject): `exportProfile(name, QUrl)` fetches the profile block
    (read the device's libconfig profile — pull the current device's profile settings; if the
    daemon exposes no per-profile getter, export the named profile from the live config the
    GUI already holds) → `ProfileIO::exportProfile` → write to the chosen file;
    `importProfile(QUrl)` reads the file → `ProfileIO::importProfile` (catch + set lastError
    on bad envelope) → apply via the existing .Device.SetProfile + per-setting setters.
    Register both as context properties in `main.cpp` (mirror the existing setContextProperty
    block). Add the new .cpp files to `logiops-gui` + `logiops-gui-lib` target_sources.
  </action>
  <verify>
    <automated>cd /home/c4duz/Primor/Projetos/logiopsv2 && cmake -S . -B build -DBUILD_GUI=ON >/dev/null 2>&1 && cmake --build build --target logiops-gui 2>&1 | tail -3 | grep -qiv "error:"</automated>
  </verify>
  <done>AppRulesModel persists rules to app-profiles.json (helper-compatible schema); ProfileSharing export/import wired to ProfileIO; both are context properties; GUI builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: AppRulesModel persistence unit + AppRulesTab.qml + tab registration</name>
  <read_first>src/logiops-gui/qml/config/ProfilesTab.qml (tab layout/Theme usage to mirror), src/logiops-gui/qml/config/ConfigTabs.qml (how tabs register in the StackLayout), tests/phase3/ProfilesModelTest.cpp (QtTest model-test style), .planning/intel/options-plus/ui-design-spec.md (visual fidelity)</read_first>
  <files>tests/phase5/app_rules_model_test.cpp, tests/phase5/CMakeLists.txt, src/logiops-gui/qml/config/AppRulesTab.qml, src/logiops-gui/qml/config/ConfigTabs.qml</files>
  <behavior>
    - addRule then a fresh AppRulesModel pointed at the same file loads the rule back (persisted).
    - removeRule removes the right row and persists.
    - Setting defaultProfile persists and reloads.
    - The written file parses with the Plan-02 RuleStore schema (version/defaultProfile/rules).
  </behavior>
  <action>
    Write `app_rules_model_test.cpp` (QtTest, mirrors ProfilesModelTest) pointed at a temp
    config dir (BUILD_TMP_DIR, never real XDG) asserting the persistence behaviors above;
    register it in tests/phase5/CMakeLists.txt guarded on `TARGET logiops-gui-lib` + EXISTS.
    Build `AppRulesTab.qml`: a rule list (each row: field combo windowClass|executable,
    pattern text field, profile combo populated from ProfilesModel), add/remove controls, a
    default-profile selector, and an Export/Import row that opens a `FileDialog` and calls
    `profileSharing.exportProfile/importProfile` (show lastError on failure). Use the existing
    Theme tokens; match the ui-design-spec layout (reference-only — own QML/strings). Register
    AppRulesTab in ConfigTabs.qml's StackLayout (add the tab entry + its index) alongside the
    existing Buttons/Pointer/Scroll/Profiles tabs.
  </action>
  <verify>
    <automated>cd /home/c4duz/Primor/Projetos/logiopsv2 && cmake -S . -B build -DBUILD_GUI=ON -DBUILD_TESTING=ON >/dev/null 2>&1 && cmake --build build --target logiops-gui >/dev/null 2>&1 && (cmake --build build --target phase5_app_rules_model_test >/dev/null 2>&1 && ctest --test-dir build -R phase5_app_rules_model --output-on-failure || echo "model test optional-if-no-Qt-Test"); grep -q AppRulesTab src/logiops-gui/qml/config/ConfigTabs.qml</automated>
  </verify>
  <done>app_rules_model persistence test GREEN; AppRulesTab.qml exists and is registered in ConfigTabs; GUI builds + QML loads clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-hardware UAT — focus-following profile switch + export/import round-trip</name>
  <action>Operator-driven verification only — no autonomous implementation. Follow the how-to-verify steps below to confirm PROF-02/03/04/05 end-to-end on the real session.</action>
  <what-built>
    End-to-end Phase 5 on real hardware/session: the session helper auto-switches the
    active profile when the focused app changes (PROF-02/03), works on the operator's
    actual session type with documented degradation (PROF-05), and profile export/import
    round-trips through a file (PROF-04).
  </what-built>
  <how-to-verify>
    1. Build + run the helper: `cmake --build build --target logiops-session-helper && ./build/src/logiops-session-helper/logiops-session-helper -v`. Note the logged session type + whether per-app switching is supported or degraded on this compositor.
    2. In the GUI, open the new App Rules tab. Add a rule: field=executable (or windowClass), pattern matching a real app you have open (e.g. `firefox`), profile=an existing profile. Set a default profile. Confirm `~/.config/logiops/app-profiles.json` now contains the rule.
    3. Focus the matched app, then focus another app. Confirm the device's active profile follows: the matched app activates its profile, everything else falls back to the default (watch the GUI's active-profile pill or `busctl get-property ... ActiveProfile`).
    4. On Wayland: confirm behavior matches the SPIKE.md degradation matrix for your compositor (works on wlroots/KWin-with-script; on stock GNOME-Wayland confirm the documented "degraded, manual profiles only" message rather than a crash).
    5. Export a profile: App Rules tab → Export → choose a path → confirm a file is written. Import it back (or on another profile) → confirm the settings apply and a bad/edited file shows the validation error instead of applying.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what did not switch / failed to import.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GUI → app-profiles.json | The GUI writes the rule file the non-root helper consumes (same user, user-owned path) |
| imported file → GUI apply | Imported profile validated by ProfileIO before any .Device setter call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | imported profile applied to device | mitigate | ProfileSharing.importProfile validates the envelope (ProfileIO throws on bad version) and surfaces lastError; only applies on a clean parse |
| T-05-04-02 | DoS | malformed rule pattern | mitigate | Patterns are plain substrings (no regex eval in the engine) so a hostile pattern cannot cause catastrophic backtracking |
</threat_model>

<verification>
- GUI builds with the App Rules tab registered; app_rules_model persistence test GREEN.
- app-profiles.json written by the GUI matches the helper's RuleStore schema.
- UAT confirms focus-following + export/import on the operator's real session.
</verification>

<success_criteria>
The user can manage window-class/executable → profile rules and a default profile in the
GUI (PROF-02/03), the helper acts on them live, and the user can export/import profiles as
files (PROF-04) — verified end-to-end on hardware.
</success_criteria>

<output>
After completion, create `.planning/phases/05-per-application-profiles-profile-sharing/05-04-SUMMARY.md`
</output>
