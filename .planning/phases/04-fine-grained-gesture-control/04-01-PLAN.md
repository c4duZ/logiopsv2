---
phase: 04-fine-grained-gesture-control
plan: 01
type: execute
wave: 1
depends_on: [00]
files_modified:
  - src/logid/Configuration.h
  - src/logid/actions/gesture/IntervalGesture.cpp
  - src/logid/actions/gesture/AxisGesture.cpp
  - src/logid/actions/GestureAction.cpp
autonomous: true
requirements: [GEST-02, GEST-03]
user_setup: []

must_haves:
  truths:
    - "An OnInterval gesture fires exactly once per logical step at the default granularity (no volume +2, no multi-desktop overshoot)"
    - "An OnInterval gesture with interval unset still repeats sensibly via a new defaults::gesture_interval"
    - "SetGesture cannot crash the root daemon on untrusted direction/type or a nullopt gestures map"
  artifacts:
    - path: "src/logid/Configuration.h"
      provides: "defaults::gesture_interval constant"
      contains: "gesture_interval"
    - path: "src/logid/actions/gesture/IntervalGesture.cpp"
      provides: "interval accounting fix + SetThreshold arg-name fix + interval default"
      contains: "gesture_interval"
    - path: "src/logid/actions/GestureAction.cpp"
      provides: "hardened setGesture (lazy-init gestures map, allowlist-safe)"
      contains: "has_value"
  key_links:
    - from: "src/logid/actions/gesture/IntervalGesture.cpp"
      to: "src/logid/Configuration.h"
      via: "value_or(defaults::gesture_interval)"
      pattern: "gesture_interval"
    - from: "tests/phase4/gesture_math_test.cpp"
      to: "src/logid/actions/gesture/IntervalGesture.cpp"
      via: "phase4_gesture_math turns GREEN"
      pattern: "phase4_gesture_math"
---

<objective>
The phase's "first daemon extension" is, per RESEARCH, mostly already present — `GestureAction::setGesture` already does the live mode rebuild. This plan does the real daemon work that remains: (a) FIX the GEST-02/03 granularity defect in `IntervalGesture` (missing interval default → never fires; integer-division overshoot → "volume +2" / multi-desktop), (b) define `defaults::gesture_interval` so the GUI slider's leftmost stop maps to "one flick = one step", and (c) HARDEN `setGesture` against untrusted `direction`/`type` strings and a `nullopt` gestures map so a group-`logiops` client cannot crash the root daemon. This turns `phase4_gesture_math` GREEN.

Purpose: deliver the GEST-02/03 behavior at full fidelity (not a tuning knob) and close the root-daemon DoS surface RESEARCH and CONCERNS flag.
Output: corrected daemon gesture engine; `ctest -R phase4_gesture_math` GREEN; daemon builds -Werror clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fine-grained-gesture-control/04-RESEARCH.md

<interfaces>
Daemon gesture engine (exact current source):

src/logid/Configuration.h (namespace defaults):
  static constexpr int gesture_threshold = 50;   // NO gesture_interval exists yet — ADD one

src/logid/actions/gesture/IntervalGesture.cpp:
  ctor registers {"SetThreshold", {this,&setThreshold,{"interval"}}}  // ARG-NAME BUG: should be {"threshold"}
  void move(int16_t axis):
    if (!_config.interval.has_value()) return;   // GEST-03 bug: with interval unset, NEVER fires
    threshold = _config.threshold.value_or(defaults::gesture_threshold);
    _axis += axis; if (_axis < threshold) return;
    new_interval_count = (_axis - threshold) / _config.interval.value();
    if (new_interval_count > _interval_pass_count) { _action->press(); _action->release(); }
    _interval_pass_count = new_interval_count;
  // Firing only on count INCREASE is correct (one fire per boundary). Overshoot is a VALUE
  // problem (interval too small) — fix = sane default + GUI slider mapping, not a logic rewrite.

src/logid/actions/gesture/AxisGesture.cpp:
  line ~49: logPrintf(WARN, "Invalid axis %s.");   // MISSING the %s arg (harmless; fix while nearby, CONCERNS #5)
  _axis_remainder carry at lines 99-104 is CORRECT — do not change the algorithm.

src/logid/actions/GestureAction.cpp:
  void setGesture(const std::string& direction, const std::string& type):
    Direction d = toDirection(direction);                 // throws std::invalid_argument on bad dir
    auto& gesture = _config.gestures.value()[dir_name];   // throws std::bad_optional_access if nullopt (Pitfall 3)
    makeGesture(_device, type, gesture, ...);             // throws InvalidGesture on bad type -> caught, rethrows std::invalid_argument
  toDirection allowlist == {up,down,left,right,none}; makeGesture(type) allowlist == {Axis,OnInterval,OnRelease,OnThreshold,None}.

NOTE (Pitfall 1, confirmed in source): Gesture::makeGesture(type) branches on interface_name; BOTH ReleaseGesture
and ThresholdGesture declare interface_name=="OnRelease", and the ReleaseGesture branch is checked FIRST, so
type=="OnThreshold" never constructs ThresholdGesture (it falls through to the OnRelease config). The GUI (Plan 02)
canonicalizes mode 2 to "OnRelease" — so this plan does NOT need to rename interface_names. Leave the collision
as-is but VERIFY the rethrow path surfaces as a clean D-Bus error (no daemon abort) for an unknown type.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix IntervalGesture granularity defect + add defaults::gesture_interval (GEST-02/03)</name>
  <read_first>src/logid/actions/gesture/IntervalGesture.cpp (move accounting + the no-interval early-return + the SetThreshold {"interval"} arg-name bug), src/logid/Configuration.h (defaults block), tests/phase4/gesture_math_test.cpp (the contract this must satisfy)</read_first>
  <files>src/logid/Configuration.h, src/logid/actions/gesture/IntervalGesture.cpp</files>
  <behavior>
    - With interval unset, an OnInterval gesture repeats using defaults::gesture_interval rather than never firing.
    - One continuous motion of M units past threshold fires exactly floor(M/interval) times.
    - SetThreshold advertises its argument as "threshold", not "interval".
  </behavior>
  <action>
1. In `src/logid/Configuration.h`, add to the `defaults` namespace: `static constexpr int gesture_interval = 50;` (next to `gesture_threshold = 50`). Add a comment: "leftmost granularity-slider stop; tuned on-hardware (MX Master 4) by UAT — one flick ≈ one step". This is the sane default GEST-02/03 require; the exact value is Claude's-discretion per CONTEXT and confirmed by hardware UAT.
2. In `IntervalGesture::move`, replace `if (!_config.interval.has_value()) return;` so a missing interval falls back to the default: use `const int interval = _config.interval.value_or(defaults::gesture_interval);` and guard only against `interval <= 0` (return early to avoid divide-by-zero). Use that local `interval` in the `(_axis - threshold) / interval` computation. This fixes the GEST-03 "repeat does nothing" path while preserving the correct fire-on-increase accounting (do NOT change the press()/_interval_pass_count logic — it already fires exactly once per boundary).
3. Fix the `SetThreshold` registration arg-name bug: change `{"SetThreshold", {this, &IntervalGesture::setThreshold, {"interval"}}}` to `{"threshold"}` so introspection (which the GUI reads) advertises the correct arg name.
4. Leave `setInterval(0)` resetting the optional (sentinel) — but since `move` now defaults, an unset interval no longer means "dead", it means "use default". Verify `getConfig` still returns `interval.value_or(0)` for readback (the GUI distinguishes "unset" via 0).
  </action>
  <verify>
    <automated>cd /home/c4duz/Downloads/logiops && cmake --build build --target phase4_gesture_math_test >/dev/null 2>&1 && ctest --test-dir build -R phase4_gesture_math --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
- `grep -q "gesture_interval" src/logid/Configuration.h` succeeds.
- `grep -q "value_or(defaults::gesture_interval)" src/logid/actions/gesture/IntervalGesture.cpp` succeeds.
- `IntervalGesture.cpp` `SetThreshold` registration contains `{"threshold"}` (not `{"interval"}`).
- `ctest --test-dir build -R phase4_gesture_math` exits 0 (the Wave-0 RED math unit is now GREEN: one flick = one fire, M=360/I=120 = 3 fires).
  </acceptance_criteria>
  <done>defaults::gesture_interval exists; interval accounting defaults sensibly and never divides by zero; phase4_gesture_math GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Harden GestureAction::setGesture against untrusted input + fix AxisGesture log arg</name>
  <read_first>src/logid/actions/GestureAction.cpp (setGesture: toDirection throw path, _config.gestures.value() bad_optional_access at Pitfall 3, the makeGesture try/catch rethrow), src/logid/actions/gesture/AxisGesture.cpp line ~49 (logPrintf missing arg), .planning/phases/04-fine-grained-gesture-control/04-RESEARCH.md §Security Domain, CLAUDE.md (CONCERNS #5 format-string, bare throw rule)</read_first>
  <files>src/logid/actions/GestureAction.cpp, src/logid/actions/gesture/AxisGesture.cpp</files>
  <action>
1. HARDEN `setGesture(direction, type)` against the `bad_optional_access` DoS (Pitfall 3): before `_config.gestures.value()[dir_name]`, lazily initialize the optional map if it is `nullopt` — `if (!_config.gestures.has_value()) _config.gestures.emplace();`. This guarantees the two-step `Button.SetAction("Gesture")` → `Action.Gesture.SetGesture(dir, mode)` sequence cannot throw `std::bad_optional_access` and abort the root daemon.
2. Untrusted `direction`: `toDirection(direction)` already throws `std::invalid_argument` for anything outside {up,down,left,right,none}. Confirm (and add an inline comment) that this propagates as a D-Bus error via ipcgull rather than aborting — do NOT swallow it; an invalid direction must be rejected, not silently mapped. Do NOT call std::stoi/atoi anywhere in this path (none currently — keep it that way).
3. Untrusted `type`: the existing `catch (InvalidGesture&)` restores the prior gesture and rethrows `std::invalid_argument("Invalid gesture type")`. Keep this. Ensure the restore path uses `makeGesture(_device, gesture, ...)` (config-driven, not type-driven) so an unknown type leaves the node on a VALID interface. Verify no untrusted string is ever passed as a `logPrintf` FORMAT argument (CONCERNS #5) — keep any logging as `logPrintf(WARN, "%s ...", x.c_str())`.
4. While nearby, fix the `AxisGesture.cpp` line ~49 `logPrintf(WARN, "Invalid axis %s.")` missing its argument: pass the axis string as a `%s` arg (`logPrintf(WARN, "Invalid axis %s.", axis.c_str())`). Harmless today but it is a format/arg mismatch flagged by RESEARCH.
5. Keep the `None`-direction `threshold = 0` special-case intact.
  </action>
  <verify>
    <automated>cd /home/c4duz/Downloads/logiops && cmake --build build --target logid 2>&1 | grep -Eiq "error:|warning:" && echo "BUILD HAS WARN/ERR" || echo "CLEAN"</automated>
  </verify>
  <acceptance_criteria>
- `grep -q "gestures.emplace\|gestures.has_value" src/logid/actions/GestureAction.cpp` succeeds (lazy-init guard present).
- `AxisGesture.cpp` line ~49 `logPrintf` now passes the axis string as a `%s` argument (grep `"Invalid axis %s.", `).
- No `std::stoi`/`atoi` introduced in the gesture path.
- `cmake --build build --target logid` compiles -Werror clean (the daemon CI uses -Werror).
  </acceptance_criteria>
  <done>setGesture lazily inits the gestures map (no bad_optional_access); bad direction/type rejected as D-Bus errors not aborts; no format-string hazard; daemon builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| group-`logiops` D-Bus client → root daemon | `Action.Gesture.SetGesture(direction, type)` and the per-mode `Set*` setters take untrusted strings/ints from any logiops-group client (bus relaxed in Phase 1). The daemon parses them as root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | DoS | GestureAction::setGesture (`_config.gestures.value()`) | mitigate | Lazy-init the optional map (`if (!gestures.has_value()) gestures.emplace();`) so a SetGesture before any gesture exists cannot throw bad_optional_access and abort the root daemon (Pitfall 3). |
| T-04-01-02 | DoS | GestureAction::setGesture (`toDirection`) | mitigate | Allowlist {up,down,left,right,none}; toDirection throws std::invalid_argument on anything else, surfaced as a D-Bus error via ipcgull — never an abort. No std::stoi on untrusted input. |
| T-04-01-03 | DoS | Gesture::makeGesture(type) | mitigate | Unknown type throws InvalidGesture; setGesture catches it, restores the prior valid gesture via the config-driven makeGesture, and rethrows std::invalid_argument as a clean D-Bus error. Node never left without a valid interface. |
| T-04-01-04 | DoS | IntervalGesture::move (divide) | mitigate | Guard `interval <= 0` before the division; default to defaults::gesture_interval so an unset/zero interval cannot divide-by-zero or silently dead-fire. |
| T-04-01-05 | Information disclosure / Tampering | logPrintf format strings | mitigate | All gesture-path logging keeps untrusted strings as `%s` args, never as the format (CONCERNS #5); fix the AxisGesture missing-arg mismatch while nearby. |
| T-04-01-06 | DoS | unbounded interval/threshold/multiplier | accept (this plan) | The integers arrive TYPED over D-Bus (no std::stoi); negative/huge values only mis-tune a gesture, not crash. Clamping is a GUI-side concern (Plan 02 validates before dispatch); daemon keeps the setThreshold(0) reset sentinel. |
</threat_model>

<verification>
- `ctest --test-dir build -R phase4_gesture_math --output-on-failure` GREEN (Wave-0 RED math unit now passes).
- `cmake --build build --target logid` compiles -Werror clean.
- Manual UAT (deferred to phase gate, per VALIDATION.md): on the MX Master 4, confirm one flick = exactly one volume tick / one desktop at the slider's leftmost stop, and that "Repeat while moving" switches multiple desktops in one continuous motion (GEST-02/03 feel-test).
</verification>

<success_criteria>
- `defaults::gesture_interval` added; IntervalGesture defaults + divide-guard in place; SetThreshold arg-name corrected.
- setGesture lazily inits the gestures map; bad direction/type rejected as D-Bus errors; no format-string hazard.
- phase4_gesture_math GREEN; daemon -Werror clean.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fine-grained-gesture-control/04-01-SUMMARY.md`
</output>
