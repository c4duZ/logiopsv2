---
phase: 04-fine-grained-gesture-control
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - CMakeLists.txt
  - tests/phase4/CMakeLists.txt
  - tests/phase4/phase4_log_stub.cpp
  - tests/phase4/gesture_math_test.cpp
  - tests/phase4/GestureModelTest.cpp
autonomous: true
requirements: [GEST-01, GEST-02, GEST-03, GEST-04]
user_setup: []

must_haves:
  truths:
    - "ctest -R phase4 discovers and runs the phase4 unit targets"
    - "gesture_math_test RED-asserts the GEST-02/03 fire-count-per-motion contract (fails until Plan 01 fixes the daemon)"
    - "GestureModelTest is gated so a Qt-less build still builds the daemon math unit"
  artifacts:
    - path: "tests/phase4/CMakeLists.txt"
      provides: "phase4 CTest registration mirroring tests/phase3"
      contains: "add_test(NAME phase4_gesture_math"
    - path: "tests/phase4/gesture_math_test.cpp"
      provides: "RED daemon-side interval/axis accounting fixtures (GEST-02/03)"
      min_lines: 40
    - path: "tests/phase4/GestureModelTest.cpp"
      provides: "RED recording-subclass model test scaffold (GEST-01/04)"
      min_lines: 30
  key_links:
    - from: "CMakeLists.txt"
      to: "tests/phase4"
      via: "add_subdirectory(tests/phase4)"
      pattern: "add_subdirectory\\(tests/phase4\\)"
    - from: "tests/phase4/gesture_math_test.cpp"
      to: "src/logid/actions/gesture/IntervalGesture.cpp"
      via: "encodes IntervalGesture::move accounting contract"
      pattern: "floor"
---

<objective>
Stand up the Phase 4 test infrastructure under `tests/phase4/`, mirroring `tests/phase3/`, BEFORE any feature work. This is the Wave 0 Nyquist gate: it registers the `phase4` CTest prefix and lands two RED test units — `gesture_math_test` (daemon-side interval/axis accounting, the GEST-02/03 bug harness) and `GestureModelTest` (GUI recording-subclass scaffold for the two-step mode-switch + preview sentence, GEST-01/04).

Purpose: every later Phase 4 task verifies against `ctest --test-dir build -R phase4`. The math unit is intentionally RED here and turns GREEN in Plan 01; the model unit's scaffold compiles against the `GestureModel` skeleton created in Plan 02.
Output: `tests/phase4/` test target wired into the root build; `ctest -R phase4` runnable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fine-grained-gesture-control/04-VALIDATION.md

<interfaces>
Reference test harness — tests/phase3/CMakeLists.txt (mirror its structure exactly):
- Daemon-side units compile config/config.cpp + util/log.cpp + a local log stub TU directly, link `ipcgull_static config++`, set CXX_STANDARD 20.
- GUI-side units are guarded by: `if(TARGET logiops-gui-lib AND EXISTS .../FooTest.cpp)` then `find_package(Qt6 6.4 COMPONENTS Test QUIET)` then link `logiops-gui-lib Qt6::Test`.
- Scratch tmp dir passed as `BUILD_TMP_DIR="..."` compile definition.
- `phase3_log_stub.cpp` exists to satisfy logPrintf linkage when not pulling the whole daemon.

Daemon gesture math under test (from src/logid/actions/gesture/IntervalGesture.cpp):
  void IntervalGesture::move(int16_t axis):
    if (!_config.interval.has_value()) return;            // GEST-03 bug: no default -> never fires
    threshold = _config.threshold.value_or(defaults::gesture_threshold==50);
    _axis += axis; if (_axis < threshold) return;
    new_interval_count = (_axis - threshold) / interval;  // fires when count INCREASES
    if (new_interval_count > _interval_pass_count) { _action->press(); _action->release(); }
  Fire-count over a single continuous motion of M units past threshold == floor(M/interval) action fires.

Root CMakeLists.txt currently has `add_subdirectory(tests/phase3)` at line 80 (inside the BUILD_TESTING / tests guard). Add `add_subdirectory(tests/phase4)` next to it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register tests/phase4 CTest target mirroring tests/phase3</name>
  <read_first>tests/phase3/CMakeLists.txt (copy structure verbatim), tests/phase3/phase3_log_stub.cpp, CMakeLists.txt line ~80 (the add_subdirectory tests/phase3 site and its enclosing test guard)</read_first>
  <files>CMakeLists.txt, tests/phase4/CMakeLists.txt, tests/phase4/phase4_log_stub.cpp</files>
  <action>
Create `tests/phase4/CMakeLists.txt` mirroring `tests/phase3/CMakeLists.txt`:
- Define `PHASE4_TMP_DIR "${CMAKE_CURRENT_BINARY_DIR}"`.
- Daemon-side `phase4_gesture_math_test` executable from `gesture_math_test.cpp`. Include dirs `${CMAKE_SOURCE_DIR}/src/logid` and `${CMAKE_SOURCE_DIR}/src/ipcgull/src/include`. CXX_STANDARD 20 / REQUIRED ON. Prefer a SELF-CONTAINED math unit (the test file reimplements only the boundary-crossing accounting under test) so it does not need to link Action::makeAction / InputDevice / Device. If you instead compile the real `IntervalGesture.cpp`/`AxisGesture.cpp` TUs, add `util/log.cpp` + `phase4_log_stub.cpp` and link `ipcgull_static config++`; document the choice in a header comment. The unit MUST be daemon-side, no Qt, no bus.
- GUI-side `phase4_gesture_model_test`: guard exactly like phase3 — `if(TARGET logiops-gui-lib AND EXISTS ${CMAKE_CURRENT_SOURCE_DIR}/GestureModelTest.cpp)` then `find_package(Qt6 6.4 COMPONENTS Test QUIET)`; on found, `set(CMAKE_AUTOMOC ON)`, add_executable from `GestureModelTest.cpp`, link `logiops-gui-lib Qt6::Test`, CXX_STANDARD 20, `add_test(NAME phase4_gesture_model COMMAND phase4_gesture_model_test)`.
- `add_test(NAME phase4_gesture_math COMMAND phase4_gesture_math_test)`.
Create `tests/phase4/phase4_log_stub.cpp` (copy `tests/phase3/phase3_log_stub.cpp`).
Edit root `CMakeLists.txt`: add `add_subdirectory(tests/phase4)` immediately after the existing `add_subdirectory(tests/phase3)` (line ~80), inside the same enclosing test guard.
  </action>
  <verify>
    <automated>cmake -S /home/c4duz/Downloads/logiops -B /home/c4duz/Downloads/logiops/build -DBUILD_TESTING=ON >/dev/null 2>&1 && grep -q "add_subdirectory(tests/phase4)" /home/c4duz/Downloads/logiops/CMakeLists.txt && echo OK</automated>
  </verify>
  <acceptance_criteria>
- `grep -q "add_subdirectory(tests/phase4)" CMakeLists.txt` succeeds.
- `tests/phase4/CMakeLists.txt` contains the literal `add_test(NAME phase4_gesture_math` and `add_test(NAME phase4_gesture_model`.
- `tests/phase4/CMakeLists.txt` contains `find_package(Qt6 6.4 COMPONENTS Test QUIET)` and the `if(TARGET logiops-gui-lib AND EXISTS` guard.
- CMake configure exits 0.
  </acceptance_criteria>
  <done>tests/phase4 is registered; cmake configure succeeds; both add_test lines present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED gesture_math_test asserting the GEST-02/03 accounting contract</name>
  <read_first>src/logid/actions/gesture/IntervalGesture.cpp (the move() accounting + the no-interval-no-default GEST-03 bug), src/logid/actions/gesture/AxisGesture.cpp (the _axis_remainder carry, the correct reference), src/logid/Configuration.h (defaults::gesture_threshold == 50, NO gesture_interval default)</read_first>
  <files>tests/phase4/gesture_math_test.cpp</files>
  <behavior>
    - A single continuous motion of M units past threshold with interval I fires the action exactly floor(M/I) times. Assert M=120,I=120 -> exactly 1 fire (one desktop per flick); M=360,I=120 -> exactly 3 fires (GEST-03 multi-desktop).
    - An OnInterval gesture with interval UNSET must still repeat at a sane default (the contract Plan 01's `defaults::gesture_interval` must satisfy) — RED now.
    - The axis remainder (move - floor(move)) is carried across successive moves; no sub-unit motion is lost and the axis path does not overshoot.
  </behavior>
  <action>
Write a daemon-side C++ unit (no Qt, no bus, plain `int main()` returning non-zero on failure) encoding the GEST-02/03 acceptance contract as assertions:
1. INTERVAL FIRE-COUNT: drive the `(axis-threshold)/interval` boundary-crossing accounting, firing only on count INCREASE; assert M=120,I=120 -> 1 fire and M=360,I=120 -> 3 fires.
2. NO-DEFAULT-INTERVAL REGRESSION (GEST-03): assert that with interval unset the gesture still repeats at an intended default. To keep the file compiling RED-by-value rather than RED-by-link, assert against a local constant equal to the intended default with a `// EXPECT after Plan 01 introduces defaults::gesture_interval` TODO.
3. AXIS REMAINDER CARRY: assert the `move - floor(move)` remainder is carried across successive moves and the axis path does not overshoot.
Keep the unit self-contained if linking the real TUs is infeasible; the assertions still encode the exact contract Plan 01 must satisfy. Add a top comment: "RED until Plan 01 fixes IntervalGesture default + accounting (GEST-02/03)."
  </action>
  <verify>
    <automated>test -f /home/c4duz/Downloads/logiops/tests/phase4/gesture_math_test.cpp && grep -q "floor" /home/c4duz/Downloads/logiops/tests/phase4/gesture_math_test.cpp && grep -q "120" /home/c4duz/Downloads/logiops/tests/phase4/gesture_math_test.cpp && echo OK</automated>
  </verify>
  <acceptance_criteria>
- `tests/phase4/gesture_math_test.cpp` exists, >= 40 lines.
- File asserts both a single-flick (exactly 1 fire) and a multi-step (exactly 3 fires) interval case (grep for `120` and `360`).
- File contains the comment string "RED until Plan 01".
- `ctest --test-dir build -R phase4_gesture_math` runs (it MAY be RED — expected and acceptable for this Wave 0 plan).
  </acceptance_criteria>
  <done>gesture_math_test.cpp encodes the one-flick-one-step + multi-step contract; builds and runs (RED is acceptable).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED GestureModelTest recording-subclass scaffold (GEST-01/04)</name>
  <read_first>tests/phase3/ButtonsModelTest.cpp (the recording-subclass / no-bus pattern), src/logiops-gui/ButtonsModel.h (the performSetAction/performParamCall virtual seam, seedButtons test ctor)</read_first>
  <files>tests/phase4/GestureModelTest.cpp</files>
  <behavior>
    - Setting a direction's mode records SetGesture(direction, type) FIRST, then any per-mode param call (e.g. SetInterval) SECOND.
    - Plain-language modes map to exactly: "Repeat while moving"->OnInterval, "Do once when moved far enough"->OnRelease, "Adjust proportionally"->Axis, "Nothing"->None. An unknown mode or direction is rejected with NO recorded dispatch.
    - previewSentence recomposes per {direction, mode, action, granularity} per UI-SPEC templates (e.g. Nothing + up -> "Moving up does nothing.") and previewChanged NOTIFY fires.
  </behavior>
  <action>
Write a QtTest unit (mirroring `ButtonsModelTest.cpp`) defining a `RecordingGestureModel` subclass of the (Plan-02) `GestureModel`, overriding the two-step dispatch virtuals to record (direction, type, method, args) WITHOUT a bus. Assert:
1. TWO-STEP ORDER: a mode set records `SetGesture(direction, type)` first, then the per-mode param call second.
2. MODE ALLOWLIST: the four plain-language modes map to `OnInterval`/`OnRelease`/`Axis`/`None` respectively; an unknown mode/direction is rejected before any dispatch (no recorded call).
3. PREVIEW SENTENCE (GEST-04): `previewSentence` recomposes per the UI-SPEC §Copywriting templates (Nothing+up -> "Moving up does nothing."); previewChanged NOTIFY fires on change.
Because `GestureModel` does not exist until Plan 02, this file is RED (won't link) now — intended Wave-0 state; the CMake `EXISTS GestureModelTest.cpp` guard keeps the daemon math unit buildable regardless. Add a top comment naming the Plan 02 symbols it expects (`GestureModel`, `setMode`, `previewSentence`, `performSetGesture`, `performParamCall`).
  </action>
  <verify>
    <automated>test -f /home/c4duz/Downloads/logiops/tests/phase4/GestureModelTest.cpp && grep -q "OnInterval" /home/c4duz/Downloads/logiops/tests/phase4/GestureModelTest.cpp && grep -q "previewSentence" /home/c4duz/Downloads/logiops/tests/phase4/GestureModelTest.cpp && echo OK</automated>
  </verify>
  <acceptance_criteria>
- `tests/phase4/GestureModelTest.cpp` exists, >= 30 lines.
- File references `OnInterval`, `OnRelease`, `Axis`, `None` (the type allowlist) and `previewSentence`.
- File defines a `RecordingGestureModel` subclass overriding the dispatch virtuals.
- File compiles RED now (acceptable); becomes GREEN after Plan 02.
  </acceptance_criteria>
  <done>GestureModelTest.cpp scaffolds the two-step order, mode allowlist, and preview-sentence assertions against the Plan 02 model contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new in Wave 0) | Tests run locally, no bus, no untrusted input. The test code itself encodes the allowlist/clamp contracts that Plans 01/02 must enforce. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Tampering | test fixtures touching /etc | mitigate | Daemon-side unit never opens /etc/logid.cfg; uses BUILD_TMP_DIR scratch only (mirror phase3). |
| T-04-00-02 | DoS | n/a | accept | Wave 0 is pure local test scaffolding; no runtime attack surface introduced. |
</threat_model>

<verification>
- `cmake -S . -B build -DBUILD_TESTING=ON` configures cleanly with `tests/phase4` registered.
- `ctest --test-dir build -R phase4 --output-on-failure` discovers `phase4_gesture_math` (and `phase4_gesture_model` when Qt is present). RED on the math unit is the expected Wave-0 state.
</verification>

<success_criteria>
- `add_subdirectory(tests/phase4)` present in root CMakeLists.txt.
- `tests/phase4/CMakeLists.txt`, `gesture_math_test.cpp`, `GestureModelTest.cpp`, `phase4_log_stub.cpp` exist.
- `ctest -R phase4` runs; math unit encodes the one-flick-one-step + multi-step contract (RED accepted).
</success_criteria>

<output>
After completion, create `.planning/phases/04-fine-grained-gesture-control/04-00-SUMMARY.md`
</output>
