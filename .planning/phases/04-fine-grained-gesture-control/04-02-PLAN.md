---
phase: 04-fine-grained-gesture-control
plan: 02
type: execute
wave: 1
depends_on: [00]
files_modified:
  - src/logiops-gui/GestureModel.h
  - src/logiops-gui/GestureModel.cpp
  - src/logiops-gui/dbus/logid_action_gesture.xml
  - src/logiops-gui/CMakeLists.txt
autonomous: true
requirements: [GEST-01, GEST-02, GEST-03, GEST-04]
user_setup: []

must_haves:
  truths:
    - "A test can drive a 4-direction gesture builder via public model methods with no live bus"
    - "Setting a mode dispatches SetGesture(direction,type) FIRST then the per-mode param setter SECOND"
    - "The mode→daemon-type mapping is an allowlist: only OnInterval/OnRelease/Axis/None are ever sent; unknown direction/mode is rejected before dispatch"
    - "previewSentence is composed in C++ and recomposes on every {direction,mode,action,granularity} change (GEST-04)"
  artifacts:
    - path: "src/logiops-gui/GestureModel.h"
      provides: "button-scoped 4-direction gesture model with two-step dispatch seam + NOTIFYable previewSentence"
      contains: "previewSentence"
    - path: "src/logiops-gui/GestureModel.cpp"
      provides: "mode allowlist, two-step dispatch, preview composition, granularity↔human readout"
      min_lines: 120
    - path: "src/logiops-gui/dbus/logid_action_gesture.xml"
      provides: "typed proxy for the stable Action.Gesture interface (SetGesture)"
      contains: "SetGesture"
  key_links:
    - from: "src/logiops-gui/CMakeLists.txt"
      to: "src/logiops-gui/dbus/logid_action_gesture.xml"
      via: "qt_add_dbus_interface"
      pattern: "logid_action_gesture"
    - from: "tests/phase4/GestureModelTest.cpp"
      to: "src/logiops-gui/GestureModel.cpp"
      via: "phase4_gesture_model turns GREEN"
      pattern: "phase4_gesture_model"
---

<objective>
Build the GUI brain of the gesture builder: a button-scoped `GestureModel` (the 4 cardinal directions of ONE selected button) that mirrors `ButtonsModel`'s two-step async dispatch, drives the daemon's already-existing `Action.Gesture.SetGesture(direction, type)` mode-switch, sequences the per-mode `.Gesture.<mode>.Set*` param setters, and composes the plain-language preview sentence (GEST-04) and granularity human-readout in C++ — zero business logic in QML. This turns `phase4_gesture_model` GREEN.

This plan has NO file overlap with Plan 01 (daemon), so it runs in parallel.

Purpose: deliver the full interface-first contract the QML (Plan 03) renders against — direction selection, mode allowlist, action, granularity, preview — all unit-testable with injected fakes per VALIDATION.md.
Output: `GestureModel.{h,cpp}` + typed `Action.Gesture` proxy + CMake wiring; `ctest -R phase4_gesture_model` GREEN; GUI lib -Werror clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-fine-grained-gesture-control/04-RESEARCH.md
@.planning/phases/04-fine-grained-gesture-control/04-UI-SPEC.md

<interfaces>
MIRROR — src/logiops-gui/ButtonsModel.h / .cpp (the two-step pattern to copy):
  - protected virtual performSetAction(int row, QString type)  -> issues Button.SetAction(type) async
  - protected virtual performParamCall(int row, QString type, QString method, QVariantList args)
       -> waits for step 1 to land (Introspect/GetAll hop on the same ordered connection), then
          QDBusInterface("pizza.pixl.LogiOps.Action."+type).asyncCallWithArgumentList(method,args)
  - Test ctor (no bus) + live ctor(devicePath, bus); seedButtons() injects rows; _configState->markDirty() on apply.
  - kService == "pizza.pixl.LogiOps".

DAEMON SURFACE (exact, confirmed in source):
  Stable parent interface: pizza.pixl.LogiOps.Action.Gesture  on the BUTTON node
     method SetGesture(string direction, string type)   // direction∈{up,down,left,right,none}; type=mode
  Per-direction child nodes: .../buttons/M/gestures/{up,down,left,right,none}
     each publishes exactly ONE interface pizza.pixl.LogiOps.Gesture.<mode>:
       Axis      : SetThreshold(int), SetMultiplier(double), SetAxis(string), GetConfig
       OnInterval: SetInterval(int), SetThreshold(int), SetAction(string), GetConfig
       OnRelease : SetThreshold(int), SetAction(string), GetThreshold
       None      : (no setters)

MODE ALLOWLIST — the ONLY type strings the GUI may send (confirmed against Gesture::makeGesture + NullGesture::interface_name=="None"):
   "Repeat while moving"            -> "OnInterval"
   "Do once when moved far enough"  -> "OnRelease"   // canonical; "OnThreshold" collides->InvalidGesture, NEVER send it
   "Adjust proportionally"          -> "Axis"
   "Nothing"                        -> "None"         // NOT "NoPress" — makeGesture branches on interface_name "None"
DIRECTION ALLOWLIST: {"up","down","left","right"} (4 cardinal; "none" exists in daemon but UI surfaces only the 4).

PREVIEW TEMPLATES (UI-SPEC §Copywriting — compose in C++):
  Nothing:                "Moving {direction} does nothing."
  Repeat while moving:    "Moving {direction} keeps {action} as long as you keep moving — about one step per {amount}."
  Do once:                "Moving {direction} far enough {action} once. A single flick does one step."
  Adjust proportionally:  "Moving {direction} {action} in proportion to how far you move."
  {amount} from granularity: e.g. "small flick" (left stop) … "long swipe" (right stop). {action} = plain verb phrase.

GUI build wiring — src/logiops-gui/CMakeLists.txt:
  - qt_add_dbus_interface(GUI_DBUS_SRCS dbus/<name>.xml <basename>_proxy) generates the typed class.
  - GestureModel.cpp goes into target_sources(logiops-gui-lib PRIVATE ...).
  - XML comments must contain NO `--` and NO bare `<` (Phase 2 lesson — breaks qdbusxml2cpp into empty proxies).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Typed Action.Gesture proxy XML + CMake wiring</name>
  <read_first>src/logiops-gui/dbus/logid_button.xml (trimmed-introspection XML format + the no-`--`/no-`<`-in-comments rule), src/logiops-gui/CMakeLists.txt lines 34-71 (the qt_add_dbus_interface block + target_sources(logiops-gui-lib))</read_first>
  <files>src/logiops-gui/dbus/logid_action_gesture.xml, src/logiops-gui/CMakeLists.txt</files>
  <action>
1. Create `src/logiops-gui/dbus/logid_action_gesture.xml`: a trimmed introspection XML declaring interface `pizza.pixl.LogiOps.Action.Gesture` with the single stable method `SetGesture` (two `in` string args: `direction`, `type`). Keep any comments free of `--` and bare `<`. This generates `PizzaPixlLogiOpsActionGestureInterface`.
2. In `src/logiops-gui/CMakeLists.txt`, add after the Phase 3 proxy block (line ~48): `qt_add_dbus_interface(GUI_DBUS_SRCS dbus/logid_action_gesture.xml logid_action_gesture_proxy)`.
3. Add `${CMAKE_CURRENT_SOURCE_DIR}/GestureModel.cpp` to `target_sources(logiops-gui-lib PRIVATE ...)` (the existing list, after ConfigState.cpp).
DO NOT add a second XML for any `.Gesture.<mode>` interface — those are dynamic and driven via generic QDBusInterface (mirror ButtonsModel::performParamCall). Only the stable `Action.Gesture` gets a typed proxy.
  </action>
  <verify>
    <automated>cd /home/c4duz/Downloads/logiops && grep -q "logid_action_gesture" src/logiops-gui/CMakeLists.txt && grep -q "GestureModel.cpp" src/logiops-gui/CMakeLists.txt && grep -q "SetGesture" src/logiops-gui/dbus/logid_action_gesture.xml && echo OK</automated>
  </verify>
  <acceptance_criteria>
- `src/logiops-gui/dbus/logid_action_gesture.xml` exists, declares interface name `pizza.pixl.LogiOps.Action.Gesture` and method `SetGesture` with `direction`+`type` string in-args.
- XML contains no `--` sequence and no bare `<` inside comments.
- `CMakeLists.txt` contains `qt_add_dbus_interface(GUI_DBUS_SRCS dbus/logid_action_gesture.xml logid_action_gesture_proxy)` and `GestureModel.cpp` in target_sources.
  </acceptance_criteria>
  <done>Typed Action.Gesture proxy generates; GestureModel.cpp is in the gui lib build.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GestureModel — 4-direction model, two-step dispatch seam, mode allowlist</name>
  <read_first>src/logiops-gui/ButtonsModel.cpp lines 230-306 (performSetAction/performParamCall two-step + applyCurrentAction/markDirty), src/logiops-gui/ButtonsModel.h (the virtual seam + test ctor + ConfigState injection), tests/phase4/GestureModelTest.cpp (the exact contract: order, allowlist, rejection)</read_first>
  <files>src/logiops-gui/GestureModel.h, src/logiops-gui/GestureModel.cpp</files>
  <behavior>
    - setMode(direction, plainMode) dispatches performSetGesture(direction, type) FIRST, then any per-mode param call SECOND.
    - The four plain-language modes map to exactly OnInterval/OnRelease/Axis/None; an unknown plain mode or a direction outside {up,down,left,right} is rejected with NO dispatch.
    - Every successful edit calls ConfigState::markDirty() (when wired).
  </behavior>
  <action>
Create `GestureModel` as a button-scoped QObject (NOT a list model per-button — it represents the 4 directions of ONE selected button; expose per-direction state as Q_PROPERTYs or a small fixed enum-keyed accessor). Mirror `ButtonsModel`'s seam exactly:
1. Live ctor `GestureModel(const QString& buttonPath, const QDBusConnection& bus, QObject*)` and a test ctor `GestureModel(QObject*)` with no bus.
2. Protected virtuals (the test-override seam, names the Plan-00 test expects): `virtual void performSetGesture(const QString& direction, const QString& type);` (issues `Action.Gesture.SetGesture` via the typed `PizzaPixlLogiOpsActionGestureInterface` async) and `virtual void performParamCall(const QString& direction, const QString& mode, const QString& method, const QVariantList& args);` (waits for SetGesture to land on the ordered connection, then opens a generic `QDBusInterface("pizza.pixl.LogiOps.Gesture."+mode)` at the `.../gestures/{direction}` child node and fires the setter — exact mirror of ButtonsModel::performParamCall).
3. `Q_INVOKABLE void setMode(const QString& direction, const QString& plainMode)`: validate `direction ∈ {up,down,left,right}` and map `plainMode` through the locked allowlist to a daemon `type` (OnInterval/OnRelease/Axis/None); on an unknown direction OR mode, emit a rejection signal and dispatch NOTHING (the T-04-02 validate-before-dispatch guard, mirroring `ButtonsModel::setChangeHost`). On success call `performSetGesture(direction, type)` then `markDirty()`.
4. Per-mode param setters (Q_INVOKABLE), each calling performParamCall AFTER setMode for that direction's current mode: `setGranularity(direction, int rawValue)` → routes to `SetInterval` (OnInterval) / `SetThreshold` (OnRelease) / `SetMultiplier` as double (Axis) per the current mode; `setGestureAction(direction, QString type)` → `SetAction` for the discrete modes. Clamp rawValue to sane positive bounds before dispatch (reject <=0 except the documented 0-reset sentinel) — the daemon takes these typed but the GUI must not send garbage (V5 Input Validation).
5. Expose the daemon mode-type allowlist as a single source of truth (a static table) so QML never hardcodes type strings.
6. Track per-direction {mode, action, granularity} state so the model can answer "is this direction configured (≠ Nothing)" for the UI-SPEC configured-dot, and recompose the preview (Task 3).
Wire `setConfigState(ConfigState*)` like ButtonsModel; markDirty on each successful edit.
  </action>
  <verify>
    <automated>cd /home/c4duz/Downloads/logiops && cmake --build build --target phase4_gesture_model_test >/dev/null 2>&1 && ctest --test-dir build -R phase4_gesture_model --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
- `src/logiops-gui/GestureModel.h` declares `performSetGesture` and `performParamCall` as protected virtuals.
- `GestureModel.cpp` contains the mode allowlist mapping "Repeat while moving"→"OnInterval", "Do once when moved far enough"→"OnRelease", "Adjust proportionally"→"Axis", "Nothing"→"None" (grep all four type strings; assert NO occurrence of `"OnThreshold"` or `"NoPress"` as a sent type).
- `setMode` rejects an unknown direction/mode without dispatch (the GestureModelTest order+rejection assertions pass).
- `ctest --test-dir build -R phase4_gesture_model` GREEN.
  </acceptance_criteria>
  <done>GestureModel drives the two-step mode-switch in correct order, sends only allowlisted types, rejects garbage before dispatch; model test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preview sentence + granularity human-readout composed in C++ (GEST-04)</name>
  <read_first>.planning/phases/04-fine-grained-gesture-control/04-UI-SPEC.md §Copywriting (the exact templates) + §5 (preview card is first-class), tests/phase4/GestureModelTest.cpp (the preview assertions), src/logiops-gui/GestureModel.h (the state added in Task 2)</read_first>
  <files>src/logiops-gui/GestureModel.h, src/logiops-gui/GestureModel.cpp</files>
  <behavior>
    - previewSentence recomposes whenever the active direction's mode, action, or granularity changes, and previewChanged() fires.
    - "Nothing" + up yields exactly "Moving up does nothing."; each mode uses its locked template; raw daemon mode names and HID++ units never appear.
    - The granularity readout maps a raw value to a human {amount} phrase ("small flick" … "long swipe"), tabular-stable.
  </behavior>
  <action>
1. Add `Q_PROPERTY(QString previewSentence READ previewSentence NOTIFY previewChanged)` and an `activeDirection` selector to `GestureModel`. Compose `previewSentence` in C++ from {activeDirection, mode, action, granularity} using the UI-SPEC §Copywriting templates verbatim:
   - Nothing → "Moving {direction} does nothing."
   - OnInterval → "Moving {direction} keeps {action} as long as you keep moving — about one step per {amount}."
   - OnRelease → "Moving {direction} far enough {action} once. A single flick does one step."
   - Axis → "Moving {direction} {action} in proportion to how far you move."
   {direction} is the lowercase cardinal word; {action} is a plain verb phrase derived from the chosen gesture action type (e.g. "switches a desktop", "raises the volume", "scrolls", "changes DPI"); {amount} is the granularity human phrase. Never surface raw mode names (OnInterval/Axis) or HID++ units. Emit `previewChanged()` on every mode/action/granularity/direction change.
2. Add a `Q_INVOKABLE QString granularityReadout(...)` (or a NOTIFYable property) that maps the current raw value to the "~1 step per {amount} of movement" phrase — leftmost stop → "small flick", rightmost → "long swipe" — so QML binds the finished string (UI-SPEC: zero JS logic; tabular-stable width is a QML/TextMetrics concern, the model just supplies the words).
3. Ensure all user-facing strings are produced such that QML can wrap them with qsTr at the binding site, OR mark them translatable here per the existing project convention (match ButtonsModel/ProfilesModel handling).
  </action>
  <verify>
    <automated>cd /home/c4duz/Downloads/logiops && cmake --build build --target phase4_gesture_model_test >/dev/null 2>&1 && ctest --test-dir build -R phase4_gesture_model --output-on-failure && cmake --build build --target logiops-gui-lib 2>&1 | grep -Eiq "error:" && echo "ERR" || echo "CLEAN"</automated>
  </verify>
  <acceptance_criteria>
- `GestureModel.h` exposes `Q_PROPERTY(QString previewSentence ... NOTIFY previewChanged)`.
- `GestureModel.cpp` contains the literal template fragments "does nothing.", "as long as you keep moving", "in proportion to how far you move".
- The GestureModelTest preview assertions pass (Nothing+up → "Moving up does nothing.").
- `ctest -R phase4_gesture_model` GREEN; `logiops-gui-lib` compiles with no `error:`.
  </acceptance_criteria>
  <done>previewSentence + granularity readout composed in C++ per UI-SPEC templates; NOTIFY fires on change; model test fully GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| QML/user input → GestureModel → root daemon | The model is the last GUI-side gate before untrusted direction/mode/granularity strings reach the root daemon's SetGesture and per-mode setters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | DoS / Tampering | GestureModel::setMode (direction/mode) | mitigate | Validate direction ∈ {up,down,left,right} and map mode through the locked allowlist BEFORE any D-Bus call (ButtonsModel::setChangeHost idiom); reject unknown with a signal and zero dispatch so the daemon never receives garbage. |
| T-04-02-02 | Tampering | mode→type mapping | mitigate | Single static allowlist table is the only source of type strings; NEVER send "OnThreshold" (interface-name collision → InvalidGesture) or "NoPress" (wrong; must be "None"). Asserted by the model test. |
| T-04-02-03 | DoS | granularity raw value | mitigate | Clamp interval/threshold/multiplier to sane positive bounds before dispatch; reject <=0 except the documented 0-reset sentinel (defense-in-depth with Plan 01's daemon divide-guard). |
| T-04-02-04 | DoS | two-step ordering | mitigate | performParamCall is sequenced AFTER performSetGesture lands (mirrors ButtonsModel) so a param setter is never called on a not-yet-created .Gesture.<mode> interface (UnknownMethod). |
| T-04-02-05 | Information disclosure | preview/readout strings | accept | Composed strings contain only plain-language copy and the user's own chosen action; no secrets, no raw daemon internals leaked to the user. |
</threat_model>

<verification>
- `ctest --test-dir build -R phase4_gesture_model --output-on-failure` GREEN (two-step order, mode allowlist, rejection, preview templates).
- `cmake --build build --target logiops-gui-lib` compiles clean (no `error:`; daemon CI is -Werror).
- The typed `Action.Gesture` proxy class generates from the trimmed XML.
</verification>

<success_criteria>
- `GestureModel.{h,cpp}` + `logid_action_gesture.xml` + CMake wiring exist.
- Mode allowlist sends only OnInterval/OnRelease/Axis/None; unknown rejected pre-dispatch.
- previewSentence + granularity readout composed in C++ per UI-SPEC; NOTIFY fires.
- phase4_gesture_model GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/04-fine-grained-gesture-control/04-02-SUMMARY.md`
</output>
