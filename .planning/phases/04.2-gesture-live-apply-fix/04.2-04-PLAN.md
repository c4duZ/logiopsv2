---
phase: 04.2-gesture-live-apply-fix
plan: 04
type: execute
wave: 3
depends_on: ["04.2-01", "04.2-02", "04.2-03"]
files_modified: []
autonomous: false
requirements: [GEST-05, GEST-01, GEST-02, GEST-03, GEST-04]
must_haves:
  truths:
    - "A gesture built in the GUI on a non-cfg button fires its action on hardware (MX Master 4)"
    - "Reopening the builder on that direction reads back the mode + action"
    - "Changing granularity from the GUI changes the on-hardware behavior"
    - "The builder copy/flow reads as the Options+ hold-and-move / preset-vs-custom model"
  artifacts:
    - path: ".planning/phases/04.2-gesture-live-apply-fix/04.2-04-SUMMARY.md"
      provides: "Recorded UAT result for the four Phase 4.2 success criteria"
      contains: "GEST-05"
  key_links:
    - from: "GUI gesture build (two-step)"
      to: "hardware fires the action"
      via: "daemon dispatch of the live-wired gesture child action"
      pattern: "fires"
---

<objective>
On-hardware UAT for Phase 4.2's four success criteria, gated as a human-verify checkpoint
after automation (build + install) has been done by Claude. The user has an MX Master 4.
This is the decisive proof that Bug A is fixed: a gesture built ENTIRELY in the GUI (on a
button that is NOT pre-configured in /etc/logid.cfg) actually fires on hardware, reads back
correctly, and respects granularity — and the builder speaks the Options+ vocabulary.

Purpose: GEST-05/01/02/03/04 — verify the live-apply fix + read-back + granularity +
UX alignment on real hardware.
Output: A SUMMARY recording the UAT result per criterion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-gesture-live-apply-fix/CONTEXT.md
@.planning/debug/gesture-live-apply-and-save.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + install the fixed daemon and GUI for on-hardware test</name>
  <read_first>
    .planning/debug/gesture-live-apply-and-save.md (the "Build/run reminder" at the end:
    run the GUI via ./build/src/logiops-gui/logiops-gui — the build binary finds the on-disk
    QML module; after any QML edit clear build/src/logiops-gui/.rcc + targeted rebuild for
    the Qt 6.4 stale-qmlcache; rebuild-gui.sh automates it). Note Bug B (Save/polkit) is
    already fixed + committed (70e9457): the daemon D-Bus policy template change must be
    INSTALLED + dbus reloaded for Save to work — but the live-apply path under test does NOT
    require Save (it applies live).
  </read_first>
  <action>
    Build the daemon (with the Plan 01 fix) and the GUI (with Plan 02 + 03), -Werror clean.
    Run the full phase4 CTest suite and confirm GREEN. Install the rebuilt daemon and restart
    logid so the running daemon carries the gesture child-action wiring fix (this requires
    privilege — if `sudo`/install fails with an auth error, create an auth gate / surface the
    exact commands for the user). Use rebuild-gui.sh (or the documented .rcc-clear + targeted
    rebuild) so no stale qmlcache masks the QML copy changes. Leave the GUI launchable via
    ./build/src/logiops-gui/logiops-gui. Do NOT require the installed /usr/bin/logiops-gui
    (it has the Qt-embedded-module bug noted in the debug file).
  </action>
  <verify>
    <automated>cd build && cmake --build . 2>&1 | grep -Ei "error|warning"; echo "build:$?"; ctest -R phase4 --output-on-failure</automated>
  </verify>
  <done>
    Daemon + GUI build -Werror clean; phase4 CTests GREEN; the fixed daemon is the running
    one (or the exact privileged install/restart commands are surfaced to the user); the GUI
    is launchable from the build tree with no stale qmlcache.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-hardware UAT of the four Phase 4.2 success criteria (MX Master 4)</name>
  <action>Pause for the user to run the four on-hardware checks below on the MX Master 4 and report PASS/FAIL per criterion. This is a verification-only checkpoint; all build/install automation is done in Task 1.</action>
  <what-built>
    The daemon gesture child-action wiring fix (Plan 01), the GUI gesture read-back of the
    bound action (Plan 02), and the Options+-aligned builder copy/flow (Plan 03), built and
    running on the user's machine with the MX Master 4.
  </what-built>
  <how-to-verify>
    Launch the GUI: `./build/src/logiops-gui/logiops-gui`. Select the MX Master 4.

    1. GEST-05 (live-apply / Bug A — the decisive test): pick a button that is NOT already a
       gesture in /etc/logid.cfg. Open the gesture builder, pick a direction, set the mode
       "Do once when moved far enough" (OnRelease), choose an action (e.g. a Keystroke like
       Volume Up, or a predefined media action). WITHOUT saving or restarting, hold that
       button and flick the chosen direction on the mouse.
       EXPECTED: the action fires on hardware (e.g. volume goes up). [Before this phase it
       engaged but never fired.]

    2. GEST-01 (read-back): close and reopen the builder on that same button + direction.
       EXPECTED: the builder shows the mode you set AND names the bound action — NOT "choose
       what this direction does" / "doing what you choose".

    3. GEST-02/03 (granularity): switch the direction (or another) to "Repeat while moving"
       (OnInterval) bound to a repeatable action (e.g. desktop switch / volume), then move
       the Granularity slider. Hold + move that direction in one continuous motion.
       EXPECTED: granularity visibly changes how often the action fires (e.g. one desktop per
       comfortable flick vs several per long swipe). Confirm a single flick = one step at the
       low end.

    4. GEST-04 + UX (Options+ alignment): read the builder copy.
       EXPECTED: directions read as the hold-and-move metaphor ("Hold + move up", etc.); the
       action choice reads as preset-vs-custom; the live preview sentence plainly states when
       and how often the action fires.

    Report PASS/FAIL per criterion (1-4) and any anomaly.
  </how-to-verify>
  <resume-signal>Type "approved" if all four pass, or describe which failed and how.</resume-signal>
</task>

</tasks>

<verification>
- phase4 CTests GREEN before the human test.
- User confirms criteria 1-4 PASS on the MX Master 4.
</verification>

<success_criteria>
- A GUI-built gesture on a non-cfg button fires on hardware (GEST-05).
- The builder reads back an existing gesture's mode + action (GEST-01).
- Granularity from the GUI changes on-hardware behavior (GEST-02/03).
- The builder wording/flow matches the Options+ hold-and-move / preset-vs-custom model and
  the preview explains when/how often the action fires (GEST-04).
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-gesture-live-apply-fix/04.2-04-SUMMARY.md`
recording PASS/FAIL per criterion and any follow-up gaps.
</output>
