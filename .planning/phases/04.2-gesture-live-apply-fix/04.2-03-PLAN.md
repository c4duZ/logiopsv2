---
phase: 04.2-gesture-live-apply-fix
plan: 03
type: execute
wave: 2
depends_on: ["04.2-02"]
files_modified:
  - src/logiops-gui/GestureModel.cpp
  - src/logiops-gui/qml/config/GestureBuilder.qml
  - tests/phase4/GestureModelTest.cpp
autonomous: true
requirements: [GEST-04, GEST-01]
must_haves:
  truths:
    - "The builder's direction labels read as the Options+ 'SEGURAR + MOVER PARA…' (hold + move) model"
    - "The mode/flow framing matches the Options+ predefinição (preset) vs personalizada (custom) vocabulary"
    - "The preview sentence still explains in plain language when/how often the action fires (GEST-04)"
  artifacts:
    - path: "src/logiops-gui/qml/config/GestureBuilder.qml"
      provides: "Direction + flow copy aligned to the Options+ hold-and-move / preset-vs-custom model"
      contains: "qsTr"
    - path: "src/logiops-gui/GestureModel.cpp"
      provides: "Preview/readout copy aligned to the Options+ vocabulary (composed in C++)"
      contains: "composePreview"
  key_links:
    - from: "GestureBuilder.qml direction labels"
      to: "the Options+ hold+move framing"
      via: "dirLabel / intro copy"
      pattern: "dirLabel"
    - from: "GestureModel::composePreview"
      to: "GEST-04 plain-language when/how-often sentence"
      via: "previewSentence binding"
      pattern: "previewSentence"
---

<objective>
Align the gesture builder's wording and flow to the mined Logitech Options+ model
(GEST-04 + GEST-01 UX rework). The real Options+ install (gitignored at
`LogiOptionsPlus/`) frames gestures as a "gesture button": hold the button and move in a
direction — strings confirmed present:
`GESTURE_ACTION_HOLD_MOVE_UP: "SEGURAR + MOVER PARA CIMA"` (and LEFT/RIGHT/DOWN),
`ASSIGNMENT_NAME_GESTURE: Gestos`, `ASSIGNMENT_NAME_CUSTOM_GESTURE: Personalizada`,
`GESTURE_CARD_DESCRIPTION: "Escolha uma predefinição ou selecione uma personalizada para
criar sua própria predefinição."` (choose a preset or pick a custom one to build your own).
Our builder currently labels directions bare ("Up"/"Down") and frames modes as raw
behaviors. Rework the copy/flow so directions read as the hold-and-move metaphor and the
action choice reads as the preset-vs-custom model — in OUR OWN English strings (hybrid /
reference-only: adapt vocabulary, do NOT bundle Logitech assets), while keeping the
plain-language preview that GEST-04 requires. Business copy stays composed in C++
(GestureModel) per the locked "zero logic in QML" rule; QML renders only.

NOTE: Phase 4.1's `.planning/intel/options-plus/vocabulary.md` is the intended canonical
source but has NOT been executed yet (4.1 plans are still open). Ground this work directly
on the confirmed real source strings in `LogiOptionsPlus/data/strings/pt-BR.yaml` (and the
en-US equivalents in the same dir) — read them as `read_first`. If vocabulary.md exists at
execution time, prefer it.

Purpose: GEST-04 / GEST-01 — the builder speaks the Options+ vocabulary/flow.
Output: Aligned copy in GestureBuilder.qml + GestureModel preview/readout; tests updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.2-gesture-live-apply-fix/CONTEXT.md

<interfaces>
<!-- Confirmed Options+ source strings (LogiOptionsPlus/data/strings/pt-BR.yaml). -->
<!-- Adapt the VOCABULARY into our own English copy — do NOT copy Logitech assets. -->
GESTURE_ACTION_HOLD_MOVE_UP    = "SEGURAR + MOVER PARA CIMA"      (hold + move up)
GESTURE_ACTION_HOLD_MOVE_DOWN  = "SEGURAR + MOVER PARA BAIXO"     (hold + move down)
GESTURE_ACTION_HOLD_MOVE_LEFT  = "SEGURAR + MOVER PARA ESQUERDA"  (hold + move left)
GESTURE_ACTION_HOLD_MOVE_RIGHT = "SEGURAR + MOVER PARA DIREITA"   (hold + move right)
ASSIGNMENT_NAME_GESTURE        = "Gestos"        (Gestures)
ASSIGNMENT_NAME_CUSTOM_GESTURE = "Personalizada" (Custom)
GESTURE_CARD_DESCRIPTION       = "Escolha uma predefinição ou selecione uma personalizada
                                  para criar sua própria predefinição."
                                  (Choose a preset, or pick custom to build your own.)

<!-- Where the copy lives today (must stay in C++; QML binds only): -->
GestureModel.cpp (src/logiops-gui/):
  plainModes()                -> the 4 mode labels (single source of truth, GestureBuilder
                                 binds via gestureModel.plainModes())
  composePreview()/previewSentence  -> GEST-04 plain-language sentence (lines 431-451)
  granularityReadout()/granularityPhrase()  -> the human "~1 step per …" readout (456-466)
  actionVerbPhrase()          -> per-action verb phrase (469-485)
GestureBuilder.qml (src/logiops-gui/qml/config/):
  dirLabel(d)                 -> "Up"/"Down"/… (lines 43-49) — rework to the hold+move framing
  intro Text                  -> "Pick a direction, then choose what it does." (61)
  "Choose what this direction does." button (196-202) + GesturePicker (predefinição vs
   "Custom keystroke…" = personalizada) — align the preset/custom framing
NOTE: plainModes() strings are the allowlist KEYS that GestureModel.mapMode matches on
(GestureModel.cpp:191-209) AND that GestureBuilder's showsAction/showsGranularity compare
against (GestureBuilder.qml:173-176). If you change a mode LABEL, update mapMode, the
showsAction/showsGranularity comparisons, and the GestureModelTest expectations together —
they are a coupled triple. Prefer changing FRAMING/intro/direction copy over renaming the
mode allowlist keys to minimize coupling churn; if you do rename, keep all four in lockstep.
</interfaces>

@src/logiops-gui/GestureModel.cpp
@src/logiops-gui/qml/config/GestureBuilder.qml
@src/logiops-gui/qml/config/GesturePicker.qml
@tests/phase4/GestureModelTest.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework builder copy/flow to the Options+ hold-and-move + preset-vs-custom model</name>
  <read_first>
    LogiOptionsPlus/data/strings/pt-BR.yaml (the confirmed gesture strings above; also grep
    the en-US yaml in the same dir for the English originals), src/logiops-gui/qml/config/
    GestureBuilder.qml (dirLabel:43-49, intro:59-65, active-direction label:101-109,
    action section:184-221, GesturePicker usage), src/logiops-gui/qml/config/GesturePicker.qml
    (the predefined-vs-"Custom keystroke…" picker — the preset/custom split), src/logiops-gui/
    GestureModel.cpp (plainModes, composePreview, granularityReadout, actionVerbPhrase).
  </read_first>
  <action>
    Adapt the vocabulary (our own English, no bundled Logitech assets):
    1. Direction framing: rework GestureBuilder.qml's dirLabel + the active-direction label
       + intro copy so each direction reads as the hold-and-move metaphor ("Hold + move up",
       etc., the English of GESTURE_ACTION_HOLD_MOVE_UP), reinforcing that a gesture =
       hold the button and move. Keep the 4-cardinal cross UI; only the COPY changes.
    2. Preset-vs-custom flow: align the action section so choosing the gesture's action reads
       as "pick a preset action, or build a custom one" (adapt GESTURE_CARD_DESCRIPTION /
       ASSIGNMENT_NAME_GESTURE / ASSIGNMENT_NAME_CUSTOM_GESTURE). The existing GesturePicker
       already lists predefined actions + a "Custom keystroke…" escape — relabel/frame it as
       presets ("Predefinição") vs custom ("Personalizada") in English. The "Choose what this
       direction does." trigger copy should match the new framing.
    3. Preview/readout (GEST-04): keep the plain-language when/how-often preview in
       composePreview, but harmonize its phrasing with the new hold-and-move vocabulary so it
       reads consistently (e.g. "Hold and move {dir} …"). The preview MUST still state when
       and how often the action fires (GEST-04 success criterion). Keep all business copy in
       C++ (GestureModel); QML binds the finished strings only.
    4. If you rename any plainModes() label, update mapMode + GestureBuilder's
       showsAction/showsGranularity comparisons + GestureModelTest expectations in lockstep
       (the coupled triple noted in interfaces). Update GestureModelTest.cpp for any changed
       preview/readout assertions. Keep the GUI lib + tests -Werror clean.
  </action>
  <verify>
    <automated>cd build && cmake --build . --target logiops-gui phase4_gesture_model_test 2>&1 | grep -Ei "error|warning"; echo "build:$?"; ctest -R phase4_gesture_model --output-on-failure</automated>
  </verify>
  <done>
    Builder direction labels read as hold-and-move; the action choice reads as preset-vs-
    custom; the preview still states when/how often the action fires; mode allowlist coupling
    (if touched) stays consistent across mapMode/QML/tests; phase4_gesture_model GREEN; GUI
    builds -Werror clean. No Logitech asset is bundled (English adaptation only).
  </done>
</task>

</tasks>

<verification>
- `cd build && cmake --build .` -Werror clean; QML loads clean.
- `ctest -R phase4_gesture_model --output-on-failure` GREEN.
- grep confirms hold-and-move framing present: `grep -ni "hold" src/logiops-gui/qml/config/GestureBuilder.qml`.
- No Logitech string copied verbatim (English adaptation; spot-check the diff).
</verification>

<success_criteria>
- The gesture builder's wording/flow matches the mined Options+ hold-and-move + preset-vs-
  custom model (GEST-04 success criterion 4).
- The plain-language preview still explains when and how often the action fires (GEST-04).
- Hybrid/reference-only boundary respected (no bundled proprietary assets).
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-gesture-live-apply-fix/04.2-03-SUMMARY.md`
</output>
