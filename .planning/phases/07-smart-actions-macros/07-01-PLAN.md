---
phase: 07-smart-actions-macros
plan: 01
type: execute
wave: 2
depends_on: [00]
files_modified:
  - src/logid/config/schema.h
  - src/logid/actions/SmartAction.h
  - src/logid/actions/SmartAction.cpp
  - src/logid/actions/Action.cpp
  - src/logid/CMakeLists.txt
  - src/logid/ipc_defs.h
  - src/session-helper/IPC_CONTRACT.md
autonomous: true
requirements: [MACRO-01, MACRO-02]

must_haves:
  truths:
    - "A new `SmartAction` action exists, selectable as a button binding type (\"SmartAction\") via the existing makeAction factory"
    - "A Smart Action holds an ordered list of steps (Keypress, Text, Delay, Launch) persisted in libconfig and exposed over D-Bus"
    - "Pressing the button fires the steps in order, scheduling inter-step delays via run_task_after (never blocking the HID/event thread)"
    - "Launch/URL steps are NEVER spawned by the root daemon — they are emitted on a D-Bus signal/call to a non-root user-session helper whose contract is documented"
    - "The daemon still builds -Werror clean"
  artifacts:
    - path: "src/logid/actions/SmartAction.h"
      provides: "SmartAction : public Action — ordered step list, D-Bus step CRUD, press() sequencer"
      contains: "class SmartAction"
    - path: "src/logid/actions/SmartAction.cpp"
      provides: "Step sequencer using run_task_after; Launch steps routed to session-helper IPC (never fork/exec)"
      contains: "run_task_after"
    - path: "src/logid/config/schema.h"
      provides: "config::SmartAction (signed_group type=\"SmartAction\") with ordered step list; added to the config::Action variant"
      contains: "SmartAction"
    - path: "src/session-helper/IPC_CONTRACT.md"
      provides: "The D-Bus contract the Phase-5 non-root session helper must expose for Launch/OpenURL, consumed by SmartAction"
      min_lines: 30
  key_links:
    - from: "src/logid/actions/Action.cpp"
      to: "config::SmartAction"
      via: "makeAction factory branch on interface_name \"SmartAction\""
      pattern: "SmartAction::interface_name"
    - from: "src/logid/actions/SmartAction.cpp"
      to: "run_task_after"
      via: "delay-step scheduling on the worker pool"
      pattern: "run_task_after"
    - from: "src/logid/actions/SmartAction.cpp"
      to: "session helper"
      via: "D-Bus emit/call to the user-session helper for Launch/URL (no daemon fork/exec)"
      pattern: "Launch|OpenURL|RequestLaunch"
---

<objective>
Add the daemon-side composite Smart Action: an ordered list of Keystroke / Text /
Delay / Launch steps bound to one button, firing in order with inter-step delays
scheduled on the worker pool (MACRO-01). Launch-app / open-URL steps are NEVER run
by the root daemon — they are delegated to a non-root user-session helper via a
documented D-Bus contract (MACRO-02). The session helper itself ships in Phase 5;
this plan defines and consumes its contract.

Purpose: This is the core feature. The composite action slots into the existing
Action/makeAction/schema machinery exactly like KeypressAction, reusing the worker
pool's run_task_after for delays (locked architecture). The root-never-spawns rule
is a hard security boundary, not a convenience.
Output: SmartAction action + config schema + factory wiring + session-helper IPC
contract doc. Daemon stays -Werror clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<read_first>
- .planning/intel/options-plus/smart-action-schema.md — the mined Options+ step/card
  vocabulary (created when Phase 4.1 Plan 02 executes). GROUND the step kinds on it.
  If this file does not yet exist at execution time, fall back to the REAL source:
  LogiOptionsPlus/data/macros/predefined_win.json (step "type" values KEYSTROKE,
  TEXT_BLOCK, DELAY{durationMs}, INPUT_SEQUENCE, plus OPEN_FILE_FOLDER /
  APP_WINDOWS_MANAGEMENT for the launch-app class). RE-IMPLEMENT our own schema;
  NEVER copy Logitech files into src/.
- src/logid/actions/KeypressAction.h + KeypressAction.cpp — the exact Action +
  ipcgull interface pattern to mirror (interface_name, ctor registering D-Bus
  methods via the tables{} arg, press()/release()/reprogFlags(), _setConfig()).
- src/logid/actions/Action.cpp — the makeAction factory; add a "SmartAction" branch
  to _makeAction (alongside Keypress/CycleDPI/...) AND to the variant-visiting
  overloads. Note GestureAction is handled specially in the config::Action overload
  (catch InvalidAction) because it is in the Action variant but not BasicAction —
  SmartAction is the same case (composite, Action-only, NOT a BasicAction).
- src/logid/config/schema.h — add config::SmartAction and append it to the
  config::Action variant (lines ~231-241). Study how GestureAction's nested map and
  KeypressAction's optional<variant<...>> keys are declared with signed_group.
- src/logid/util/task.h — run_task_after(fn, std::chrono::milliseconds) is the
  delay primitive (locked). run_task(fn) for the immediate first step.
- src/logid/InputDevice.h — pressKey/releaseKey/registerKey/toKeyCode for Keypress
  + Text steps (Text = per-character toKeyCode + press/release). moveAxis unused here.
- src/logid/ipc_defs.h — SERVICE_ROOT_NAME = "pizza.pixl.LogiOps"; action interfaces
  are SERVICE_ROOT_NAME ".Action." + name.
</read_first>

<interfaces>
<!-- Contracts this plan must produce. The GUI (Plan 02/03) consumes these. -->

D-Bus interface (mirror KeypressAction's tables{} registration):
  pizza.pixl.LogiOps.Action.SmartAction at the button's action node:
    GetSteps()                       -> a(...) ordered step descriptors
    AddStep(kind, payload)           -> append a step (kind: "Keypress"|"Text"|"Delay"|"Launch")
    RemoveStep(index)                -> remove step at index (bounds-checked)
    MoveStep(fromIndex, toIndex)     -> reorder (bounds-checked)
    SetStepPayload(index, payload)   -> edit a step's payload (keys/text/durationMs/target)
  (Exact D-Bus signatures are the executor's discretion — match ipcgull's
   supported marshalling as KeypressAction's SetKeys(vector<string>) does. Keep
   payloads simple/typed; document them in the SUMMARY.)

config::SmartAction (schema.h), signed_group<std::string>("type","SmartAction"):
  ordered list of step groups. Each step is a signed_group("kind", <Kind>) with:
    Keypress: keys (reuse the KeypressAction keys variant shape)
    Text:     text (std::string)
    Delay:    durationMs (int, >= 0)
    Launch:   target (std::string), launchKind ("App"|"URL")
  Append config::SmartAction to the `config::Action` variant (NOT BasicAction).

Session-helper IPC contract (define in src/session-helper/IPC_CONTRACT.md):
  The daemon (root) must NOT fork/exec or open URLs. On a Launch step it issues a
  D-Bus request to a per-user-session helper that Phase 5 will implement. Specify:
    - bus: session bus (the helper runs in the user session, not the system bus)
    - well-known name + object path + interface (propose, e.g.
      pizza.pixl.LogiOpsSession / .Launcher with RequestLaunch(kind, target))
    - how the root daemon reaches the user session (the documented mechanism;
      e.g. the GUI/helper subscribes to a daemon signal, OR the daemon emits a
      system-bus signal the helper relays — pick one and document the rationale +
      the Phase-5 dependency explicitly).
  This plan WIRES SmartAction to emit that request (signal or stub call); the
  helper that consumes it lands in Phase 5. Until then, Launch steps emit the
  request and log — they MUST NOT spawn anything from the daemon.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: config::SmartAction schema + ordered step list</name>
  <files>src/logid/config/schema.h</files>
  <read_first>src/logid/config/schema.h (KeypressAction ~67-77, GestureAction ~220-229, config::Action variant ~231-241)</read_first>
  <action>
    Add `struct SmartAction : public signed_group<std::string>` with tag
    ("type","SmartAction"). It owns one optional ordered field: a list of step
    groups (use std::list to preserve order, matching the codebase's list usage).
    Define the four step kinds as signed_group<std::string>("kind", <Kind>):
      - StepKeypress: keys (reuse the exact optional<variant<string,uint,
        list<variant<uint,string>>>> shape from config::KeypressAction so Text/Keypress
        share KeyNameMapper vocabulary)
      - StepText: optional<std::string> text
      - StepDelay: optional<int> durationMs
      - StepLaunch: optional<std::string> target; optional<std::string> launchKind
    Wrap a step as a std::variant<StepKeypress, StepText, StepDelay, StepLaunch>
    and make SmartAction hold optional<std::list<that-variant>> steps.
    Append `SmartAction` to the `config::Action` typedef variant (the one that
    already contains GestureAction). Do NOT add it to BasicAction (composite actions
    are not basic — same rule GestureAction follows). Forward-declare
    actions::SmartAction at the top alongside the other action class fwd-decls, and
    add `typedef actions::SmartAction action;` inside the struct.
  </action>
  <verify>
    <automated>grep -q "struct SmartAction" src/logid/config/schema.h &amp;&amp; grep -q "SmartAction" src/logid/config/schema.h</automated>
  </verify>
  <done>config::SmartAction with an ordered 4-kind step list compiles into the schema and is a member of the config::Action variant; not in BasicAction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SmartAction action — D-Bus step CRUD + run_task_after sequencer</name>
  <files>src/logid/actions/SmartAction.h, src/logid/actions/SmartAction.cpp, src/logid/actions/Action.cpp, src/logid/CMakeLists.txt</files>
  <read_first>src/logid/actions/KeypressAction.h, src/logid/actions/KeypressAction.cpp, src/logid/actions/Action.cpp, src/logid/util/task.h, tests/phase7/smart_action_sequence_test.cpp</read_first>
  <behavior>
    - press() fires steps in index order; Keypress/Text steps emit input immediately,
      a Delay(durationMs) step schedules the REMAINDER of the sequence via
      run_task_after(remainder, durationMs) — the press() call returns without
      blocking the HID/event thread (matches sequence_test Test B cumulative-offset
      contract).
    - Steps fire in order 0..n-1 (sequence_test Test A).
    - A Launch step does NOT press a key or type text from the daemon; it routes to
      the session-helper request path (sequence_test Test C "delegate" contract).
    - reprogFlags() returns TemporaryDiverted (like KeypressAction) so the button is
      diverted to the daemon.
    - Bounds-checking: RemoveStep/MoveStep/SetStepPayload reject out-of-range indices
      (V5 input validation — never index past the list).
  </behavior>
  <action>
    Create SmartAction.h/.cpp mirroring KeypressAction: `class SmartAction : public
    Action`, `static const char* interface_name = "SmartAction"`, ctor registering
    the GetSteps/AddStep/RemoveStep/MoveStep/SetStepPayload D-Bus methods via the
    Action(device, interface_name, tables{...}) pattern, holding `config::SmartAction&
    _config` and a built std::vector of runtime step objects (rebuilt by a private
    _setConfig() like KeypressAction). For Keypress steps reuse the
    InputDevice::toKeyCode/registerKey/pressKey/releaseKey path; for Text steps map
    each character to a keycode (toKeyCode) and press+release in turn.
    press(): build a chained continuation. Walk the step list; for non-delay steps
    run them, for a Delay step capture the remaining steps in a continuation and
    schedule it with run_task_after(cont, std::chrono::milliseconds(durationMs)).
    Use the worker pool only — NEVER std::this_thread::sleep on the event thread.
    Guard the runtime step list with _config_mutex (shared_lock in press(), unique
    in the setters), matching KeypressAction.
    For a Launch step: call a private _requestLaunch(kind, target) that issues the
    session-helper D-Bus request defined in Task 3's IPC contract (emit a signal or
    a stubbed async call) and logs; it MUST NOT fork/exec/system()/open a URL from
    the daemon. Add a clear comment that the consuming helper lands in Phase 5.
    Wire the factory: in src/logid/actions/Action.cpp add a `SmartAction::interface_name`
    branch to _makeAction (config = config::SmartAction()), and handle it in the
    config::Action makeAction overload the SAME way GestureAction is (catch
    InvalidAction → set config::SmartAction → makeAction), since it is in the Action
    variant but not BasicAction. Add SmartAction.cpp to src/logid/CMakeLists.txt
    next to the other actions/*.cpp entries.
  </action>
  <verify>
    <automated>cmake --build build --target logid 2>&amp;1 | tee /tmp/p7_build.log; grep -qiE "error:|\\-Werror" /tmp/p7_build.log &amp;&amp; (echo BUILD_FAILED; exit 1) || grep -q "run_task_after" src/logid/actions/SmartAction.cpp</automated>
  </verify>
  <done>SmartAction builds -Werror clean; selectable via makeAction("SmartAction"); press() sequences steps with run_task_after for delays; Launch routes to _requestLaunch (no daemon spawn); index setters are bounds-checked.</done>
</task>

<task type="auto">
  <name>Task 3: Session-helper IPC contract (MACRO-02 boundary)</name>
  <files>src/session-helper/IPC_CONTRACT.md, src/logid/ipc_defs.h</files>
  <read_first>src/logid/ipc_defs.h, ROADMAP.md Phase 5 (session helper introduced there)</read_first>
  <action>
    Write src/session-helper/IPC_CONTRACT.md defining the D-Bus contract the
    Phase-5 non-root user-session helper MUST expose so SmartAction's Launch steps
    execute OUTSIDE the root daemon: bus (session), well-known name, object path,
    interface, and the RequestLaunch(kind, target) method (kind ∈ App|URL).
    Document the chosen daemon→user-session reach mechanism and WHY (the root
    daemon cannot open the user's session/URLs; the helper runs as the logged-in
    user). State the explicit Phase-5 dependency: the helper is built in Phase 5;
    Phase 7 only emits the request. Document the security rationale: root never
    fork/exec/system()/xdg-open; the helper validates and executes in user context.
    If SmartAction needs a stable signal/interface name constant, add it to
    ipc_defs.h next to SERVICE_ROOT_NAME (e.g. a SERVICE_SESSION_NAME or the
    Launch-request signal name) so daemon + future helper agree on one symbol.
  </action>
  <verify>
    <automated>test -f src/session-helper/IPC_CONTRACT.md &amp;&amp; grep -qiE "RequestLaunch|session" src/session-helper/IPC_CONTRACT.md</automated>
  </verify>
  <done>IPC_CONTRACT.md fully specifies the session-helper Launch interface, the daemon→user reach mechanism, the Phase-5 dependency, and the root-never-spawns security rationale; any shared name constant lives in ipc_defs.h.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root daemon → user session | A Launch/URL step must execute as the user, never as root |
| D-Bus client → SmartAction | step indices/payloads from an unprivileged caller are untrusted |
| HID event thread → worker pool | delays must not block the real-time HID/event thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01-01 | Elevation of Privilege | Launch step | mitigate | Daemon NEVER fork/exec/system()/xdg-open; Launch routes to a non-root session helper over the documented D-Bus contract (MACRO-02 hard boundary) |
| T-7-01-02 | Tampering | RemoveStep/MoveStep/SetStepPayload | mitigate | Bounds-check every index against the step-list size before access; reject out-of-range with no mutation |
| T-7-01-03 | Denial of Service | press() sequencer | mitigate | Delays scheduled via run_task_after on the worker pool — never sleep on the HID/event thread; clamp durationMs to a sane non-negative max |
| T-7-01-04 | Tampering | Text/Keypress step | mitigate | Map characters/keys via InputDevice::toKeyCode and skip-with-WARN on invalid codes (mirrors KeypressAction::_setConfig); never index raw |
| T-7-01-05 | Denial of Service | Launch target string | accept | Target string validation/escaping is the session helper's responsibility (Phase 5); daemon only forwards an opaque string, documented in the contract |
</threat_model>

<verification>
- `cmake --build build --target logid` succeeds -Werror clean.
- A config with a button bound to `{ type: "SmartAction"; steps: (...) }` parses and
  round-trips through Configuration::save() (Phase 3 machinery) without loss.
- Introspecting the button's action node shows pizza.pixl.LogiOps.Action.SmartAction
  with GetSteps/AddStep/RemoveStep/MoveStep/SetStepPayload.
- A Launch step, when fired, produces a session-helper request + log line and spawns
  NOTHING from the daemon (grep the source: no system()/exec*/fork/popen/xdg-open in
  SmartAction.cpp).
</verification>

<success_criteria>
SmartAction is a selectable button binding; ordered Keypress/Text/Delay/Launch steps
persist and fire in order; delays use run_task_after; Launch is delegated to the
documented session-helper contract and never spawned by root; daemon -Werror clean.
</success_criteria>

<output>
After completion, create `.planning/phases/07-smart-actions-macros/07-01-SUMMARY.md`
</output>
